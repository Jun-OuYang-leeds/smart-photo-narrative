# Smart Photo Narrative：论文思路与实验跟踪

> 创建日期：2026-07-16  
> 当前阶段：v2 工程基线与 376 张个人照片索引已完成；个人 Scene Graph 生成和人工 qrels 正式实验待完成  
> 主项目：`smart-photo-narrative`  
> 复现与方法参考：`Simulate SnapSeek3.0`

## 1. 文档目的

本文件用于持续记录项目从原始智能相册到多模态检索与可信照片叙事系统的演进，包括：

- 每项设计决策产生的原因；
- 从相关论文中吸收了什么，以及没有照搬什么；
- 每次实验的假设、配置、指标和结果；
- 消融实验如何证明各模块的独立贡献；
- 失败实验、局限和后续调整；
- 可以直接转化为论文方法、实验和讨论章节的证据。

记录时使用以下状态，避免把“计划”误写成“已经证明”：

- **已验证**：有代码、测试或实验结果支持；
- **已实现**：功能已经完成，但尚未经过正式实验；
- **计划实验**：已有明确假设和实验设计；
- **研究设想**：值得探索，但尚未排入实现；
- **放弃/推迟**：记录不采用的原因。

## 2. 项目目标与研究定位

### 2.1 一句话目标

构建一个面向个人照片的多模态智能检索与叙事系统：系统融合 CLIP 视觉语义、BLIP caption、Qwen Scene Graph 和照片元数据，帮助用户准确找到照片，并基于可追溯的照片证据生成个人日记或故事。

### 2.2 与现有 LSC 系统的关系

现有 LifeSeeker、SnapSeek 和 Memento 等系统主要研究：

> 如何在连续 lifelog 中，通过语义检索、元数据过滤、时序上下文和交互反馈，尽快定位正确图片或事件。

本项目在上游与它们相同，也需要解决多模态检索；区别在于本项目还要完成：

> 检索个人照片 → 组织照片或事件 → 生成有照片证据、少幻觉的叙事。

因此，本项目不是单纯复现 SnapSeek，也不是只做 caption 或故事生成，而是把多模态检索作为底层，将原有 Narrative 功能扩展为检索驱动、证据可追溯的照片叙事。

### 2.3 初步研究问题

- **RQ1：检索贡献**  
  融合 CLIP、BLIP caption、Qwen Scene Graph 和元数据，是否比 CLIP 单独检索取得更好的 Recall、MRR 和 nDCG？

- **RQ2：查询类型与融合**  
  不同查询类型是否需要不同通道，例如视觉查询依赖 CLIP、关系查询依赖 Scene Graph、精确文字查询依赖 caption/OCR、时间地点查询依赖 metadata？

- **RQ3：叙事可信度**  
  将检索结果、Scene Graph 和照片 ID 作为叙事证据，是否能降低原始故事生成功能中的无依据内容？

## 3. 当前系统基线

### 3.1 已有功能

当前 `smart-photo-narrative` 已经具备：

- Streamlit 照片画廊；
- EXIF 时间、GPS、尺寸和文件信息提取；
- CLIP 图像向量与文本到图像检索；
- CLIP 零样本 tags；
- BLIP caption；
- ChromaDB 持久化索引；
- vector、keyword 和 hybrid 三种搜索模式；
- 按日期选择照片；
- 使用 Ollama 生成英文 Daily Story。

现有故事流程位于 [`story_agent.py`](story_agent.py)，界面位于 [`app.py`](app.py)。故事功能是原始项目已经具备的核心能力，后续工作是增强它，而不是重新增加一个故事模块。

### 3.2 改造前基线：并非完整多路融合

以下内容记录 2026-07-16 改造前的原始基线，用于说明为什么需要本轮升级：

```text
CLIP 召回候选
    ↓
在 CLIP 候选中统计 caption 单词命中
    ↓
固定权重重新排序
```

现有 keyword 模式使用 caption 子串包含匹配；hybrid 模式只在最多 100 个 CLIP 候选中加入 caption 单词分数。因此：

- 当前没有 BM25/SQLite FTS5；
- caption 强命中但 CLIP 未召回的照片仍可能被遗漏；
- 当前没有 Qwen Scene Graph 检索通道；
- 当前 hybrid 分数不能直接解释为统一的“相似度百分比”。

### 3.3 2026-07-16 改造前审计发现的工程状态

本节是失败点与改造动机的历史记录，不代表当前 v2 状态。

- caption comparison 的 4 个现有单元测试通过；
- 当前测试主要覆盖 caption 文件生成，尚未覆盖索引、过滤、融合和故事忠实度；
- 当前环境使用 ChromaDB 0.6.3，打开现有 `chroma_db` 时出现 `KeyError: '_type'`；
- `requirements.txt` 使用 `chromadb>=0.4.22`，尚未锁定兼容版本；
- 当前批处理会先将所有图片加载进内存，无法安全扩展到 10,056 张图片；
- `BATCH_SIZE=32` 对 RTX 3050 Ti 4GB 显存偏大；
- 中间图片损坏时，过滤后的特征列表可能与原始 metadata 列表错位；
- 无 EXIF 时间时使用文件修改时间，但尚未记录时间来源和可信度。

这些问题属于实验前必须稳定的基础条件，否则后续结果可能无法复现或存在错误关联。

### 3.4 2026-07-17 当前 v2 已验证状态

- 主存储采用 SQLite（WAL、外键、schema migration、FTS5/BM25），向量采用版本化 ChromaDB 1.5.1；
- `photo_id` 使用稳定 UUID，文件关联同时保存 SHA-256、相对路径、大小与修改时间；
- CLIP、BLIP 直接复用本机 `torchtest` 环境，不新增重复模型；
- CLIP 图像、BLIP Caption/BM25、Scene Graph 三路可独立召回，metadata 在召回前生成 eligible IDs，再使用加权 RRF 融合；
- 已实现文本检索、以图搜图、日期/地点/tag 过滤、关系查询、双事件 before-after 和单锚点前后邻域查询；
- 已实现按日期、时间间隔、相邻 CLIP 变化和 GPS 距离组织事件；
- 原有 Story 功能已扩展为故事篮、日期、事件三种入口，并要求结构化照片证据引用；
- Qwen 仅保留为 AIRE 离线生成模块，本地负责带 dataset/photo/hash 的安全导出、审计和导入；
- A0--A4 消融评测器已经实现，但在完成人工 qrels 前拒绝输出伪造准确率。

## 4. 最近三轮讨论形成的研究决策

### 4.1 第一轮：相关论文如何实现较精确的图片检索

#### 论文共同范式

相关系统的准确性通常不是来自一个模型，而是来自以下组合：

```text
离线多模态索引
    ↓
查询拆解
    ↓
时间/地点等结构化过滤
    ↓
图像、文本、关系等独立召回
    ↓
融合排序与时间上下文
    ↓
用户交互、反馈和查询修改
```

从论文中得到的具体启示：

- **CLIP/OpenCLIP 应作为主视觉召回通道。**  
  lifeXplore 2025 的实验表明，直接 OpenCLIP 图像特征明显优于 BLIP2/GPT caption 检索。

- **时间和地点应作为召回前过滤条件。**  
  将查询中的时间、地点等非视觉条件拆出，通常比继续增加生成模型更有效。

- **Qwen Scene Graph 适合补充关系查询。**  
  例如 `(person, using, laptop)` 比普通 caption 更容易支持“谁对什么做了什么”的查询。

- **BLIP caption 是辅助文本证据。**  
  它适合展示、精确词语搜索和故事输入，不应取代直接图像向量。

- **事件与时间上下文适用于连续 lifelog。**  
  Memento 的事件组织和 SnapSeek 的 SeqWin/ParChain 依赖连续、可信的时间序列，不能直接在当前 376 张非连续个人照片上声称有效。

#### 本项目与论文目标的差异

| 维度 | 相关 LSC 论文 | 本项目 |
|---|---|---|
| 主要任务 | 在连续 lifelog 中快速找到目标帧 | 找到个人照片并生成记忆叙事 |
| 典型用户 | 竞赛熟练操作者 | 普通照片用户 |
| 数据 | 连续第一视角照片和较完整元数据 | 当前为普通个人照片，另有 Geograph 实验数据 |
| 输出 | 排序图片、QA答案、相邻帧 | 图片、事件、日记/故事和证据 |
| 评价 | 命中率、速度、竞赛得分 | 检索指标＋叙事忠实度 |

**形成的决策：**以多模态检索为可量化的技术核心，以现有故事功能作为最终应用和差异化展示。

### 4.2 第二轮：主项目当前应进行的改进

改进顺序确定为：

#### P0：先稳定数据和索引

1. 固定依赖和索引 schema；
2. 解决 ChromaDB 版本不兼容，或迁移到 SQLite＋FAISS/NumPy；
3. 修复损坏图片导致的 features/metadata 错位；
4. 使用稳定 `photo_id` 和内容哈希关联照片、caption、Scene Graph 与向量；
5. 保存 `timestamp_source` 和 `timestamp_confidence`；
6. 改为逐批懒加载，本机 batch size 初始设为 4–8；
7. 支持 checkpoint、upsert、文件更新和中断恢复。

#### P1：建立真正的多通道检索

计划中的独立通道：

```text
CLIP image top-N
BLIP caption BM25/FTS5 top-N
Qwen Scene Graph semantic/relationship top-N
metadata eligible IDs
        ↓
候选并集
        ↓
RRF 或经过验证的融合方式
        ↓
最终 top-k 与命中解释
```

每个结果至少记录：

```text
channel_scores
channel_ranks
matched_modalities
matched_caption_terms
matched_scene_graph_triples
filters_applied
```

#### P2：完成检索与 Narrative 的闭环

```text
检索照片
    ↓
用户选择/调整照片
    ↓
形成故事证据集合
    ↓
调用原有故事生成功能
    ↓
显示故事和照片引用
```

### 4.3 第三轮：确认故事功能是原有能力

需要在论文中准确表述：

- Story/Daily Diary 不是后续新增的功能；
- 原始项目已经实现按日期汇总照片并调用 Ollama 生成日记；
- 后续创新点不是“加入故事生成”，而是增强现有故事功能的输入、可控性和证据追溯。

当前故事基线是：

```text
日期
  → BLIP caption、tags、时间、地点
  → Ollama prompt
  → 英文日记
```

计划增强为：

```text
检索或事件中的照片
  → BLIP caption＋Qwen Scene Graph＋可靠 metadata＋用户备注
  → 带 photo_id 的结构化证据
  → 忠实模式或创意模式
  → 带照片引用的故事
```

故事增强的重点：

- 保留现有 Story 页面和 Ollama 调用；
- 默认提供低温度“忠实记录”模式；
- 将创意故事与事实型日记明确区分；
- 单张照片只生成照片笔记，不自动扩写成完整一天；
- 每个事实段落引用实际照片 ID；
- BLIP 与 Scene Graph 冲突时标记“不确定”；
- 用户能够从检索结果选择照片加入故事。

## 5. 图片内容转录：核心消融实验

用户提供的截图提出以下五组配置。由于截图位于系统临时目录，文字在此永久转录：

```text
CLIP
CLIP + BLIP Caption
CLIP + Scene Graph
CLIP + Caption + Scene Graph
CLIP + Caption + Scene Graph + Metadata
```

以上配置应通过正式消融实验验证，而不能仅凭系统最终效果推断各模块贡献。

### 5.1 正式实验矩阵

| 实验ID | 配置 | 目的 | 主要假设 | 状态 |
|---|---|---|---|---|
| A0 | CLIP | 建立直接图像语义检索基线 | CLIP 对普通视觉查询具有最高且最稳定的基础召回 | 评测器已实现；待人工 qrels 正式运行 |
| A1 | CLIP + BLIP Caption | 检验 caption 文本通道贡献 | caption/BM25 能补回精确词语命中但 CLIP 排名较低的图片 | 评测器已实现；待人工 qrels 正式运行 |
| A2 | CLIP + Scene Graph | 检验结构化关系贡献 | Scene Graph 对人物、动作、物体关系查询改善最明显 | 评测器已实现；待个人 SG＋人工 qrels |
| A3 | CLIP + Caption + Scene Graph | 检验三个视觉/文本通道的互补性 | 三路独立召回优于任意双通道，但要避免重复证据过度加分 | 评测器已实现；待个人 SG＋人工 qrels |
| A4 | CLIP + Caption + Scene Graph + Metadata | 检验完整系统 | 时间、地点等预过滤能显著提高含结构化条件查询的准确率 | 评测器已实现；待个人 SG＋人工 qrels |

### 5.2 实现约定

- A1 中的 Caption 通道应使用 SQLite FTS5/BM25 或明确记录的文本检索方法，而不是当前 `$contains` 子串匹配；
- A2 中同时保留原始 triples JSON、规范化 Scene Graph 文本和 SG 向量；
- A3 第一版优先使用 RRF，避免直接相加 CLIP、BM25 和 SG 的不同分数尺度；
- A4 的 Metadata 主要作为召回前过滤条件，不应把“是否符合日期”仅作为一个很小的软分数；
- 所有实验必须使用相同数据集、查询集、相关性标注、`top_k`、模型版本和索引版本；
- 不得在看到测试集结果后反复调权重而不记录过程。

### 5.3 查询集分层

正式查询集至少包含以下类型，并分别报告结果：

| 查询类型 | 示例 | 预期主要通道 |
|---|---|---|
| 整体视觉/场景 | `a beach at sunset` | CLIP |
| 物体属性 | `a red apple on a table` | CLIP＋Caption |
| 关系查询 | `a person using a laptop` | Scene Graph＋CLIP |
| 精确文本描述 | `coffee cup beside a keyboard` | Caption/BM25＋CLIP |
| 时间/地点 | `photos in Cambridge in June` | Metadata＋CLIP |
| 中文自然语言 | `一个人在桌边使用电脑` | 查询翻译/解析＋Scene Graph＋CLIP |
| 以图搜图 | 上传示例照片 | CLIP image-to-image |

### 5.4 检索评价指标

至少记录：

- Recall@1、Recall@5、Recall@10；
- Precision@5、Precision@10；
- MRR；
- nDCG@10；
- 查询延迟 p50/p95；
- 建索引时间；
- 峰值 RAM 和 VRAM；
- 各查询类型的分组指标。

如果每条查询只有一张目标图，可以重点使用 Recall@K 和 MRR；如果存在多张分级相关图片，则应增加 nDCG。

### 5.5 空白结果表

| 实验ID | R@1 | R@5 | R@10 | MRR | nDCG@10 | p50延迟 | p95延迟 | 备注 |
|---|---:|---:|---:|---:|---:|---:|---:|---|
| A0 |  |  |  |  |  |  |  |  |
| A1 |  |  |  |  |  |  |  |  |
| A2 |  |  |  |  |  |  |  |  |
| A3 |  |  |  |  |  |  |  |  |
| A4 |  |  |  |  |  |  |  |  |

## 6. Narrative 独立消融计划

检索准确率和故事质量必须分开评测。检索结果好不代表故事一定忠实。

| 实验ID | 故事输入/约束 | 目的 | 状态 |
|---|---|---|---|
| N0 | 原始 Caption＋Tags＋Metadata prompt | 原始 Story 基线 | 已实现，待正式评测 |
| N1 | N0＋Scene Graph | 测试关系证据是否改善事实细节 | 输入管线已实现；待个人 SG 与正式评测 |
| N2 | N1＋照片 ID 引用＋结构化输出 | 测试证据引用和事实可追溯性 | 已实现；待正式评测 |
| N3 | N2＋用户选择照片/事件 | 测试检索到叙事的完整闭环 | 已实现；待正式评测 |

建议的 Narrative 指标：

- **事实支持率**：故事中的可核查陈述有多少能由照片证据支持；
- **无依据陈述率**：人物身份、情绪、因果和活动等无证据内容所占比例；
- **引用有效率**：引用的 photo_id 是否存在且真正支持对应陈述；
- **覆盖率**：选中照片中有多少被故事合理使用；
- **人工评分**：连贯性、可读性、忠实度和整体偏好；
- **生成延迟**与输出长度。

## 7. 数据集角色与实验边界

### 7.1 当前 376 张主项目个人照片

用途：

- 功能演示；
- 搜索结果和界面验证；
- BLIP/Scene Graph 小规模检查；
- Story 和证据引用演示。

限制：

- 不是严格连续 lifelog；
- 不足以证明 SeqWin、ParChain 或事件分割效果。

### 7.2 Geograph 10,056 张图片

用途：

- 检索规模、速度和资源占用实验；
- CLIP 与 Scene Graph 的消融实验；
- 现有 Qwen Scene Graph 结果的复用。

限制：

- 与主项目 376 张照片存在 67 个 basename 碰撞，但完整 SHA-256 内容哈希匹配为 0；
- 不能把现有 10,056 条 Scene Graph 错误关联到主项目照片；
- 不是可靠的连续个人 lifelog，不适合证明个人时间事件检索。

### 7.3 后续需要的数据

如果论文要验证事件分割、before/after 或完整“一天的故事”，需要另准备：

- 连续采集的照片；
- 可靠的拍摄时间；
- 可选地点和用户备注；
- 人工标注的事件边界；
- 与事件对应的查询和相关性标签。

## 8. 分阶段实现路线

### 阶段 S0：稳定基线

- [x] 采用 SQLite FTS5＋版本化 Chroma v2，保留 legacy Chroma 回滚备份；
- [x] 建立 schema/index manifest；
- [x] 修复 features 与 metadata 错位；
- [x] 增加 `content_sha256` 和稳定 `photo_id`；
- [x] 增加时间来源与可信度；
- [x] 改为逐批懒加载、checkpoint、OOM 自动减半和单图失败隔离；
- [x] 增加索引、过滤和损坏图片测试；
- [x] 重建 376 张照片的可复现基线索引。

### 阶段 S1：Caption 文本召回

- [x] 建立 SQLite FTS5 caption 索引；
- [x] 实现多词 BM25 检索；
- [x] 实现 CLIP 和 BM25 独立召回；
- [ ] 完成 A0、A1 实验。

### 阶段 S2：Scene Graph 集成

- [ ] 为主项目 376 张照片单独生成 Scene Graph；
- [x] 以 `dataset_id + photo_id + content_sha256` 导入，禁止只按文件名关联；
- [x] 实现 triples、文本、模型版本和生成状态的存储 schema；
- [x] 建立 SG 三元组向量检索；
- [x] 实现 CLIP、BM25、SG 候选并集和 RRF；
- [ ] 完成 A2、A3 实验。

### 阶段 S3：Metadata 与完整系统

- [x] 查询解析出视觉、关系、时间、地点等条件；
- [x] 在召回前生成 eligible photo IDs；
- [x] 修复文本检索和以图搜图的统一过滤语义；
- [x] 展示每张结果的实际命中通道；
- [ ] 完成 A4 实验。

### 阶段 S4：增强原有 Story

- [x] 搜索结果加入 Story basket；
- [x] 支持用户调整、排序和排除照片；
- [x] Story 输入加入 Scene Graph 和证据等级；
- [x] 增加忠实模式、创意模式和输出语言选项；
- [x] 生成带 photo_id 引用的结构化故事；
- [x] 保存故事、证据、模型和索引版本；
- [ ] 完成 N0–N3 实验。

## 9. 单次实验记录模板

后续每次实验复制本节并填写，失败实验也必须保留。

### EXP-YYYYMMDD-编号：实验名称

**状态：** 计划实验 / 运行中 / 已完成 / 失败 / 放弃  
**日期：**  
**Git commit：**  
**负责人：**  

#### 研究假设

说明为什么要做本实验，以及预期哪类查询会受益。

#### 实验配置

| 项目 | 值 |
|---|---|
| 数据集及版本 |  |
| 图片数量 |  |
| 查询集及版本 |  |
| 查询数量 |  |
| 相关性标注方式 |  |
| CLIP 模型/版本 |  |
| Caption 模型/版本 |  |
| Scene Graph 模型/版本 |  |
| 文本索引方式 |  |
| 融合方式及参数 |  |
| top-k |  |
| 随机种子 |  |
| Python/依赖锁文件 |  |
| CPU/GPU/RAM |  |
| 索引版本 |  |

#### 结果

| 指标 | 总体 | 视觉查询 | 关系查询 | Caption查询 | Metadata查询 |
|---|---:|---:|---:|---:|---:|
| Recall@1 |  |  |  |  |  |
| Recall@5 |  |  |  |  |  |
| Recall@10 |  |  |  |  |  |
| MRR |  |  |  |  |  |
| nDCG@10 |  |  |  |  |  |

#### 资源与延迟

| 指标 | 结果 |
|---|---:|
| 建索引时间 |  |
| 查询 p50 |  |
| 查询 p95 |  |
| 峰值 RAM |  |
| 峰值 VRAM |  |
| 索引磁盘占用 |  |

#### 观察与错误案例

- 成功案例：
- 失败案例：
- 是否存在模型重复证据：
- 是否存在 BLIP/SG 幻觉：
- 是否存在 metadata 错误：

#### 结论与决策

- 假设是否得到支持：
- 是否保留该模块：
- 下一步修改：
- 论文中可使用的图表/结论：

## 10. 决策日志

| 日期 | 决策 | 原因 | 状态 |
|---|---|---|---|
| 2026-07-16 | 保留 CLIP 作为主视觉召回 | 论文实验显示直接图像特征可靠，且适合本机运行 | 已确定 |
| 2026-07-16 | BLIP caption 作为辅助文本和 Story 证据 | Caption 与 CLIP 部分重复，但适合精确文本和自然语言展示 | 已确定 |
| 2026-07-16 | Qwen Scene Graph 只做离线生成 | RTX 3050 Ti 4GB 不适合本地运行 Qwen2.5-VL-7B | 已确定 |
| 2026-07-16 | 不直接相加三种原始分数 | CLIP、BM25、SG 分数尺度不同且语义可能重复 | 已确定 |
| 2026-07-16 | 第一版融合采用加权 RRF | 它对不同尺度的独立排名较稳健 | 已实现，权重待消融验证 |
| 2026-07-16 | 故事功能保留并增强，不重新定义为新增模块 | Daily Story 已是原项目功能和 Narrative 核心 | 已确定 |
| 2026-07-16 | 当前 376 张个人照片不用于证明连续 lifelog 算法 | 虽有可靠时间的照片，但采集并非严格连续且没有人工事件边界 | 已确定 |
| 2026-07-16 | 正式结论必须来自消融实验 | 完整系统得分不能证明单个模块贡献 | 已确定 |
| 2026-07-16 | 主存储采用 SQLite FTS5，向量使用独立 Chroma v2 | 保留简单本地部署，同时隔离旧 Chroma schema 风险 | 已实现 |
| 2026-07-16 | Scene Graph 导入必须同时校验 dataset、ID 和完整 SHA-256 | 个人相册与 Geograph 有 67 个同名碰撞但 0 个哈希匹配 | 已实现 |
| 2026-07-17 | 无人工 qrels 时不报告 Recall/MRR/nDCG | 避免把功能烟测或模型自标注伪装成正式准确率 | 已确定 |
| 2026-07-17 | 日常索引采用 path＋size＋mtime＋ctime 快路，正式实验冻结前执行 `--force` | 常规扫描应避免重复哈希/EXIF，但 stat 指纹不能替代内容哈希 | 已实现并实测 |
| 2026-07-17 | API 密钥只从环境变量读取，不在配置和文档中保存值 | 复现项目曾存在凭据暴露风险，工作区清理不能替代服务商撤销和 Git 历史清理 | 工作区已修复；旧密钥待用户轮换 |

## 11. 相关材料

- [`retrieval_improvement_plan.md`](retrieval_improvement_plan.md)
- [`README.md`](README.md)
- [`hybrid_retriever.py`](hybrid_retriever.py)
- [`model_pipeline.py`](model_pipeline.py)
- [`story_agent.py`](story_agent.py)
- [SnapSeek 3.0](../references/SnapSeek%203.0.pdf)
- [LifeSeeker 3.0](../references/LifeSeeker%203.0.pdf)
- [LifeSeeker 6.0](../references/LifeSeeker%206.0.pdf)
- [Memento Prototype](../references/Memento_A%20Prototype%20Lifelog%20Search%20Engine.pdf)
- [Memento 4.0](../references/Memento%204.0.pdf)
- [lifeXplore 2025](../references/lifeXplore%202025.pdf)
- [The State-of-the-Art in Lifelog Retrieval](../references/The%20State-of-the-Art%20in%20Lifelog%20Retrieval.pdf)

## 12. 更新记录

| 日期 | 更新内容 |
|---|---|
| 2026-07-16 | 创建文档；整理最近三轮讨论；转录截图中的五组消融配置；建立检索和 Narrative 实验模板。 |
| 2026-07-16 | 完成 `photos/` 图像格式标准化：153 个 HEIC 和 5 个 PNG 转换为 JPG，218 个已有 JPG 未重新编码；建立原图备份、SHA-256 清单和元数据验证报告。 |
| 2026-07-17 | 完成 v2 多模态检索、事件组织、可信叙事、AIRE Scene Graph 安全往返和 A0–A4 评测器；记录 376 张真实基线、性能、失败修正、测试和尚未完成的正式实验。 |
| 2026-07-17 | 对 376 条个人 Qwen 旧格式结果完成显式确认后的可追踪恢复：374 张入库、5,222 个三元组/向量、2 张失败；修复 Caption 通道读取 SG FTS 字段造成的消融泄漏，并完成真实 SG-only 检索烟测。 |
| 2026-07-17 | 完成统一照片详情弹窗：Library、Search、Events、Story 的照片卡片不再直接展开 BLIP/Qwen 原文，点击后按需读取并展示大图、BLIP、完整三元组和基础元数据；完整测试 85/85 通过。 |
| 2026-07-21 | 就地修复 v5 连续回忆叙事（§26）：新增证据组按时间合并到 ≤4、取消强制“我看见”事实起句、过渡改为可选且禁止跨照片物理连接、事实段允许轻度氛围、长度下限适配 qwen3:4b；story grounding 34 项、完整 134 项通过；如实记录模型仍会虚构“七夕夜”等场合。 |
| 2026-07-25 | 合并 pic2 第二批照片与 Scene Graph（§29）：扩展恢复器加 `--batch-subdir` 子目录作用域，1223 张新照片 + Scene Graph 入库；主相册 376→1599、SG 374→1597、三元组 5,222→22,200，索引 0 失败，`unittest discover` 162/162 通过。冻结的 A0–A4 v1 与 Story 正式实验未在扩充相册上重跑。 |
| 2026-07-26 | Story 新增可切换远程 LLM 后端（§34）：新增 `DashScopeGenerator` + `config.py` 远程配置 + 工厂 `backend` 选择 + 前端切换控件，远程走 DashScope OpenAI 兼容接口的 `qwen3.5-27b`（欧洲区），本地 `qwen3:4b` 保留为默认/备选；密钥仅从 `.env` / `DASHSCOPE_API_KEY` 读取，远程失败报错不回退；核心生成/校验/提示词/温度与全部冻结实验路径不动。 |
| 2026-07-26 | 完成远程 Story 真实验收与 Schema 修复（§34.7）：真实调用已能返回 JSON，但初版仅启用 `json_object` 且丢弃动态 `output_schema`，导致两次输出无法被 `groups[].factual_text` 契约读取；现将完整动态 Schema 注入远程 system prompt，并让 Faithful 修复携带被拒输出、错误码和结构要求。单图真实 `qwen3.5-27b` 验收首轮 `ok`、字段结构正确、零校验错误且 `save=False`；Story 定向 54/54、完整测试 171/171 通过，用户随后确认生产生成成功。 |
| 2026-07-27 | 移除 72 个 CLIP 预设标签并解除其对 Story 的约束（§35）：发现 `tags` 表兼作 CLIP 完成/版本身份标记，改用非语义哨兵 `__clip_indexed__` 保留该职责（无 schema 迁移，3 个安全测试原样通过）；Story 证据不再含 `clip_tags_model_observations`，Creative 第一人称资格不再由标签判定；迁移 4,797→1,599 行、语义标签 0 残留、FTS tags 列全空；增量快路保住（indexed 0 / skipped 1599 / 19.4 s），检索与 Story 回归正常，完整测试 173/173 通过。冻结实验不重跑即不受影响。 |

## 13. 数据预处理记录：照片统一为 JPG

### 13.1 处理原则

- 已有 JPG 不重新编码，避免二次有损压缩；
- 非 JPG 先在 staging 目录转换并逐张验证，全部成功后才提交；
- 原始 HEIC/PNG 移至独立回滚备份，不删除原始字节；
- HEIC/PNG 转 JPG 使用质量 98、4:4:4 色度采样和 progressive JPEG；
- 透明 PNG 使用白色背景合成，原始带 alpha 文件保留在备份中；
- 保留并核验 EXIF 拍摄时间、GPS、相机/镜头信息、Apple MakerNote、ICC 和 XMP；
- 复制文件创建时间和修改时间；
- 每个源文件、备份和输出均记录 SHA-256。

### 13.2 处理结果

| 项目 | 结果 |
|---|---:|
| 初始图片 | 376 |
| 原有 JPG/MPO | 218 |
| 转换 HEIC | 153 |
| 转换 PNG | 5 |
| 最终 JPG 扩展名文件 | 376 |
| 原图备份 | 158 |
| 含 EXIF 拍摄时间的转换源文件 | 153 |
| EXIF 时间验证通过 | 153/153 |
| 含 GPS 的转换源文件 | 151 |
| GPS 验证通过 | 151/151 |
| 原有 JPG 哈希保持不变 | 218/218 |
| 转换输出/备份哈希验证 | 158/158 |
| 最低视觉 PSNR | 48.3136 dB |

其中 109 个原有 `.JPG` 是 JPEG 兼容的 MPO 多图容器。为保留辅助画面，它们保持原始字节不变；应用仍可把主图作为 JPG 读取。

### 13.3 可复现材料

- 转换脚本：[`scripts/convert_photos_to_jpg.py`](scripts/convert_photos_to_jpg.py)
- 本地转换报告：`photo_conversion_20260716_jpg_normalization/conversion_report.md`
- 最终逐图清单：`photo_conversion_20260716_jpg_normalization/manifest_final.json`
- 原始非 JPG 备份：`photo_conversion_20260716_jpg_normalization/original_non_jpeg/`

转换目录包含个人 EXIF/GPS 和原始照片，已加入 `.gitignore`，不得提交到公开仓库。

## 14. 2026-07-16—17：v2 实现、验证与真实基线

### 14.1 实现状态与论文可用结论

| 子系统 | 当前状态 | 现在可以声称什么 | 仍不能声称什么 |
|---|---|---|---|
| SQLite＋FTS5/BM25 | 已实现并验证 | Caption、标签、位置等能独立文本召回，且 Metadata 可先过滤候选 | 尚无人工 qrels，不能声称准确率提升 |
| Chroma CLIP 图像向量 | 已实现并验证 | 支持文本到图像和以图搜图，并与 SQLite `photo_id` 对齐 | 功能烟测不能替代正式 Recall/MRR/nDCG |
| Qwen Scene Graph | 374 张个人照片已恢复入库，2 张生成失败；严格 Manifest 路线仍保留 | 374 张结果通过精确相对路径、当前文件 SHA-256、SQLite SHA-256、mtime 与用户来源确认；SG 独立通道可检索 | 旧批次没有生成时 Manifest/hash 和已版本化 prompt，不能声称严格端到端来源证明；没有 qrels，不能声称 A2/A3 提升 |
| 加权 RRF | 已实现 | CLIP、BM25、SG 可独立召回后按排名融合，避免直接相加异构分数 | 当前权重仍需在开发集上消融，不能写成最优权重 |
| 事件组织 | 已实现确定性基线 | 可按时间、视觉变化和 GPS 形成可解释事件候选 | 没有人工事件边界，不能证明事件分割准确率 |
| 可信叙事 | 已增强原有 Story | 可从篮子、日期或事件生成带照片 ID 引用的结构化输出，并在失败时确定性回退 | 引用只能说明可追溯，不能把视觉模型预测当作客观事实 |
| A0–A4 评测 | 评测器已实现 | 已固定变体、指标、延迟记录、qrels 哈希和 temporal 独立任务 | qrels 尚未人工完成，指标表必须保持空白 |

### 14.2 376 张个人照片的真实工程基线

以下数字来自本机 `torchtest` 环境与当前 v2 SQLite/Chroma，不是估计值。

| 项目 | 实测结果 |
|---|---:|
| 照片记录 / CLIP 向量 | 376 / 376 |
| 成功 BLIP Caption / FTS5 文档 / 有 CLIP 标签照片 | 376 / 376 / 376 |
| 含 GPS 照片 | 258 |
| 时间置信度：高 / 中 / 低 | 355 / 1 / 20 |
| 时间来源：EXIF Original / EXIF DateTime / 文件名 / mtime | 354 / 1 / 1 / 20 |
| 个人 Scene Graph / 三元组向量 | 374 / 5,222（另有 2 张 Qwen 失败） |
| 自动事件 / 已分配照片 / 未分配低置信度照片 | 162 / 356 / 20 |
| 当前索引失败 / 测试故事残留 | 0 / 0 |
| 全量 CLIP＋BLIP 重建 | 542 秒（9 分 02 秒） |
| 全量批次 / OOM | CLIP 8、BLIP 4 / 0 |
| 事件组织 | 12.989 秒 |
| 优化前无变化增量 | 34.0 秒 |
| 优化后无变化增量 | 8.657 秒，376 张全部跳过，约减少 74.5% |

增量时间是 `index_paths` 内部计时，不含 Conda、Python、Chroma 初始化和目录扫描，因此不能直接等同于用户从命令启动到退出的端到端等待时间。第一次导入仍必须运行 CLIP 和 BLIP；300 多张照片耗时数分钟是模型推理成本，不是 BM25 或 SQLite 写入造成的。

### 14.3 增量索引优化与失败恢复

日常无变化扫描先比较精确相对路径、size、mtime 和 ctime；全部一致时复用 SQLite 中的 SHA-256 与 EXIF，避免再次读取整张文件。快路仍独立核对 Chroma 图像 ID、当前 CLIP 模型版本标签和当前 BLIP 模型版本的成功 Caption，缺哪一阶段只补哪一阶段。

本轮代码审查发现并修复了两个不能忽略的恢复缺陷：

1. 临时 metadata 提取失败后若仍保存完整 stat 指纹，下一轮会误走快路并永久跳过 metadata。修复后失败记录的 size/mtime/ctime 留空，下一轮必定重新哈希和提取。
2. `--force` 同模型重算失败时，旧 tag/vector/Caption 会掩盖失败。修复后强制重算先使对应派生结果失效；若推理失败，普通增量会继续回补。

新增回归测试覆盖：快路不调用哈希/EXIF、缺失阶段回补、force 绕过、size/mtime/ctime 变化、改名保持 UUID、metadata fail-once 恢复、force Caption fail-once 恢复、Caption 版本变化只重算受影响阶段，以及 CLIP v1→v2→v1 往返不会复用陈旧向量身份。CLIP 重算先清除持久身份标记，再写 Chroma，最后在一个 SQLite 事务中写入当前 `clip:*` 标签与 FTS；即使向量成功而标签提交失败，旧身份也不能误授权新向量。故障注入测试已经覆盖这一窗口。

仍需保留的边界：stat 指纹不是密码学证明；如果内容被替换且路径、大小及所有可见时间戳都被人为保持，日常快路可能漏检。因此正式实验冻结数据、迁移文件系统或怀疑外部修改时，应执行一次 `--force` 并保存状态报告。真实模型版本标识目前能区分 checkpoint 名与固定依赖环境，但尚未编码标签词表、prompt 和全部 generation 参数；这些配置发生变化时也应使用新 pipeline 版本或 `--force`。

### 14.4 最终非 Ollama 验证

| 验证项 | 结果 |
|---|---|
| Python 单元测试 | 80/80 通过，31.220 秒 |
| Python 源码语法编译 | 41 个文件全部通过 |
| 真实无变化增量 | 376 discovered、0 indexed、376 skipped、0 failed |
| 数据一致性 | 376 photos / captions / FTS / tags / image vectors；0 failures |
| 应用导入 | `app_import_ok`，未加载或调用 Ollama |
| 先前 Streamlit 界面烟测 | AppTest 0 exceptions，服务可监听 |
| Scene Graph 旧数据安全性 | Geograph 与个人相册 67 个 basename 碰撞、完整 SHA-256 交集 0、可导入 0 |
| 已确认个人旧批次恢复 | 376 路径全覆盖；374 可恢复、2 失败、0 阻断；入库后 374 SG / 5,222 triples / 5,222 vectors |
| 恢复后完整单元测试 | 83/83 通过，30.756 秒 |
| 真实 SG-only 烟测 | 3/3 功能通过；CLIP 冷启动首条 27,186.864 ms，后两条 94.658 / 84.641 ms；未使用 Ollama/Qwen |

遵照用户要求，本轮没有测试 Ollama，也没有把确定性 fallback 误写成真实大模型生成结果。

### 14.5 实现过程中的失败点与修正

| 失败或风险 | 原因 | 修正/当前处理 |
|---|---|---|
| legacy Chroma 打开出现 `_type` 错误 | 旧 schema 与当前 Chroma 版本不兼容 | 冻结旧库，建立独立 v2 collection |
| 已缓存模型仍尝试 Hugging Face HEAD | 库默认进行网络元数据探测 | 优先离线缓存和本地权重；正式实验固定离线环境 |
| NumPy 数组被当作布尔值 | `if embedding` 对数组语义不明确 | 改用显式长度/空值判断并加测试 |
| Scene Graph 导出字段与旧结果不一致 | Geograph 旧格式与个人数据身份契约不同 | 统一 manifest schema，并严格校验 dataset、ID、完整哈希 |
| before/after 语义混淆 | “邻居”与“A 后出现 B”是不同任务 | 分为单锚点 temporal neighbor 与双事件 ordered pair |
| Windows 下 Chroma 资源关闭问题 | 持久客户端/文件句柄生命周期不清 | 显式关闭向量存储并保持 SQLite 为规范源 |
| 临时 eligible-ID 表出现重复 | 多通道过滤重复写入临时集合 | 使用去重集合/唯一语义后再召回 |
| 旧 Qwen 文件名看似可匹配 | basename 不是身份，实际 67 个同名但哈希交集为 0 | 禁止 basename/stem 导入，旧结果 apply 为 0 |
| 快路测试首次出现 10 个同源 `TypeError` | 测试 helper 插入位置错误 | 修正 helper 后专项、全套测试均通过 |
| metadata/force 失败可能被快路掩盖 | 完成标记与失败重试语义不完整 | 留空失败指纹、force 先失效派生结果并新增恢复测试 |
| CLIP v1→v2→v1 可能混用旧标签与新向量 | 单一向量 ID 过去只检查“某版本标签是否存在”，且向量/标签跨存储写入有失败窗口 | 先清身份标记、再写向量、最后事务性写当前标签和 FTS；增加往返与标签提交失败故障测试 |
| Conda 列表与实际 import 版本不一致 | 环境元数据与运行时解析存在漂移 | `requirements.txt` 记录实测 import 版本；正式实验另存环境快照 |
| 工作区曾有 API key 风险 | 密钥写入配置文件 | 当前改为 `OPENAI_API_KEY`/`DASHSCOPE_API_KEY` 环境变量；旧凭据仍须撤销轮换并清理 Git 历史 |
| 没有人工 qrels | 功能结果不能作为真值 | 评测器拒绝无正相关标注的伪指标，当前不报告准确率 |
| 个人 Qwen 结果缺少生成前 Manifest | 旧生成脚本只记录 image_path、三元组和时间，没有 photo_id/SHA/model/prompt 版本 | 新增必须显式确认的专用恢复器；绑定精确路径＋当前/SQLite SHA＋mtime，保存原始文件/行哈希，并固定标记 `generation_sha256_verified=false`；普通 legacy importer 仍禁止个人相册 |
| A1 Caption 消融会读取 SG 文本 | `photo_fts` 同时保存 Caption、Scene Graph、Tags、Location，原检索器未限制 BM25 列 | `search_bm25` 新增显式字段过滤；主检索的 Caption 通道只查询 `caption` 列，新增“Caption 不得召回仅存在于 SG 的词”回归测试 |
| SG 语义召回不等于精确关系匹配 | `tree beside road` 的 SG-only 前三名来自语义近邻，但显示的三元组并非严格 subject=tree、relation=beside、object=road | 当前烟测只证明通道和证据展示可用，不作为准确率；正式 qrels 中需单列 exact-relation 查询，并比较 SG 向量召回与结构化/词法约束 |

### 14.6 可复现实验命令

```powershell
cd "D:\code\vscode\Msc-code\smart-photo-narrative"

# 查看状态，不运行 CLIP/BLIP
conda run -n torchtest python scripts/report_index_state.py

# 日常增量并重新组织事件
conda run -n torchtest python scripts/rebuild_multimodal_index.py --organize-events

# 实验冻结前完整强校验（会重新运行 CLIP/BLIP）
conda run -n torchtest python scripts/rebuild_multimodal_index.py --force --organize-events

# 人工完成 qrels 后运行 A0--A4；默认不使用 Ollama
conda run -n torchtest python scripts/evaluate_retrieval.py `
  evaluation/my_qrels.jsonl `
  --output outputs/retrieval_ablation_report.json `
  --ks 1 5 10 --repeats 3 --warmup 1

# 启动界面
conda run -n torchtest python -m streamlit run app.py
```

AIRE Scene Graph 的导出、三 GPU Slurm、审计、`--apply` 和正式入库命令以 [`README.md`](README.md) 及 [`scripts/cloud/README_scene_graph_aire.md`](scripts/cloud/README_scene_graph_aire.md) 为准。

### 14.7 下一轮正式实验入口

1. 用严格 Manifest 流程单独重跑 `IMG_0621.JPG`、`IMG_6249.jpg`；若论文要求全链路密码学来源证明，则重新生成全部 376 张，而不是把恢复记录描述为严格结果。
2. 人工建立分层查询集与 qrels，至少覆盖视觉、Caption 词项、精确关系、语义关系、Metadata、中文和以图搜图。
3. 冻结照片、qrels、模型/依赖、索引版本和 RRF 权重；先在开发查询上调参，再一次性运行测试查询。
4. 填写 A0–A4 的 Recall@K、MRR、nDCG、P50/P95；同时保留逐查询失败案例，重点比较 SG 语义近邻与精确关系约束。
5. 单独标注事件边界并运行 N0–N3 人工叙事评测，报告事实支持率、无依据陈述率、引用有效率和用户偏好。

## 15. 2026-07-17：个人 Qwen Scene Graph 恢复、入库与验证

### 15.1 输入批次与证据边界

输入文件：`outputs/scene_graph/raw/qwen_scene_graph_photos_full.jsonl`。用户明确确认该批次由当前 `photos/` 中未经修改的 376 张照片生成。程序没有据此伪造原始 Manifest，而是保留两层结论：

- 可验证：376 个相对路径与当前相册一一对应；无重复、缺失或越界；当前文件 SHA-256 与 SQLite 一致；文件 mtime 均不晚于对应 `generated_at`；
- 不可验证：旧行没有生成时照片 SHA-256、`dataset_id`、稳定 `photo_id`、模型字段或 prompt 版本，因此无法从结果文件本身密码学证明 Qwen 当时看到的字节。

恢复结果统一标记：

```text
provenance = legacy_path_time_recovered
generation_sha256_verified = false
sha256_origin = current_local_file_at_recovery
prompt_version = legacy-snapseek-scene-graph-unversioned-20260717
```

Qwen2.5-VL-7B 模型信息来自本次运行上下文和 AIRE 指南，不是旧 JSONL 自带字段；论文方法部分必须披露这一点。

### 15.2 审计与恢复结果

| 项目 | 结果 |
|---|---:|
| JSONL 总行数 / 当前相册图片 | 376 / 376 |
| 路径覆盖 / 重复 / 缺失 / 越界 | 376 / 0 / 0 / 0 |
| `remote` / `remote_repaired` / `failed` | 373 / 1 / 2 |
| 可恢复 Scene Graph | 374 |
| 有效三元组 | 5,222 |
| 平均三元组/成功照片 | 13.96 |
| SQLite 哈希冲突 / 生成后修改 / 阻断问题 | 0 / 0 / 0 |
| 失败照片 | `IMG_0621.JPG`、`IMG_6249.jpg` |

两张失败均为模型输出 JSON 截断，初次解析和 repair 均在约第 33/34 行失败。它们未写入 SQLite/Chroma，也没有用 fallback 三元组冒充 Qwen 结果。

固定哈希：

| 对象 | SHA-256 |
|---|---|
| 原始 Qwen JSONL | `f5ec5e2b6785f3637e8882b0bc5ecb465cddc65484634cd084c7cc4701e24e7b` |
| 当前相册路径＋逐图哈希快照 | `13670dd24a78c51cefb6e36b2071cdcfe4227a8cf43360932d01ae282c0c1beb` |
| 恢复后的 indexable JSONL | `c06f3629dcb0bc943509f8e467ed20858e9a01378f1188c9c491d8550896e935` |
| 恢复审计报告 | `ea2a4912e231f1b6afba877a1deec5def1d8e13b61a768c330dcb159d28352f8` |
| SG-only 烟测报告 | `5f2a49e0df3e98fe22d7f9313ca7ea5c0d11ac617c102a1cfb2d311459215302` |

本地私有产物位于：

- `outputs/scene_graph/recovered/scene_graph_indexable_recovered_20260717.jsonl`
- `outputs/scene_graph/recovered/scene_graph_recovery_report_20260717.json`
- `outputs/scene_graph/reports/scene_graph_search_smoke_20260717.json`
- 导入前回滚副本：`outputs/scene_graph/backups/scene_graph_preimport_backup_20260717_164115/`

导入前 SQLite 备份 SHA-256 为 `27d07a20be889de5787594f3bdbfb504637922de0912e57161d5ec7f1a37d5ce`。以上目录含个人照片派生信息，已由 `.gitignore` 排除，不得公开提交。

### 15.3 两阶段导入与最终一致性

恢复转换 dry-run 与索引 dry-run 均先于任何写库操作：

```text
恢复 dry-run: total 376, recovered 374, failed 2, blocking 0
索引 dry-run: total 374, applied 374, skipped 0, checksum_conflicts 0, malformed 0
正式索引:    total 374, applied 374, skipped 0, checksum_conflicts 0, malformed 0
```

正式写入后：

| 存储层 | 结果 |
|---|---:|
| SQLite photos / Caption / FTS5 / tagged photos | 376 / 376 / 376 / 376 |
| SQLite Scene Graph / triples | 374 / 5,222 |
| Chroma image vectors / SG triple vectors | 376 / 5,222 |
| 索引失败记录 | 0 |

SQLite 保存规范化三元组、恢复来源引用和原始结果文件哈希；FTS5 被逐图刷新；Chroma 用本机已有 CLIP 文本编码器为每条三元组生成独立向量。正式导入没有调用 Ollama，也没有再次调用 Qwen。

### 15.4 真实检索烟测及其解释边界

SG-only 功能烟测三条查询均返回 3 个结果，且结果的唯一 modality 为 `scene_graph`，界面证据字段能够显示匹配三元组：

| 查询 | 功能结果 | 代表性证据 | 延迟 |
|---|---|---|---:|
| `person wearing jacket` | 通过 | `person wear jacket` | 27,186.864 ms（含 CLIP 冷启动） |
| `tree beside road` | 通道通过，但非精确关系命中 | `tree near cathedral`、`tree grow nearby`、`trees line sidewalk` | 94.658 ms |
| `building with windows` | 通过 | `building have windows` | 84.641 ms |

因此 3/3 只能写成“Scene Graph 通道、向量和证据展示功能可用”，不能写成“3/3 检索准确”。第二条明确暴露了 CLIP 三元组向量的语义近邻性质：它能找概念相似项，但不会自动满足严格 subject–predicate–object 约束。这个失败案例应保留到 exact-relation qrels 和后续结构化匹配实验。

### 15.5 导入后发现的消融泄漏与修正

`photo_fts` 为减少重复存储，同时包含 Caption、Scene Graph、Tags 和 Location。原 `caption` 通道调用 BM25 时没有列过滤，因此在 SG 入库前看不出问题，入库后 A1 会读取 SG 文本，造成：

```text
A1 = CLIP + Caption + 隐式 Scene Graph/Tags/Location
```

这会使 A1、A2、A3 的对比失效。现已改为：

- `PhotoStorage.search_bm25(..., fields=...)` 支持显式、安全的 FTS5 列过滤；
- 主检索 `caption` 通道固定 `fields=("caption",)`；
- SG 仍只通过独立 `scene_graph` 通道参与 A2/A3；
- Metadata 只在 A4 使用 qrels 明确给出的过滤条件；
- 回归测试证明 Caption-only 不会召回只存在于 SG 的 `person laptop`，而 SG-only 仍能召回。

最终全套单元测试为 83/83 通过。该修正是正式 A0–A4 实验前的必要条件，而不是一次检索性能提升结论。

## 16. 2026-07-17：照片详情弹窗与描述可检查性

### 16.1 目标与交互变化

为了逐张检查 BLIP 描述和 Qwen 三元组，同时避免 50 张/页的 Library 被长文本淹没，统一照片卡片改为“缩略信息＋按需详情”：

- Library、Search、Events 和 Story 共用的所有 `display_results` 照片卡片均提供 `View details / 查看详情`；
- 卡片不再直接显示 `BLIP: ...` 或 `SG: ...`，仍保留时间、地点、检索分数、命中 modality 和折叠的命中原因；
- 可加入 Story 的卡片把详情和 Story 按钮并排，按钮 key 使用动作＋页面前缀＋稳定 photo UUID，避免 before/after 或重复照片发生组件冲突；
- 点击详情后用 Streamlit 1.54 原生 large dialog 显示文件名、大图、时间及置信度、地点、分辨率、标签、完整 BLIP Caption 和全部 Qwen 三元组表格；
- 界面不显示绝对路径、哈希、UUID、模型来源引用等技术审计字段。

### 16.2 数据接口与性能原则

新增 `PhotoStorage.get_photo_detail(photo_id)`，返回：

```text
photo       基础照片字段
caption     主成功 Caption；没有 primary 时回退到最新成功记录
scene_graph 主成功 Scene Graph；没有 primary 时回退到最新成功记录
triples     按 ordinal 排序的 subject/relation/object/confidence
tags        不区分大小写去重后的标签
```

照片、Caption、Scene Graph、三元组和标签在一次 SQLite 只读事务中取得。Library 首次渲染只使用原有批量文档查询，详情数据只在用户点击时加载，因此没有给每页 50 张照片增加 50 次详情查询，也没有触发 CLIP、BLIP、Qwen 或 Ollama。

缺失状态分别处理：照片文件缺失、无 BLIP、无 Qwen 都只影响对应区域。`IMG_0621.JPG` 和 `IMG_6249.jpg` 的真实检查结果均为照片存在、BLIP 存在、Scene Graph 不存在、triples=0，因此详情弹窗会显示 `No Qwen Scene Graph available / 暂无 Qwen Scene Graph`，不会伪造内容。

### 16.3 测试与验收

| 验证项 | 结果 |
|---|---|
| `PhotoStorage` 专项测试 | 10/10 通过，2.489 秒 |
| 主记录优先和非主记录回退 | 通过 |
| 三元组 ordinal 顺序 / 标签大小写去重 | 通过 |
| 无 Caption、无 Qwen、未知 photo_id | 均返回明确空状态，无异常 |
| Library 初始 AppTest | 0 exceptions；首屏 50 个详情按钮、50 个 Story 按钮 |
| 卡片直接描述检查 | `BLIP:` / `SG:` caption 数量均为 0 |
| 真实详情弹窗 | 0 exceptions；检测到文件名、大图、基础信息、BLIP 区域和 1 个 Qwen dataframe |
| Qwen 失败照片检查 | 2/2 正确显示 0 triples，BLIP 仍可用 |
| Story 按钮回归 | 点击后正确出现 1 个 `In story` 状态，50 个详情按钮仍存在 |
| 完整单元测试 | 85/85 通过，30.171 秒 |

第一次弹窗 AppTest 的应用执行已经完成，但 `conda run` 在 Windows GBK 控制台回显中文时出现 `UnicodeEncodeError`。这不是 Streamlit 或详情弹窗异常；测试改为 `PYTHONUTF8=1`、`PYTHONIOENCODING=utf-8` 并将诊断 JSON `ensure_ascii=true` 后，同一点击流程得到 0 exceptions。该失败点说明 Windows 自动化报告应固定 UTF-8，不能把外层命令包装器编码错误误判为应用故障。

本轮是可检查性和界面信息架构改进，不产生新的 Caption/三元组，也不构成检索准确率提升证据。存储中若已有泛化 Caption（例如 `an image`），详情会忠实显示原值，质量改善仍应通过后续 Caption 模型对比实验完成。

## 17. 2026-07-17：项目目录整理与 BM25 数据边界复核

### 17.1 保守整理原则

本轮只整理生成物与历史输出，没有移动根目录 Python 模块、`photos/`、当前 SQLite 或 `indexes/chroma_v2/`。原因是当前代码普遍采用顶层模块导入，机械改成 `src/` 布局会同时影响 Streamlit、脚本、测试和启动方式，属于单独的包结构重构，而不是普通文件整理。

Scene Graph 私有产物已分成：

```text
outputs/scene_graph/raw/        原始 Qwen 返回与压缩包
outputs/scene_graph/recovered/  可导入恢复 JSONL 和恢复审计报告
outputs/scene_graph/reports/    搜索烟测报告
outputs/scene_graph/backups/    导入前 SQLite/Chroma 回滚副本
```

早期 `test_output.txt` 移至 `docs/legacy/test_output_ollama_20260222.txt`，可再生成的 Python `__pycache__` 已清理。`Msc/` 仅含指向另一个 GitHub 仓库的嵌套 Git 元数据，不是主项目运行依赖；Windows/工作区拒绝移动该目录，因此未强制改权限或删除。完整说明见 `PROJECT_STRUCTURE.md`。

移动后的四个关键 Scene Graph 文件重新计算 SHA-256，与第 15.2 节记录完全一致，证明本次只改变路径、没有改变实验数据。随后在 `torchtest` 环境运行完整单元测试：85/85 通过，32.216 秒；没有调用 Ollama、BLIP、Qwen 或重新生成向量。

### 17.2 当前 BM25 的准确含义

SQLite `photo_fts` 每张照片对应一个 FTS5 文档，可保存 Caption、Scene Graph 文本、标签和地点。通用底层接口可按列检索，但主检索的 `caption` 通道固定为 `fields=("caption",)`，所以当前前端的 Keyword 模式是“对现有 BLIP Caption 做 BM25 词项检索”，不会暗中读取 Qwen、Tags 或 Location。Hybrid 模式再把该 BM25 排名与独立 CLIP、Scene Graph 排名做加权 RRF；Metadata 是检索前过滤条件。

当前库的 FTS5 状态为 376 个文档、376 个非空 Caption、374 个非空 Scene Graph、376 个有标签、258 个有地点。只检索 Caption 的只读实例检查得到：`person` 命中 35 张、`bridge` 命中 9 张；`red bicycle` 使用 OR 命中 5 张，但要求两个词同时出现时命中 0 张。这一结果说明 BM25 依赖已写入 Caption 的字面词项，不会因为图像中实际存在某物就自动识别，也不等价于 CLIP 的语义相似检索。

## 18. 2026-07-18：硕士 Final Report 定位收缩与写作执行方案

### 18.1 学校材料确认的硬性要求

本轮检查了论文目录中的 Final Report 模板、评分 rubric、Assessment Summary、写作 checklist、课程讲座总结、官方讲座幻灯片和原 Scoping Document。确认：

- Final Report 截止时间为 **2026-08-04 14:00**，占模块总评 30%；
- 正文必须为英文、IEEE 期刊双栏，严格 6 页，图、表和参考文献均计入页数；
- 学术诚信签字页、AI 使用声明和 Supplementary Material 不计入 6 页；
- 评分权重为 Technical Achievement 60%、Conclusion 20%、Presentation 20%；
- Distinction 要求高质量结果明确对应 aims/objectives、批判且独立的反思、严格 IEEE 表达和高质量原创图表；
- 课程建议最迟在截止日前两周把草稿发给 supervisor。当前已经进入该窗口，须尽快提供带方法和初步结果的版本。

因此论文不能写成软件菜单或开发流水账，主要篇幅必须用于“研究问题—方法选择—实验结果—失败解释—目标达成度”。

### 18.2 项目最终定位

用户澄清本项目是“**智能检索个人相册**”，不是通用网络图片搜索。个人照片集合是目标应用域，而不是必须掩饰的缺点。建议把论文技术核心收缩为：

> 设计并评估一种面向个人照片集合的多模态智能检索系统，独立融合 CLIP 视觉语义、BLIP Caption/BM25、Qwen Scene Graph 和 EXIF Metadata，并分析不同模态对不同查询类型的贡献。

Event 和 Story 作为检索后的下游应用及系统完整性展示，不再与检索争夺同等篇幅。若截止前未完成正式 N0–N3 人工评测，则不能把“叙事忠实度得到提升”作为核心研究结论；Story 只写成 evidence-grounded application/prototype。

原 Scoping Document 将认知或记忆障碍用户作为目标群体，但当前没有该群体的正式用户研究、临床验证、可用性实验或伦理审批。因此可以保留“无需准确记住日期即可找回照片”的研究动机，但不得声称系统已经改善认知障碍用户的记忆或可用性。建议明确写：

> The prototype is evaluated as a personal photo retrieval system; evaluation with cognitively impaired users remains future work.

### 18.3 建议标题、Aim、Objectives 与 Research Questions

主标题建议二选一：

1. `Smart Photo Retrieval: Multimodal Search for Personal Photo Collections Using CLIP, Captions, Scene Graphs and Metadata`
2. `Independent Multimodal Recall and Rank Fusion for Personal Photo Retrieval`

若没有正式叙事评测，标题不应把 storytelling/narrative 列为与 retrieval 同等级的已评估贡献。

建议 Aim：

> To design, implement and evaluate a resource-conscious multimodal retrieval system for natural-language search over personal photo collections.

建议将原 O1–O5 收缩为三个 Objectives：

- **O1**：建立可复现的照片摄取与多模态索引，包含稳定身份、CLIP、BLIP、Qwen Scene Graph 和 EXIF Metadata；
- **O2**：实现独立召回、Metadata 预过滤和加权 RRF，避免直接混合异构原始分数；
- **O3**：通过人工 qrels、消融实验、分类指标、延迟和失败案例评估检索有效性及资源权衡。

建议只保留两个核心 RQ：

- **RQ1**：`Does combining CLIP, BLIP caption retrieval, Qwen scene graphs and metadata improve retrieval effectiveness over CLIP-only search?`
- **RQ2**：`Which retrieval modalities are most beneficial for different types of personal photo queries?`

如需第三个问题，优先研究 accuracy/latency/resource trade-off；只有完成叙事人工评测后，才能恢复“grounding 是否降低无依据叙述”的 RQ。

### 18.4 可声称与不可声称边界

当前可写入的工程事实包括：376 张照片、376 个 Caption/FTS 文档/CLIP 向量、374 个 Qwen Scene Graph、5,222 个三元组及向量、两张 Qwen 失败、增量索引和完整测试结果。系统已经实现 CLIP、Caption BM25、Scene Graph 向量、Metadata 过滤和 RRF，但“实现”不等于“准确率得到证明”。

正式 qrels 与 A0–A4 未完成前，不可声称：

- 多模态融合优于 CLIP；
- Scene Graph 提升关系查询准确率；
- 当前 SG 能严格满足 subject–predicate–object 关系；
- 事件组织准确；
- Story 更忠实；
- 系统对认知障碍用户有效；
- 376 张个人照片可以代表所有用户或所有相册。

个人相册结果应描述为一个 personal-collection case study/prototype evaluation；如使用 Geograph 10,056 张数据，只能明确作为规模、资源或独立泛化补充，不能暗示当前个人相册前端直接在该集合上完成了相同任务。

### 18.5 6 页正文建议预算

| 内容 | 预算 | 必须回答的问题 |
|---|---:|---|
| Title / Abstract / Index Terms | 0.20 页 | 问题、方法、数据、最关键真实结果 |
| I. Introduction | 0.70 页 | 为什么个人相册检索困难；Aim、O1–O3、RQ1–RQ2 |
| II. Related Work | 0.45 页 | 文献如何影响 CLIP/Caption/SG/Metadata/RRF 设计 |
| III. System Design | 1.20 页 | 独立召回、数据流、身份、索引、RRF 与资源约束 |
| IV. Experimental Setup | 0.65 页 | 数据、查询、qrels、A0–A4、指标和环境 |
| V. Results and Discussion | 1.55 页 | 总体/分类结果、延迟、失败案例、对 RQ 的回答 |
| VI. Conclusion | 0.55 页 | 目标达成、单一最重要技术成果、具体未来工作、反思 |
| References | 0.70 页 | 约 12–15 篇高质量 IEEE 格式来源及适用 DOI |

正文建议只保留两张主要原创图：一张系统架构图，一张代表性成功/失败检索图；其余 UI 截图和完整结果放 Supplementary Material。Results 应比 Background 占更多篇幅，因为 rubric 的 Technical Achievement 占 60%。

### 18.6 正式实验成为最高优先级

当前最大论文风险不是功能少，而是功能多但尚无正式人工相关性标注。即日起应暂停非必要的大功能扩展，优先完成：

1. 设计约 40–50 条查询，覆盖整体场景、物体/属性、关系、精确词项、时间/地点和中文自然语言；
2. 在查看各系统排名之前建立人工 qrels，记录标注规则和正相关照片；
3. 冻结照片、qrels、模型/依赖、索引版本、RRF 参数和运行配置；
4. 正式运行 A0–A4，至少报告 Recall@5/10、MRR、nDCG@10、p50/p95；
5. 按查询类型分组，回答 RQ2，而不是只报告一个总体均值；
6. 保留 3–5 个成功与失败案例，解释 BLIP 漏词、SG 语义近邻不等于精确关系、Metadata 缺失及查询翻译限制；
7. 将每个结果明确回链 O1–O3 和 RQ1–RQ2。

Qwen 三元组可以使用 BM25，但当前主 SG 通道是逐三元组向量检索。若核心 A0–A4 已完成且仍有时间，可增加小型关系检索子实验：

```text
S0  Scene Graph vector
S1  Scene Graph BM25
S2  SG vector + SG BM25（先在 SG 内部融合，再作为一个顶层通道）
```

该实验不能先于人工 qrels 和主基线，也不应把 SG BM25 与 SG vector 作为两个顶层独立证据重复加权。

### 18.7 截止日前计划

- **7 月 18–20 日**：冻结范围，完成查询集、相关性标准和 qrels；建立英文 IEEE 正文骨架；
- **7 月 21 日**：向 supervisor 发送含方法、实验设计和初步结果/明确占位符的草稿；
- **7 月 21–23 日**：运行 A0–A4、检查可复现性、整理总表和分组表；
- **7 月 24–27 日**：优先写 Results、System Design、Experimental Setup 和 Conclusion；
- **7 月 28–30 日**：根据反馈修订，补充失败案例和原创图；
- **7 月 31 日–8 月 2 日**：压缩到严格 6 页，检查英文、IEEE 引用、DOI、图表编号和正文引用；
- **8 月 3 日**：完成签字声明、AI 使用声明、Supplementary Material 和最终提交检查，避免最后一天提交风险。

### 18.8 对现有详细提纲的处理决定

`Smart_Photo_Narrative_论文详细提纲_ELEC5882M.docx` 继续作为完整资料蓝图，不直接照搬为正文。其优点是数据边界诚实、架构和实验矩阵清晰、已记录 SG 恢复与消融泄漏；主要问题是 O1–O5、RQ1–RQ3、A0–A4、N0–N3、事件、叙事、HPC 溯源和 Accessibility 同时展开，超过 6 页可证明范围。

最终正文采用“智能检索个人相册”为主线：检索消融是论文证据核心，Event/Story/详情弹窗作为系统下游能力简述。即使融合没有在所有查询上超过 CLIP，只要诚实报告各查询类型的正负结果和原因，仍是有效且符合 rubric 的硕士工程研究结论。

## 19. 2026-07-18：检索 qrels 与事件级 Story 双核心正式冻结

本节是对第 18 节“检索单核心”范围建议的后续修订。用户确认 Story 是论文中与智能检索同等级的重要模块，因此最终研究范围冻结为两个技术核心，而不是把 Story 仅作为界面展示：

- **RQ1**：不同多模态通道及其融合如何影响不同查询类型的检索效果？
- **RQ2**：事件级证据组织与严格验证能否减少故事中的重复和无依据陈述，同时提升连贯性？

这并不提前声称两个问题已经获得正向答案。A0–A4、完整 N0–N3 和人工偏好实验仍须在独立实验阶段运行；本轮只冻结输入、规则、代码和验收基线。

### 19.1 Story v2 截图失败基线

`2026-07-12` 的 14 张照片在旧 Story 页面中被展开成近似“一图一句”的英文列表。即使选择中文，截图中的正文仍为英文，并出现：

```text
possibly to wash their hands
possibly heading to a specific destination
likely sitting in a living room
possibly loading or unloading dishes
possibly getting ready for the day
possibly working or studying
likely working or studying
```

这不是可信故事：它重复 Caption 结构、缺乏事件级组织、没有语言强校验，并把 `possibly/likely/working/studying/getting ready` 等无证据意图或活动判断放进 Faithful 正文。因此该截图保留为 N1/Story v2 的正式失败案例，而不是删除或美化。

### 19.2 Story v3 证据组织与冲突边界

Prompt 版本冻结为 `grounded-story-v3-event`。实现保留逐图 `EvidenceItem`，并新增 `EvidenceGroup`：

- Date 先使用已保存 Event 作为一级分区；Event、Date 和 Story Basket 在分区内继续按相邻照片处理；
- 相邻时间差不超过 10 分钟，并满足 CLIP 余弦不低于 0.88 或模型观察词 Jaccard 不低于 0.60，才归入同组；
- 连续照片 CLIP 余弦不低于 0.94 时压缩为近重复，只保留一个代表观察，但全部 evidence ID 继续用于引用；
- 每个输入最多 5 个证据组，超出时按相邻时间与相似度合并最接近的组；
- BLIP Caption、Qwen 三元组和 CLIP Tags 始终标记为模型观察；低词汇重合本身不算冲突，只有 `dish rack/cage`、`dog/cat`、`indoor/outdoor`、`day/night` 等显式互斥标签才进入 `uncertain_observations`；
- 用户可选填写 `verified_context`。只有该字段可以支持身份、关系、情绪、目的等个人背景，模型观察本身不能。

冻结的 `2026-07-12` 回归案例由 14 张照片形成 5 个证据组，识别 1 张近重复照片和 2 个明确冲突组，已不再生成 14 个逐图段落。

### 19.3 输出契约、模式与安全闸门

Story v3 固定 JSON 字段为：

```text
title
paragraphs[{text, evidence_ids, group_id}]
uncertain_observations[{text, evidence_ids}]
unused_evidence_ids
creative_transitions
```

多图且至少 3 组时目标为 3–5 段；1–2 组时每组一段；单图仍是一条 photo note。Faithful 是默认模式，采用中性、可核查叙述，禁止第一人称、无依据身份/情绪/意图/目的/因果以及中英文风险词。Creative 可以使用第一人称和想象性过渡，但必须在 `creative_transitions` 中显式标记。

校验器检查 JSON、已知 evidence ID、组与引用对应、证据组覆盖、未用证据记账、标题和正文语言、段落相似度及 Faithful 风险词。中文有效文字中的汉字比例至少 25%，英文中的汉字不超过 10%；任意两段相似度超过 0.80 视为重复。失败仅修复一次，仍失败时退回按证据组生成的确定性记录，不再复制逐图 Caption，且不会把原 Caption 中的推测词泄漏到回退正文。

Story 页面同步增加证据分组预览、近重复提示、冲突提示、用户确认背景输入和 Faithful/Creative 差异说明。保存仍使用现有 `grounding_json`，记录 Prompt 版本与哈希、证据组、冲突、引用、未用证据、验证历史和来源类型，不迁移数据库。

### 19.4 检索 query/qrels v1 冻结

冻结数据基线为 376 张照片、376 个 Caption、374 个 Scene Graph、5,222 个三元组及相应向量。查询在查看系统排名前，依据全部原图联系表和可靠 EXIF 设计；没有从 BLIP/Qwen 输出反推查询。48 条查询配额如下：

| 类别 | 数量 | 设计重点 |
|---|---:|---|
| `scene` | 8 | 整体场景和环境 |
| `object_attribute` | 8 | 物体及属性 |
| `relation_semantic` | 8 | 同义动作和语义关系 |
| `relation_exact` | 8 | 严格方向的 S–P–O 关系 |
| `caption_lexical` | 8 | 精确 Caption 词项 |
| `metadata` | 8 | 日期、地点和可靠时间置信度 |

其中英文 36 条、中文 12 条；dev 12 条、test 36 条，每类固定 2 dev/6 test。相关性等级为 2（清晰完整满足全部条件）、1（仍满足全部条件但较小、遮挡或代表性弱）、0（不满足；exact-relation 关系或方向错误必须为 0）。Metadata 只允许日期、地点和时间置信度，不读取 Tags 或 Event ID。

候选池由 CLIP-only、Caption-only、SG-only 和 A0–A4 各 Top-20 去重合并，并用全相册联系表补充遗漏正例。两轮标注使用不同的稳定打乱顺序，判断表隐藏通道与分数。第二轮还纠正了一处联系表序号误读：橙色瓶的正例由 ordinal 50 修正为实际的 ordinal 49。当前私有 qrels 状态严格保留为 `agent_pass2`，没有冒充人工终审；用户确认正例和疑难项后才能改成 `human_validated`。

验证器已确认：48 条配额正确、查询文本不重复、全部 UUID 存在、每条至少一个 grade 2、Metadata 正例满足过滤条件，并可由 `load_qrels()` 读取。私有 qrels 不进 Git，公开清单 SHA-256 为：

```text
97dafa1112628643d90f9373f921483a130f952e881c6cb4c340904e270b238f
```

本轮没有运行正式 A0–A4，也没有产生 Recall、MRR 或 nDCG 提升结论。

### 19.5 Story N0–N3 与 12 案例冻结

变体定义冻结为：N0 是 legacy Caption＋Tags＋Metadata；N1 是逐图证据与引用的 `grounded-story-v2`；N2 加入事件分组、近重复压缩和冲突分离；N3 在 N2 上增加语言、引用、覆盖、重复和无依据声明验证、一次修复与分组回退。

私有案例集固定为 2 个单图、8 个 Event、2 个多事件 Date，中文/英文各 6 个。Event 尺寸覆盖 3 个 2–3 图、3 个 4–7 图和 2 个 8 图以上案例；`2026-07-12` 的 14 图 Event 是强制大事件回归。案例文件 SHA-256 为：

```text
fa0f47905413faae20eb2863f0ea2b5889f758c42f5c08d63dc8f0ff18eb8a5b
```

自动指标包括 JSON 有效率、引用有效率、证据组覆盖率、证据记账率、语言合规率、重复率、fallback 率、段落压缩率、延迟、声明数、最终无依据声明率和冲突处理率。N0/N3 的 12 组盲排顺序由 case ID 的 SHA-256 稳定生成，用户偏好选择留到正式实验。

### 19.6 离线测试与真实三例验收

| 验证 | 结果 |
|---|---|
| Story v3 / Story evaluation 专项 | 18/18 通过 |
| 完整单元测试 | 100/100 通过，33.360 秒 |
| qrels v1 结构与语义约束 | 通过，当前 `agent_pass2` |
| 12 个 Story 案例/照片 ID/分组上限 | 全部通过 |
| Streamlit AppTest | Library、Story Basket、Date 分组预览零异常 |

AppTest 在刚完成长时 Ollama 后使用 60 秒冷启动限制曾超时；提高到 180 秒资源竞争预算后通过。这是测试环境与 4 GB 笔记本 GPU 的冷启动限制，不应伪装成零成本。

按照协议只运行了一次、共三个真实 `llama3:latest` 案例，没有重跑失败案例：

| 案例 | 照片/组 | 生成延迟 | 模型草稿 | 最终硬约束 |
|---|---:|---:|---|---|
| 单图中文 | 1/1 | 26.513 秒 | 两次均未形成合规中文结构，fallback | 通过 |
| `2026-07-12` Event 中文 | 14/5 | 34.770 秒 | 组数、正文、冲突与中文校验失败，fallback | 通过 |
| 多事件 Date 中文 | 27/5 | 547.219 秒 | 两次 JSON 均因输出截断无法解析，fallback | 通过 |

三例最终结果的语言、引用有效率、组覆盖率、证据记账率、重复率、Faithful 风险词和冲突处理均通过，但三个模型草稿都没有被接受。正确结论是：**N3 的校验和分组回退成功阻止了不合规输出，但当前 `llama3` 在严格结构、中文遵循和长上下文 JSON 完整性上表现不足。** 这不能写成“Story v3 已提高生成质量”；正式 N0–N3 和 12 组盲评仍需后续运行。

三例私有输入、输出、Prompt 哈希、模型 digest、验证历史和指标已保存，其公开清单哈希为：

```text
09ff4d016ff3281148c627910f769432702534220c6757365f371214d2c06c66
```

下一步只剩两项论文实验动作：用户终审 qrels 正例/疑难项后运行 A0–A4；另行生成完整 N0–N3 并完成 12 组 N0/N3 盲排。二者都必须填入真实结果，不得根据本轮功能测试预设提升。

## 20. 2026-07-20：qrels 用户终审完成

用户检查了五张视觉正例审核表，确认除一项外其余 Scene、Object Attribute、Relation Semantic、Relation Exact 和 Caption Lexical 正例均正确。发现的具体问题是：

```text
query_id: relation_semantic_zh_dev_02
query: 一只手拿着饮料罐
private photo reference: f17e8a3c…（完整 UUID 仅保存在 Git 忽略的审核记录）
原等级: 2
终审等级: 0
```

该照片不能完整满足这条关系语义查询，因此从正例中移除；同一照片在其他查询中的等级没有被机械修改。查询仍保留另一个明确的 grade-2 正例，符合“每条查询至少一个 grade 2”的冻结规则。公开跟踪文档不保存完整照片 UUID。

终审后重新生成五张审核表和 8 条 Metadata 摘要，并重新运行完整 qrels 验证。结果为：

| 项目 | 终审结果 |
|---|---:|
| 查询数 | 48 |
| `human_validated` | 48 |
| 英文 / 中文 | 36 / 12 |
| dev / test | 12 / 36 |
| 每类查询 | 8 |
| grade 2 | 184 |
| grade 1 | 7 |
| grade 0 | 2,423 |
| 正相关判断总数 | 191 |

终审后的私有 qrels SHA-256 为：

```text
62828606955f317ed938a914bb18b19f65b56a55e2c8249ac32a2e0b98d96f41
```

这一步完成了“查询集与 qrels 冻结”的人工相关性依据。论文应准确描述为：查询和初始正例在查看系统排名前依据原图与可靠 EXIF 建立；候选池接受两轮独立打乱的 agent 判断；用户终审正例与疑难项并纠正一个错误正例。它仍是单一人工终审者，而不是多标注者一致性研究，属于需要在限制部分披露的边界。

至此 7 月 18–20 日计划中的“冻结论文范围，完成查询集和 qrels”正式结束。下一步可以运行 A0–A4 检索消融；在产生真实报告前仍不得声称任何模态或融合方法提升了 Recall、MRR 或 nDCG。

## 21. 2026-07-20：A0–A4 正式检索消融开始

### 21.1 实验输入与不可变约定

本次是 `human_validated` qrels 完成后的第一次正式 A0–A4 运行。开始时间为 2026-07-20 11:31（Europe/London）。实验期间不修改查询、相关性等级、照片、Caption、Scene Graph、向量、Metadata、RRF 参数或索引。

| 冻结项 | 值 |
|---|---|
| 照片 / Caption / Scene Graph / 三元组 | 376 / 376 / 374 / 5,222 |
| 查询 | 48；六类各 8；36 EN / 12 ZH；12 dev / 36 test |
| qrels 状态 | 48 条 `human_validated` |
| qrels SHA-256 | `62828606955f317ed938a914bb18b19f65b56a55e2c8249ac32a2e0b98d96f41` |
| 冻结范围清单 SHA-256 | `CA2D87FE66A7D35ABC44C40FD8BA079BE7E6A3199F325854DB08F3032C514C84` |
| CLIP | `openai/clip-vit-base-patch32` |
| Caption | `Salesforce/blip-image-captioning-base` 已保存结果；实验不重新推理 |
| Scene Graph | 已导入 Qwen 离线 JSONL；实验不重新推理 |
| RRF / 每通道候选深度 | `k=60` / `100` |
| 机器 | Windows；NVIDIA GeForce RTX 3050 Ti Laptop GPU；`torchtest` 环境 |

变体严格采用冻结定义：A0=CLIP；A1=CLIP＋Caption/BM25；A2=CLIP＋Scene Graph；A3=CLIP＋Caption/BM25＋Scene Graph；A4=A3＋仅由 qrels 明确给出的 Metadata 过滤。before-after、SG BM25 与 Ollama 查询解析均不混入本次实验。

### 21.2 正式命令与输出

```powershell
C:\Users\lenovo\.conda\envs\torchtest\python.exe scripts\evaluate_retrieval.py `
  evaluation\private\qrels_v1.json `
  --output outputs\experiments\retrieval_ablation_A0_A4_qrels_v1.json `
  --ks 1 5 10 `
  --repeats 3 `
  --warmup 1 `
  --variants A0 A1 A2 A3 A4
```

指标为宏平均 Recall@1/5/10、MRR（检索深度 10）、分级 nDCG@1/5/10，以及每次搜索调用的 mean/P50/P95 延迟。每条查询的三次排名必须完全一致；warm-up 不计入延迟。本节在报告通过结构、哈希、查询配额和确定性验证后再填写结果，运行前不预设哪一个变体最好。

### 21.3 报告完整性与可复现性验证

正式运行成功完成 5 个变体、48 条查询、每条 3 次计时搜索，共 720 次计时调用，另有每个变体 1 次不计时 warm-up。验证结果为：

- 报告 schema=1，`accuracy_status=computed_from_supplied_qrels`；
- 48 条均为 photo query，检索深度 10，cutoff 为 1/5/10；
- A0–A4 各有 48 条完整逐查询记录，且均读取 `human_validated` 判断；
- 报告 qrels SHA-256 与冻结值 `62828606…d96f41` 完全一致；
- Ollama parser=false，before-after optional mode 未运行；
- 所有查询在三次重复中的排名完全一致；
- 公开分析脚本从逐查询指标重新计算全部宏平均，与报告存储值在 `1e-12` 容差内一致；
- 六类各 8 条、36 EN/12 ZH、12 dev/36 test 配额再次通过。

评测模块回归测试 12/12 通过。独立隐私审计确认公开聚合不含 `query`、`photo_id`、`ranked_ids`、`relevance` 字段或 UUID；A3 与 A4 的 40 条非 Metadata 查询排名逐项完全相同，8 条 Metadata 查询排名发生变化，符合单变量消融设计。

私有逐查询报告位于：

```text
outputs/experiments/retrieval_ablation_A0_A4_qrels_v1.json
SHA-256: 5c810c11a09e0e4b1df94348d2f400df473013f34dfec213cb540d888e241e3b
```

该文件包含私有查询、UUID、排名和 qrels，因此保持 Git 忽略。公开聚合文件 `evaluation/retrieval_ablation_v1_summary.json` 不含查询文本、照片 UUID、逐项 qrels 或排名，并额外提供固定种子的 10,000 次 paired bootstrap。公开聚合 SHA-256 为：

```text
A20C321B7980A83C0C11E6038C44FBE64F6B7EF63B156D46D8520FF7E4DB7C9C
```

### 21.4 36 条 held-out test 主结果

12 条 dev 只用于开发分析，论文主要有效性结论使用 36 条 test；每类正好 6 条 test 查询。

| 变体 | MRR | Recall@1 | Recall@5 | Recall@10 | nDCG@10 |
|---|---:|---:|---:|---:|---:|
| A0 CLIP | 0.6501 | **0.4253** | **0.6211** | 0.6771 | 0.6218 |
| A1 CLIP＋Caption/BM25 | 0.5084 | 0.2903 | 0.4557 | 0.5701 | 0.4810 |
| A2 CLIP＋Scene Graph | 0.5614 | 0.3574 | 0.5474 | 0.6362 | 0.5482 |
| A3 CLIP＋Caption＋Scene Graph | 0.5118 | 0.2764 | 0.4636 | 0.6262 | 0.4950 |
| A4 A3＋Metadata | **0.6522** | 0.2882 | 0.5140 | **0.7282** | **0.6480** |

A4 的 MRR、Recall@10 和 nDCG@10 数值最高，但 Recall@1/5 仍低于 A0。Recall@1 在这里是“所有相关照片中被 Top-1 覆盖的比例”，Metadata 查询常有 9–27 个正例，因此即使首位结果正确，Recall@1 也不会接近 1；MRR 与 nDCG 更能反映首个正确项和排序质量。

作为补充，全 48 条（dev＋test）描述性 nDCG@10 为 A0 0.5918、A1 0.4660、A2 0.5326、A3 0.4925、A4 0.6386。该合并值不作为 held-out 主结论。

### 21.5 按 test 查询类型的 nDCG@10

| 类别（每类 6 条） | A0 | A1 | A2 | A3 | A4 |
|---|---:|---:|---:|---:|---:|
| Scene | **0.665** | 0.654 | 0.572 | 0.655 | 0.655 |
| Object Attribute | **0.833** | 0.750 | 0.772 | 0.750 | 0.750 |
| Relation Semantic | **0.792** | 0.231 | 0.708 | 0.339 | 0.339 |
| Relation Exact | 0.576 | 0.476 | **0.609** | 0.548 | 0.548 |
| Caption Lexical | **0.667** | 0.634 | 0.576 | 0.597 | 0.597 |
| Metadata | 0.198 | 0.140 | 0.052 | 0.082 | **1.000** |

分类结果说明：

- Metadata 是唯一清晰、稳定的增益来源；A4 与 A3 在其他 30 条 test 查询上排名完全相同，只改变 6 条带显式 Metadata 条件的查询；
- A2 在 `relation_exact` 上由 0.576 描述性提高到 0.609，但在 `relation_semantic`、Scene、Object 和 Caption 类均未超过 A0；每类只有 6 条，不能把该小差异写成普遍显著提升；
- A1 没有在 Caption Lexical 类超过 CLIP，说明 BM25 只能匹配 BLIP 已经写出的字面词，并且固定 RRF 可能把较弱的词项排名抬高；
- A3 在 Relation Semantic 上明显低于 A0/A2，说明把 Caption 与 SG 同时等权式融合并不会自动互补，噪声通道会稀释 CLIP 的强排名。

### 21.6 配对 bootstrap：增量是否稳定

以下为 36 条 test 的逐查询差值，95% CI 来自固定种子的 10,000 次 paired bootstrap：

| 比较 | ΔMRR [95% CI] | ΔRecall@10 [95% CI] | ΔnDCG@10 [95% CI] |
|---|---|---|---|
| A1−A0 | −0.1416 [−0.2705, −0.0189] | −0.1069 [−0.2114, −0.0267] | −0.1408 [−0.2450, −0.0469] |
| A2−A0 | −0.0887 [−0.1905, 0.0038] | −0.0408 [−0.1228, 0.0453] | −0.0737 [−0.1510, −0.0067] |
| A3−A0 | −0.1383 [−0.2588, −0.0231] | −0.0509 [−0.1024, −0.0087] | −0.1269 [−0.2167, −0.0466] |
| A4−A0 | +0.0021 [−0.1638, 0.1661] | +0.0511 [−0.0258, 0.1338] | +0.0262 [−0.1130, 0.1709] |
| A4−A3 | +0.1404 [0.0432, 0.2546] | +0.1020 [0.0324, 0.1860] | +0.1531 [0.0489, 0.2747] |

因此不能声称 A4 整体显著优于 A0：三项 A4−A0 区间都跨 0。可以声称在当前冻结查询集中，显式 Metadata 相对同样三通道但不应用过滤的 A3 带来稳定增益；同时 A1 和 A3 相对 A0 的下降区间不跨 0。A2 的 nDCG@10 下降区间刚好不跨 0，但 MRR 与 Recall@10 跨 0，结论应写成“未观察到总体 Scene Graph 增益”，而不是断言 SG 永远无效。

### 21.7 中文查询失败与防止 test 泄漏

36 条 test 中包含 30 条英文和 6 条中文。nDCG@10 为：

| 语言 | A0 | A1 | A2 | A3 | A4 |
|---|---:|---:|---:|---:|---:|
| English（30） | 0.7462 | 0.5772 | 0.6434 | 0.5940 | 0.7443 |
| 中文（6） | 0.0000 | 0.0000 | 0.0718 | 0.0000 | 0.1667 |

评测路径确实调用了项目已有的确定性中译英函数，但冻结词表没有覆盖“热气球、鹭鸟、贪吃蛇、坐、湖边”等关键概念，翻译后仍残留大量中文；英文 CLIP 与英文 Caption/SG 索引因此无法稳定匹配。A4 的中文非零结果主要来自结构化日期过滤，而不是视觉中文理解。

这是正式失败结果，不能在看到 test 后补词并使用同一 6 条查询重跑为“改进版 test”。后续若研究中文支持，应只用 dev 查询设计翻译改进，再建立新的未见 test 或明确把新运行命名为 post-hoc v2，不得覆盖本次 v1。

### 21.8 延迟与资源权衡

每个变体有 144 个计时样本（48 查询×3）：

| 变体 | Mean ms | P50 ms | P95 ms | Max ms |
|---|---:|---:|---:|---:|
| A0 | 85.4 | 72.4 | 211.4 | 270.1 |
| A1 | 74.4 | 69.6 | 97.2 | 185.1 |
| A2 | 134.2 | 129.5 | 167.7 | 318.4 |
| A3 | 170.8 | 145.7 | 301.0 | 496.3 |
| A4 | 173.6 | 149.3 | 339.8 | 513.8 |

SG 通道使 P50 从 A0 的 72.4 ms 增至 A2 的 129.5 ms，完整 A4 为 149.3 ms。A1 的观测延迟反而略低于 A0，不应解释为 Caption 使系统加速：变体按 A0→A4 顺序运行，GPU/OS/SQLite 缓存和不同查询文本会影响尾延迟；当前 latency 是固定协议下的工程观测，不是随机交错、严格隔离缓存的微基准。

运行环境为 Python 3.10.19、PyTorch 2.10.0+cu126、Transformers 5.2.0、ChromaDB 1.5.1、NumPy 1.26.4、Windows 10 和 NVIDIA GeForce RTX 3050 Ti Laptop GPU。CLIP 冷启动和模型加载发生在正式计时前。

### 21.9 对 RQ1 的当前回答

本轮不支持“增加所有模态会普遍提高检索”这一简单假设。CLIP 是当前个人相册上最稳健的非 Metadata 基线；Caption/BM25 与 Scene Graph 在固定 RRF 下的质量和校准不足，可能降低前列排序。Metadata 对明确日期/地点查询非常有效，并使 A4 的深度 10 指标最高，但 A4 相对 A0 的整体差异仍不确定。

因此 RQ1 应回答为：**多模态通道的价值强烈依赖查询类型；结构化 Metadata 对 Metadata 查询提供明确增益，而当前 Caption/Scene Graph 融合未产生总体提升，且中文翻译覆盖是主要失败来源。** 这是一项有效的负面与条件性实验结论，而不是系统失败后需要隐藏的结果。

## 22. 2026-07-20：Story N0–N3 正式实验完成生成与证据审计

### 22.1 实现、冻结与可复现边界

本轮补齐了四条真正独立的生成路径，而不是只给 N0–N2 起名字：

| 变体 | Prompt 版本 | 输入与处理 |
|---|---|---|
| N0 | `legacy-story-n0-v1` | Caption＋Tags＋可靠时间地点；自由文本、无引用 |
| N1 | `grounded-story-v2-per-photo` | 原始逐图多模态观察；每图一段和 evidence ID；只做事后结构/引用检查 |
| N2 | `grounded-story-v3-event-unvalidated` | 事件分组、近重复压缩和冲突分离；只生成一次，不修复、不回退 |
| N3 | `grounded-story-v3-event` | 严格复用 N2 首稿；校验后最多修复一次，仍失败则确定性分组回退 |

所有变体固定使用 `llama3:latest`、temperature 0.25、`num_predict=1200` 和由 case ID 稳定派生的同一 seed。模型 digest 与三例验收一致：

```text
365c0bd3c000a25d28ddbf732fe1c6add414de7275464c4e4d1c3b5fcb5d8ad1
```

N0 不启用 JSON format；N1–N3 启用。N2 与 N3 在每个案例上的首轮 Prompt 哈希、seed 和首稿输出哈希全部一致。正式输出逐条原子写入 Git 忽略的 JSONL；checkpoint 只有在 case、Prompt、context、digest 和 seed 全部一致时才跳过。实验没有写入生产 Story 数据库，没有重跑三个既有验收案例，也没有因正式输出较差而重采样、改 Prompt 或换模型。

正式生成完成 12 个冻结案例×4 变体共 48 条，无缺失、无重复；每个变体均为 6 中文/6 英文、2 单图/8 Event/2 Date。生成后的派生指标曾做一次语义修正：无正文的无效输出，其重复率、风险词率和压缩率从错误的 `0` 改为 `N/A`。该修正只重算指标，48 份原始/规范化模型输出及其输出哈希没有变化。

私有正式 JSONL 的当前 SHA-256 为：

```text
188fbddcaaaf637667488be97a192133fa063471b06859554d618b382badd6b8
```

### 22.2 自动指标与真实状态

下表为 12 个案例的宏平均；`N/A` 不作为 0 参与平均。unsupported 为逐案例声明审计率，而不是风险词命中率。

| 指标 | N0 | N1 | N2 | N3 |
|---|---:|---:|---:|---:|
| 状态 | 12 ok | 10 ok / 2 invalid | 10 unvalidated / 2 invalid | 1 ok / 11 fallback |
| JSON valid | N/A | 1.000 | 0.833 | 1.000（含确定性 fallback） |
| Citation valid | N/A | 0.917 | 0.833 | 1.000 |
| Evidence-group coverage | N/A | N/A | 0.365 | 1.000 |
| Language compliant | 0.500 | 0.583 | 0.500 | 1.000 |
| Text-unit duplicate rate | 0.000 | 0.0103 | 0.000 | 0.000 |
| Risk-term rate | 0.0952 | 0.0205 | 0.000 | 0.000 |
| Audited unsupported rate（macro） | 0.994 | 0.106 | 0.256（6 个有正文案例） | 0.083 |
| Paragraph compression | N/A | N/A | 0.597（可用输出） | 0.264 |
| Mean latency | 68.1 s | 172.9 s | 134.6 s | 273.4 s |
| P50 / P95 / Max latency | 68.9 / 100.1 / 101.5 s | 141.1 / 329.7 / 332.3 s | 91.8 / 365.9 / 401.2 s | 171.1 / 660.3 / 751.4 s |

N1 的两条 `invalid` 是逐图数量/引用契约失败，不等于 JSON 解析失败，所以 JSON valid 仍可为 1.000。N2 的两条大 Date 输出 JSON 截断；对无正文结果，重复与风险指标保持 `N/A`。N3 的 JSON、语言、引用和覆盖达到 1.000，主要原因是 11/12 进入了安全但泛化的确定性回退，不能解释为 `llama3` 的结构生成能力达到 91.7% 或 100%。

### 22.3 独立声明证据审计

风险词率与无依据声明率严格分开。私有匿名审计表逐条检查 376 个文本单元，状态和证据如下：

| 变体 | Supported | Unsupported | Uncertain | Non-factual | Unsupported / factual units（micro） |
|---|---:|---:|---:|---:|---:|
| N0 | 1 | 113 | 0 | 0 | 99.12% |
| N1 | 172 | 10 | 6 | 2 | 5.32% |
| N2 | 17 | 8 | 0 | 0 | 32.00% |
| N3 | 42 | 5 | 0 | 0 | 10.64% |

审计来源明确标为 `agent_evidence_audit`：先按确切引用检查 BLIP/Qwen/CLIP 模型观察与可靠 Metadata，再对跨语言和部分匹配项进行保守语义终审；没有冒充多个人工标注者。个人身份、关系、情绪、意图、因果、声音、气味等在 `verified_context` 为空时判为 unsupported；部分支持与额外细节混合则判为 uncertain。这里的“支持”只表示声明没有超出提供给生成器的模型观察/可靠元数据，不证明上游视觉模型对原图绝对正确。

最重要的新失败点是：N3 唯一被直接接受的模型输出包含 5 条无依据推断，例如由 `may / implies / focused / work routine / tools of trade` 表达的目的或工作状态；旧 validator 的词表没有覆盖这些同义表达，因此五条全部在独立审计中判为 unsupported。其余 11 个 N3 fallback 产生 42 条泛化但可核查的模板声明，没有 unsupported，但可读性和信息量较低。由此不能声称 N3 消除了幻觉，只能声称：**当前门控在绝大多数不合规输出上选择了保守回退，但规则仍会漏掉未列举的推断表达。**

私有声明审计 SHA-256：

```text
2fd2a6285e5e9f8788861a7cc787a713771b2b490d82895ac2f19ea3e95c1d76
```

### 22.4 配对变化与解释限制

只在两侧指标都可用的相同案例上做固定种子 10,000 次 paired bootstrap：

| 比较 | 平均差 | 95% CI | 配对案例 |
|---|---:|---:|---:|
| N2−N1 text-unit duplicate rate | −0.00386 | [−0.01118, 0.00000] | 6 |
| N3−N2 language compliance | +0.500 | [+0.250, +0.750] | 12 |
| N3−N2 audited unsupported rate | −0.0889 | [−0.2000, 0.0000] | 6 |
| N3−N2 risk-term rate | 0.000 | [0.000, 0.000] | 6 |

N1→N2 的重复率下降只覆盖 6 个两侧均有正文的案例，且上界为 0；它可作为当前样本中的描述性趋势，不能夸大成全 12 个案例的稳定提升。N3 的语言和 unsupported 改善主要由 91.7% fallback 驱动，因此安全性收益与信息量、可读性和平均延迟翻倍同时存在。

### 22.5 `2026-07-12` 定性案例

14 图、5 证据组的固定大事件结果为：N0 96.8 秒、N1 264.3 秒、N2 首稿 131.7 秒、N3 总计 174.4 秒并 fallback。N2 首稿只返回一个不合规结构，缺少标题和有效正文，遗漏全部 5 个组、14 个 evidence ID、两个冲突组，并且中文比例为 0；N3 的唯一修复又产生截断 JSON，因此进入 5 段确定性分组回退。这个案例证明门控阻止了坏结构进入最终故事，但最终文本更像安全的照片记录骨架，而不是丰富、连贯的回忆叙事。

### 22.6 N0/N3 用户盲评完成

独立盲评包与 A/B 映射分开保存。页面只展示同一案例的原图和匿名 N0/N3 标题/正文，不显示模型、variant、fallback、引用或验证信息。用户完成并锁定了全部 12 个 `A / B / 平局` 选择；锁定前没有读取映射，锁定后才由独立分析脚本揭盲。结果为：

| 用户盲评 | 结果 |
|---|---:|
| N0 胜 | **12** |
| N3 胜 | 0 |
| 平局 | 0 |
| 非平局 exact binomial（双侧） | `p = 0.00048828125` |

因此，N3 虽然通过 11 次确定性 fallback 把语言、引用、证据组覆盖和词表风险约束提升到 100%，却在 12 个案例的主观偏好中全部输给 N0。N0 的 factual-unit 微平均 unsupported 比例为 99.12%，中文案例也全部输出英文，但仍被用户选择；N3 fallback 则安全、简短，却过于泛化，缺少具体事件内容。由于用户没有填写可选原因，不能把偏好原因直接归因于“丰富性”或某个单独维度，只能结合文本和指标提出这一解释。

这 12 次选择来自同一位用户对 12 个案例的配对判断，不是 12 位独立参与者；exact binomial 只把案例级偏好作为探索性配对结果，不代表总体用户群。论文中必须把它描述为单用户个人相册 case study，并将更大规模、多参与者用户研究列为未来工作。

盲评响应、揭盲结果、公开汇总和公开 manifest 的 SHA-256 分别为：

```text

```

### 22.7 对 RQ2 的最终回答

RQ2 的答案是混合且带明显权衡的：

- N1→N2 在两侧均有正文的 6 个案例中，文本单元重复率描述性下降 `0.00386`，95% CI `[−0.01118, 0]`；由于另 6 个 N2 案例无可比较正文，不能声称全样本稳定减少重复。
- N2→N3 的语言合规提高 `0.500`，审计 unsupported 宏平均下降 `0.0889`；但 11/12 依赖 fallback，唯一直接接受稿仍漏过 5 条无依据推断，所以这主要证明保守回退有效，不证明 validator 完整或模型事实性稳定。
- N3 平均延迟为 273.4 秒，是 N0 的约 4.0 倍；最大延迟 751.4 秒。
- 用户盲评 N0 以 12:0 完胜 N3，说明当前安全门控没有提升感知连贯性或叙事吸引力，反而暴露了“可信但泛化”与“丰富但无依据”的尖锐权衡。

最终可写为：**事件级组织在可用案例中减少了逐图重复，严格门控和分组回退显著提高了形式合规并降低了可检测的无依据声明；然而当前 `llama3` 的结构遵循能力不足导致 91.7% fallback、显著延迟和高度泛化文本，主观偏好没有提升。下一版系统应优先改进基于声明的语义验证和信息保持型 fallback，而不是继续扩大禁止词表。**

## 23. 正式实验后的生产模型迁移：Qwen3 4B

### 23.1 迁移边界

2026-07-20，在 N0--N3 结果、盲评和哈希全部冻结后，生产应用的默认 Ollama 模型从 `llama3` 切换为本地 `qwen3:4b`。此次变更用于后续 Story 工程改进及可选查询解析，不覆盖、不重跑也不重新命名既有 N0--N3 结果；`story_evaluation.py` 继续固定 `llama3:latest` 及原模型 digest。后续若形成正式新变体，应命名为 Story v4 / N4，而不能写成原 N3 的结果。

### 23.2 配置与可验证约束

- `config.py` 的生产默认模型为 `qwen3:4b`，并允许通过 `SMART_PHOTO_OLLAMA_MODEL` 显式覆盖。
- Story 与可选 Ollama 查询解析的每次 `ollama.chat` 调用都显式传入 `think=False`，避免推理轨迹混入 JSON 或叙事正文。
- 查询解析使用 4096 context / 300 prediction tokens；当前整篇 Story v3 路径暂用 8192 context / 1200 prediction tokens。后续逐证据组 Story v4 应再把单次上下文压缩到 4096。
- Streamlit 侧边栏显示实际配置的模型名称和 `thinking off`，便于运行时审计。

### 23.3 本地验收

本机 Ollama 已存在 `qwen3:4b`，模型文件约 2.5 GB。使用本地 `/api/chat` 发送 `think=false`、JSON 模式的最小请求，返回模型名 `qwen3:4b`、内容 `{"status":"qwen-ready"}`、空 thinking 字段和 `done_reason=stop`。4096 context 下 `ollama ps` 显示模型总占用约 3.6 GB，并发生 CPU/GPU 混合卸载；因此后续性能结论必须记录实际 context 和 processor split，不能只按模型文件大小推断完全 GPU 运行。

新增配置契约测试覆盖生产默认模型、Story 请求和查询解析请求中的 `think=False` 及 context/prediction 参数；相关 33 项定向测试全部通过，完整 `unittest discover` 两次以退出码 0 完成。项目自己的 `OllamaGenerator` 真实调用返回 `{"status":"project-qwen-ready"}`，随后 Streamlit 测试服务的 `/_stcore/health` 返回 HTTP 200，测试服务已关闭。此处只证明配置、调用契约和应用启动生效，不提前声称 Qwen 已提高 Story 质量；质量与 fallback 改善需要独立的 N4 工程评测。

## 24. 2026-07-21：Creative 分层叙事失败审计与生产 v4 修复

### 24.1 真实失败案例

本节记录正式 N0--N3 实验结束后的生产失败，不修改第 22 节的实验输出、哈希或结论。失败输入来自 Story Basket，共 4 张照片、3 个证据组，语言为中文，模式为 Creative；已保存失败记录的 `story_id` 为 `db804d9d-70ca-4c8b-b17e-905a2b0425d7`，时间为 `2026-07-21T09:18:55+00:00`。

Qwen 首稿的事实段落 1、3错误使用第一人称，同时输出了不存在的 uncertain evidence ID；因此触发 `E_FIRST_PERSON_IN_FACT` 和 `E_UNCERTAIN_CITATION`。修复稿仍在事实段落 1、3使用第一人称，第二轮继续被拒绝。旧生产路径随后无条件调用 `deterministic-group-fallback`，页面标题为“事件照片记录”，正文是按证据组排列的固定安全模板，而不是 Qwen 的 Creative 正文。

这个案例暴露的不是“模型完全没有生成”，而是两个系统设计问题：第一，v3 要求小型本地模型同时生成事实、引用、冲突、未使用证据和创意字段，契约负担过重；第二，两次失败后的全篇安全回退虽然阻止了不合规内容，却牺牲了具体信息与故事性。用户看到的泛化模板与 Creative 预期明显不符。当前失败记录保留在数据库中，作为真实审计证据，不删除、不覆盖。

### 24.2 修复边界与版本命名

修复命名为 `grounded-story-v4-layered-creative`，只用于正式实验之后的生产 Creative，不覆盖 N0--N3，也不改变 Faithful 的 `grounded-story-v3-event`、8192 context、1200 prediction tokens、最多一次修复和确定性 fallback 行为。生产模型继续是 `qwen3:4b`，`think=False`；本次没有重新加入此前撤销的 compact prompt 或单轮长事件加速方案。

v4 将输出职责拆成两层：

```text
title
groups[{group_id, factual_text}]
creative_transitions[string]
```

- `factual_text` 只能写可观察证据、可靠时间地点和用户明确确认的背景；禁止第一人称、身份、关系、情绪、目的、因果及无依据推测。
- `creative_transitions` 是显式的非事实层，允许第一人称、气氛、反思和想象性连接，但不能把新增人物、物体、地点、动作或事件伪装成照片事实。
- 3 个证据组由动态 JSON Schema 固定为 3 个事实段和 2 个过渡；单组固定为 1 个事实段和 1 个创意反思。
- evidence ID、冲突观察和未使用证据由程序根据 `StoryContext` 确定性注入，模型不能再自行制造未知引用。
- 修复 Prompt 包含第一次被拒绝的原始输出和具体错误代码；若第二次仍失败，结果为 `status=error`、空正文且不写入 Story 数据库，不再展示通用 fallback，也不展示被拒绝草稿。
- 页面把每个创意过渡交错放在相邻事实段之间，并标记“✨ 创意过渡——非照片事实”；`ok`、`repaired`、`fallback`、`error` 状态均在故事顶部显示。Creative 失败时提供重新生成按钮。

### 24.3 验证记录

实现后首先使用假生成器验证：Creative 事实段中的第一人称和推测词会被拒绝，第一人称创意过渡可被接受；中文标题、事实段和过渡均进入语言校验；引用与冲突由程序注入；修复请求包含首稿与错误；连续两次失败不生成 fallback、不写数据库；Creative 成功记录使用 v4 Prompt 版本；Faithful 原有成功、修复和 fallback 路径保持 v3。

随后复用本节失败案例的同一 4 图/3 组 Basket 做一次 `save=False` 的真实 `qwen3:4b` 中文 Creative 验收。首稿因输出为英文触发 `E_LANGUAGE_ZH`；模型在唯一一次修复中改正，最终状态为 `repaired`，得到 3 个事实段和 2 个创意过渡，独立再次调用 validator 的结果为空，且没有使用 `deterministic-group-fallback`。生成前后 Story 表记录数一致，确认验收没有写入数据库。记录如下：

```text
model_name: qwen3:4b
prompt_version: grounded-story-v4-layered-creative
prompt_hash: c8acfca4844daba0ee1b543aaf23bde6041db58d85fb4ba62f74c5fb9e81f99f
status: repaired
latency_ms: 49355
paragraphs / transitions: 3 / 2
first_attempt_validation: E_LANGUAGE_ZH (Chinese ratio 0.000)
final_validation: pass
fallback_used: false
database_write: false
```

定向 Story、UI helper、Story evaluation、ablation 和 Ollama 配置测试共 38 项，全部通过；完整 `unittest discover` 共 121 项，退出码为 0。Streamlit 测试服务启动后，`/_stcore/health` 返回 HTTP 200，随后已关闭；AppTest 分别加载默认页和 Story 页，异常数均为 0，并确认 Story 标题及生成按钮存在。前端 Markdown 测试确认事实段、醒目标记的创意过渡和 Evidence 按预期顺序交错。

该验收只证明新契约与失败处理在一个真实开发案例上可用，不构成 N4 的整体质量实验，也不提前声称 v4 优于 N3。

## 25. 2026-07-21：Creative v5 第一人称连续回忆叙事

### 25.1 v4 可读性失败与新目标

生产 v4 的真实 3 图/3 组中文 Creative 记录（`story_id=64edc42a-0059-496d-8c6a-a42276fa7a03`）虽然由 `qwen3:4b` 生成并在一次修复后通过，但页面呈现为三条“完整日期＋地点＋照片描述”和两个醒目的蓝色创意框。标题 `Antalya Horizon` 为英文，仍因正文整体中文比例超过阈值而被接受；过渡则使用“仿佛时间被海风拉长”等通用诗意表达。该输出没有使用 fallback，但证明“结构通过”不等于“自然故事”：一组一条事实句、分层标签直接暴露、全局语言比例和缺少开头/收束共同造成了割裂感。

新的生产版本命名为 `grounded-story-v5-first-person-memoir`。它属于正式 N0--N3 之后的工程改进，不修改 Faithful v3、历史 v4 类、正式实验输出或哈希，也不把本节开发验收命名为新的正式 N4 实验。

### 25.2 v5 设计与可信边界

v5 的模型输出契约为：

```text
title
opening
groups[{group_id, factual_text}]
transitions[string]
closing
```

- opening、事实组、相邻过渡和 closing 按顺序组成正式连续正文；3 个证据组固定为 3 个事实段和 2 个过渡，单组不再额外制造 transition。
- 默认 narrator role 为 `observer`：事实段必须使用“我从画面中看到”“映入我眼帘的是”“我的视线转向”等第一人称观察表达，不能把照片人物直接称为“我”。
- Story 页面新增“照片中的主要人物是我”结构化确认；勾选后使用 `confirmed_subject`，只允许把有人物证据支持的主要人物动作改写为第一人称。该确认不支持情绪、目的、关系或因果，这些仍必须来自 `verified_context`。
- 中文与英文 narrator 句式分开校验，中文不能用英文 `I see` 冒充第一人称。中文 opening 必须明确建立“我”的视角；后续 transition 和 closing 允许符合中文语法的主语省略。英文仍要求显式第一人称。
- 标题、opening、每个事实段、每个 transition 和 closing 分别做语言比例校验；纯英文中文标题会失败，包含拉丁字母专名但具有足够中文内容的标题可以通过。
- 模型 Prompt 不再提供用于正文复制的时间地点；validator 拒绝正文中的 ISO 日期、具体时分和已知地点。界面在标题下根据 `timestamp_confidence >= 0.6` 的证据只展示一次日期、时间范围和去重地点。
- 2--3 组中文故事目标仍为 250--400 字；硬性“非 Caption”下限为 160 个中文字符，避免因差几字将已有完整七段结构丢弃。英文及其他组数使用独立下限。
- validator 拒绝“仿佛时间被”“岁月定格”“展开新的篇章”等已知通用套话，同时继续检查推测、冲突、身份、情绪、目的、引用、覆盖、重复和结构。
- evidence ID、冲突、未使用证据和 narrative block 类型均由程序注入；两次失败仍返回 `error`、不保存、不 fallback。

### 25.3 保存与页面呈现

`stories` 表不迁移。v5 的 `content` 保存用户实际看到的完整连续正文，而 `grounding_json` 保存 opening、closing、事实/创意 block 类型、group ID、Evidence、event summary、narrator role、Prompt 版本和校验历史。

主页面只显示标题、一次事件摘要和连续故事段落，不再直接显示蓝色创意框、G001 或 Evidence 行。“故事依据与校验 / Story grounding audit”默认折叠，内部展示事实/非照片创意类型、引用、冲突、未使用证据、模型、Prompt、状态、校验历史和证据照片。Markdown 主体与页面顺序一致，并使用折叠审计附录；JSON 保留旧字段并增加新的叙事块字段。

### 25.4 开发失败点与最终真实验收

实现过程中保留了以下失败点，而不是只记录最终成功：

1. 首次真实 v5 输出避开了日期、地点和套话，但自然使用了白名单之外的观察句式，且总长 175 字，比初始 180 字硬阈值少 5 字；两轮后返回 `error`，未保存。
2. 尝试把第一人称正则放进动态 JSON Schema 时，本机 Ollama 返回 `invalid JSON schema in format`（HTTP 500）。确认本地结构化输出不支持该 `pattern` 子集后，Schema 恢复为基础类型、数量和长度约束；第一人称规则留在 Prompt 与本地 validator。
3. Qwen 的后续输出在部分中文过渡与 closing 中省略“我”。这属于中文连续叙事的正常主语省略，因此规则调整为 opening 和所有事实组建立第一人称视角，中文过渡/收束可延续隐含主语；英文不放宽。
4. 一次验收暴露中英文 observer 规则混用可能让 `I see` 出现在中文正文；最终将中英文第一人称模式完全隔离并增加回归测试。

最终复用截图对应的 Antalya 3 图/3 组案例执行 `save=False` 的真实 `qwen3:4b` observer 验收，结果为：

```text
model_name: qwen3:4b
prompt_version: grounded-story-v5-first-person-memoir
prompt_hash: 65d14b18da20eebdc028681b2753a7552bc0d42e93199ff8c4f0d48ef90fa11a
status: ok
latency_ms: 27720
title: Antalya的光影碎片
photos / groups: 3 / 3
opening / factual paragraphs / transitions / closing: 1 / 3 / 2 / 1
Chinese characters: 233
post_validation: pass
date_or_clock_time_in_body: false
known_cliche_in_body: false
fallback_used: false
database_write: false
```

Story/UI 定向测试扩展到 32 项并通过；完整 `unittest discover` 共 130 项，退出码为 0。Streamlit AppTest 加载默认页及 Creative Story 页均为 0 异常，并确认生成按钮和 narrator identity checkbox 存在；测试服务 `/_stcore/health` 返回 HTTP 200 后已关闭。

该单例说明 v5 的第一人称、连续结构、语言、元数据分离和失败处理在一个真实开发案例上可运行；它不证明 v5 在完整故事集上的主观质量优于 v4/N3。若论文需要比较，应另行冻结案例和评分规则，不能把本节调试过程当作正式实验结果。

## 26. 2026-07-21：v5 连续回忆的割裂/重复修复（异质多情景编织）

### 26.1 真实失败案例（生产）

2026-07-10 晚间 Story Basket（7 张，21:21–23:00，拼图、猫、智能马桶红色指示灯、浴室镜柜、床，页面标题“桌面谜题”）在 §25 的 v5 `observer` 下真实生成后暴露三个问题：

- 每张照片各成一段：异质照片彼此不满足相邻分组阈值（时间≤10min 且 CLIP≥0.88 或 Jaccard≥0.60），退化为“一图一段”。
- 每个事实段被强制以“我从画面中看到 / 映入我眼帘的是”开头，正文出现约 6 次同构起句。
- 强制 N-1 条过渡“连接相邻两组的两个场景”，在不相关照片之间编造物理连接（如线缆延伸至浴室镜子、拼图碎片影子与窗外夜空重叠）。

同时事实段禁止任何感受，读起来是观察清单而非自然回忆。这与用户“整个事件连贯且自然、把不同情景编织在一起”的诉求冲突。此现象不是新失败模型，而是 §25 v5 契约中“逐组一段＋强制观察者模板＋强制过渡”三条硬约束在异质输入上的必然结果。

### 26.2 就地修复（仍为 `grounded-story-v5-first-person-memoir`，未换模型）

不新增模式、不改动 Faithful v3 与冻结的 N0--N3、不修改历史 v4：

| 病根 | 修复 |
|---|---|
| 一图一段 | 新增 `StoryGenerator._reduce_groups`：Creative 生成前把证据组按相邻时间合并到 ≤ `STORY_NARRATIVE_MAX_PARAGRAPHS`(=4)，优先合并时间最接近的相邻对；多情景被编织进少数流动段落。 |
| 强制“我看见”开头 | 删除事实段固定观察者模板强制（去掉 `E_OBSERVER_ROLE` 模板要求）；仅保留 opening/过渡/closing 的第一人称引导，事实段开头由模型自选并要求多样化。 |
| 强制过渡编造连接 | 过渡改为可选（schema `minItems=0`，校验新增 `allow_fewer_transitions`）；Prompt 明确过渡只做时间/氛围衔接，禁止断言两张不同照片之间的物理、空间或因果连接。 |
| 事实段禁止一切感受 | 事实段推测词改用缩减集 `NARRATIVE_BANNED_TERMS`（仅身份/关系、硬因果、他人活动/目的），允许轻度氛围与感受；身份/关系仍需 `verified_context` 支持。 |
| 小模型达不到长度 | 合并后组数更少，memoir 长度下限与目标下调到 `qwen3:4b` 可达（例如 >3 组中文正文下限由 280 降到 200，目标由 400–650 降到 320–520）。|

保留 v5 既有可信约束：冲突分离、引用/覆盖/证据记账、逐字段语言比例、元数据不入正文、通用套话拦截、两次失败返回 `error` 且不保存。

版本命名说明：为满足“就地修复”要求，生产版本名仍为 `grounded-story-v5-first-person-memoir`；本次修改改变了 Prompt 与 validator 行为，因此 §25.4 记录的 Antalya `prompt_hash` 不再复现。若论文需要严格版本溯源，应把本次改动另命名为 v5.1。

### 26.3 测试与真实验收

- 定向 story grounding 测试更新 3 项（原先编码旧 observer 模板与“情绪一律禁止”策略）、新增 4 项（组合并、生成级编织、过渡可选、轻度感受与身份禁令），共 34 项通过；完整 `unittest discover` 134 项通过。
- 复用 §26.1 同一 2026-07-10 晚间 basket 做 `save=False`、中文、`qwen3:4b`、temperature 0.5 的真实验收：

```text
evening_photos: 7
pre_reduce_groups: 5  ->  reduced_groups / paragraphs: 4
transitions: 2（模型自选，少于 N-1=3）
status: ok（首轮通过，无 fallback，未写库）
opening / closing: 均为第一人称
factual 起句多样: 七夕夜的房间里 / 房间里 / 卫生间里 / 床边的毯子下
无强制“我从画面中看到”，无跨照片物理连接
```

结果由观察清单式割裂文本转为 4 段连贯的第一人称回忆，并包含轻度氛围。

### 26.4 修复引入的新可信代价（如实记录）

放宽“允许氛围/感受”后出现新的、词表无法拦截的失败：

- 上述验收输出把 2026-07-10 命名为“**七夕夜**”，而 2026 年七夕实为 8 月 19 日——这是模型虚构的**场合/框架**，`NARRATIVE_BANNED_TERMS` 与套话表都不覆盖。
- 通用诗意表达（如“仿佛时间在这一刻被拉长”）因中间插入字符绕过了 `仿佛时间被` 的精确匹配。

因此本节只能声称：**v5 修复解决了割裂、强制同构起句与编造物理连接，并允许自然氛围**；不能声称它消除了幻觉。相反，允许创造性使模型可以发明未拍到的场合，须用 Faithful 模式或 `verified_context` 规避。单个非确定性样本也不构成质量证明；如需正式比较，应另立 v5.1 / N4 并冻结案例与评分规则。

### 26.5 `confirmed_subject` 回归：提示词与校验器契约错位

在 §26 修改后，Creative 页面勾选“照片中的主要人物是我”时出现两轮均被拒绝、页面无故事的真实回归。失败摘要包含：

```text
E_FIRST_PERSON_VOICE: factual_text 1 must use first-person narration
E_FIRST_PERSON_VOICE: factual_text 3 must use first-person narration
E_MEMOIR_REPETITION: narrative blocks 1 and 7 exceed 0.90
E_MEMOIR_REPETITION: narrative blocks 1 and 6 exceed 0.90
```

这不是 Qwen 未返回内容，而是程序在收到首稿和修复稿后主动拒绝。根因有两项：

1. Prompt 要求全篇维持第一人称，但 `confirmed_subject` validator 额外要求**每个事实段**显式包含“我/I”。自然中文在 opening 建立主语后会省略“我”；纯物体证据组也不应为了形式而声称叙述者身处画面，因此正确内容被误判。
2. v5 额外把 opening、事实段、transition 和 closing 全部两两比较。opening 与过渡/收束本来就会呼应统一主题，仍被 `>0.90` 当成致命重复；该风格问题因此导致整篇不保存。

就地修复保持 Faithful v3、N0--N3 和数据库结构不变：

- 第一人称改为故事级契约：中文 opening 必须明确建立“我”，后续事实段、过渡和收束允许自然主语省略，不再强迫每段重复“我从画面中看到”。
- Prompt 为每个证据组注入 `has_visible_person_evidence` 与 narration instruction。只有 `confirmed_subject` 且组内确有人物证据时，才允许把明确观察到的主要人物动作改写为“我……”；纯物体组只能使用观察者口吻或直接场景描述。
- 仍保留 `E_CONFIRMED_SUBJECT_NO_PERSON`：没有人物证据时写“我坐在/我走向”等画内动作仍会被拒绝，勾选框不能绕过证据边界。
- 事实段重复继续由原有 `E_PARAGRAPH_DUPLICATE` 检查；v5 额外重复检查只比较 transition 之间，不再把 opening 与 transition/closing 的主题呼应作为致命错误。
- 观察者句式白名单补充“我从下一幅画面中看到”“我在最后的画面中看到”“我把目光投向”“我望向/看向”等自然变体；它们不再被误判为画内动作，同时“我坐在/我走向/我拿起”仍不属于观察表达。
- 一次修复 Prompt 新增按错误码生成的具体指导；失败页面使用去重后的 `validation_codes`，不再把首稿与修复稿的同一错误重复展示。

验证结果：Story/Streamlit 定向测试 **41/41** 通过，完整 `unittest discover` **139/139** 通过，退出码 0；`git diff --check` 通过。新增测试覆盖中文 `confirmed_subject` 的主语省略、纯物体组画内动作拒绝、自然观察句式、逐组人物证据提示、opening/transition 主题呼应、修复提示，以及一条复现截图多错误组合的生成级回归。本节未重新运行真实 Qwen，避免把非确定性单例误写为质量提升；最终生产验收仍需用户在相同 Basket 中重新生成一次并观察状态是否为 `ok` 或 `repaired`。

## 27. 2026-07-22：照片级拍摄者心情 Metadata 与 M0/M1 单案例实验

### 27.1 研究动机与证据边界

本轮为 `2026-07-07 · Event 1` 的 5 张热气球照片增加“拍摄者在按下快门时的心情”Metadata。由于当前照片没有同步心率、HRV 或其他可穿戴传感器记录，系统不模拟生理数据，也不声称从单次心率识别情绪。论文中的正式定义为：

> User-annotated photographer mood metadata, used as a proxy for future wearable affect signals.

该定位参考了情感计算研究中把生理信号与参与者自评标签配合使用的实验范式，而不是把主观标签伪装成传感器输出：[EmoWear](https://pubmed.ncbi.nlm.nih.gov/38898046/)；[Smart bracelet heart-rate emotion recognition](https://pubmed.ncbi.nlm.nih.gov/32012920/)。本项目只采用人工确认的六类离散标签：`neutral`、`calm`、`happy`、`excited`、`tense`、`sad`。Mood 的主体固定为 **photographer**，与“照片中的主要人物是我”完全独立，也不能用于推断照片中人物的情绪、关系、目的、原因或医学状态。

### 27.2 数据层与迁移审计

SQLite schema 从 v1 升级为 v2，新增独立 `photo_moods` 表；`photos` 主表、FTS5 文档和现有 Story 表均未修改。每条 Mood 包含：`photo_id`、规范标签、`source=manual`、`subject_role=photographer`、`confirmed=1`、`annotation_set=hot_air_balloon_mood_v1`、创建及更新时间。数据层增加 `PhotoMood`、批量读取、原子保存/覆盖/删除以及非法标签拒绝；照片详情可只读显示标签、来源和人工确认状态。

迁移前建立了两份本地、Git 忽略的数据库备份：

```text
data/smart_photo.pre_mood_schema_v2.20260722.db
data/smart_photo.schema_v1_backup.db
```

真实数据库迁移前后计数如下，全部保持不变：

```text
photos: 376 -> 376
captions: 376 -> 376
scene_graphs: 374 -> 374
scene_graph_triples: 5,222 -> 5,222
tags: 1,128 -> 1,128
events: 162 -> 162
event_photos: 356 -> 356
stories: 20 -> 20
schema_version: 1 -> 2
photo_moods after migration: 0
```

`photo_moods=0` 是预期状态：用户尚未为 5 张照片选择真实心情，系统没有代替用户编造标签。

### 27.3 Creative v6 与检索隔离

生产 Creative 新版本为 `grounded-story-v6-photographer-mood`。`EvidenceItem` 可携带独立的人工 Mood 记录，但 `evidence_text()`、BLIP/Qwen/CLIP `model_observations`、事件分组、近重复压缩和检索文档均不读取 Mood。因此 Mood 不进入 FTS5/BM25、CLIP、Scene Graph、Metadata 检索或 A0--A4，也不改变冻结实验。

Story 页仅在选中冻结的 5 张热气球照片时显示“拍摄者心情 Metadata（实验）”：每张照片有缩略图、文件名与六类下拉框，可一次原子保存整组。页面明确说明这是拍摄者人工回忆，不是照片人物情绪或心率推断。Library 详情增加只读 Mood 信息。

Creative 增加 `Use photographer mood metadata` 开关；Faithful 始终强制忽略。M0 与 M1 使用完全相同的 v6 system prompt、照片、模型参数和 seed，区别只在 user context 是否含已确认 Mood：

- M0：Mood disabled；出现明确拍摄者心情声明触发 `E_UNSUPPORTED_MOOD`。
- M1：只允许使用当前证据组已确认的标签；至少自然使用一个标签，否则触发 `E_MOOD_UNUSED`。
- 把标签归给照片人物触发 `E_MOOD_SUBJECT`；Mood 不支持原因、目的、关系、因果或医学推断。
- 输入 Mood、正文 Mood 声明、Prompt 版本、引用和校验历史写入 `grounding_json` 审计层。
- `OllamaGenerator` 现支持显式 seed；本案例固定 `qwen3:4b`、`seed=20260707`、`temperature=0.25`、`think=False`、8192 context、1200 prediction tokens。

### 27.4 私有 M0/M1 运行与盲评工具

新增 `mood_evaluation.py`、`scripts/run_mood_story_experiment.py` 和独立 `scripts/mood_story_blind_review_app.py`：

1. 运行器要求冻结 Event 恰有 5 张照片且 5 条 Mood 全部存在；按有序 photo ID、图片 SHA-256、标签和 annotation set 生成 Git 忽略的 manifest。已冻结内容发生漂移时拒绝覆盖。
2. M0/M1 均以 `save=False` 生成，不写生产 `stories` 表；记录模型 digest、seed、Prompt/context/output SHA-256、原始两次尝试、规范化正文、状态、延迟、修复次数、重复率、Mood 覆盖率和无依据 Mood 声明数。
3. 独立盲评页显示相同 5 张证据照片和匿名 Story A/B，隐藏 M0/M1 映射。用户选择 A、B 或平局，并分别给连贯性、个性化、可信度打 1--5 分；提交后锁定。
4. 公开摘要只保留指标、标签分布、私有结果哈希和匿名偏好，不含 UUID、路径、照片或故事全文。本实验只作为单案例探索性研究，不命名为 N4，不进行统计显著性或总体提升声明。

正式命令为：

```powershell
# 先在 Story 页面选择 2026-07-07 · Event 1，为 5 张照片保存真实心情
conda run -n torchtest python scripts/run_mood_story_experiment.py
conda run -n torchtest streamlit run scripts/mood_story_blind_review_app.py --server.port 8503
# 完成并锁定盲评后揭盲并生成隐私安全摘要
conda run -n torchtest python scripts/run_mood_story_experiment.py --reveal
```

### 27.5 测试结果与尚未完成项

新增测试覆盖：v1→v2 迁移、Mood 原子保存/覆盖/删除、非法标签整批回滚、照片详情、FTS 排名不变、Faithful Prompt 隔离、M0/M1 system prompt 相同、M1 已确认标签使用、错误标签/人物归因/未使用拦截、Ollama seed、manifest 漂移拒绝、匿名映射，以及盲评页提交/恢复/锁定。

结果：Mood/Story 定向测试 **63/63** 通过；完整 `unittest discover` **148/148** 通过，退出码 0；主应用 AppTest 的 Library、Story Basket、Story Date preview 为零异常，新 Mood 盲评页 AppTest 也为零异常。一次额外的 PowerShell 后台健康检查因本机环境同时含 `Path` 与 `PATH` 键而在 `Start-Process` 前失败；它没有启动应用，也不是 Streamlit 代码异常，页面启动已由 AppTest 覆盖。

用户随后为 5 张照片完成真实人工标注，有序标签为 `happy, tense, tense, excited, excited`；分布为 happy 1、tense 2、excited 2。私有 manifest 成功冻结，SHA-256 为 `643780e01ac7dd1f8631959acc9d96ff76de657fe0dce97cef119d4e71a56bb5`。本机 `qwen3:4b` digest 为 `359d7dd4bcdab3d86b87d73ac27966f4dbb9f5efdfcc75d34a8764a09474fae7`。M0/M1 的 system prompt SHA-256 均为 `df7271f904df260b8f717fdfed396a70afca16fe9e085f1de2d0816404306377`，确认两者使用同一 system prompt；差异只存在于 user context 的 Mood 开关与记录。

真实固定种子生成结果如下：

| 变体 | 状态 | 尝试 | 延迟 | Mood 声明 | Mood 覆盖 | 校验结果 |
|---|---|---:|---:|---:|---:|---|
| M0 | `ok` | 1 | 31,021.76 ms | 0 | N/A | pass |
| M1 | `error` | 2 | 50,041.02 ms | 0 | 0.0 | `E_MOOD_UNUSED` |

M0 没有无依据 Mood 声明并在首轮通过。M1 的首稿没有使用任何已确认标签；唯一一次修复后仍完全未使用 Mood，因此按预注册规则返回 `error`、空正式正文、不保存且不 fallback。审计确认两次输出均不含六类 Mood 词，这不是标签同义词漏检或人物归因误判，而是本次 `qwen3:4b` 对 Mood 指令的结构遵循失败。结果文件 SHA-256 为 `441deb3f3d6665cc7f45f5e2eba8cdb1d16613d8ba36a4007c3d0e98bbb4cfd0`。生成前后生产 `stories=20`，数据库完整性为 `ok`。

该负面结果不重跑、不调 Prompt，也不把 M1 失败改写为“心情提升了故事”。它说明：在此单案例和固定条件下，增加 Mood 输入没有形成可评估的合规叙事，反而触发了额外约束失败；因此当前证据只能支持“功能和审计链路可运行，但小模型未可靠消费 Mood metadata”。匿名盲评材料已经生成并通过真实 AppTest（0 exception，1 个偏好选择、6 个评分控件）；用户盲评及揭盲摘要仍待完成。原 A0--A4、N0--N3、v4/v5 记录和哈希均未覆盖或重跑。

### 27.6 用户拒绝空输出盲评后的 v6.1/v6.2 修复

用户查看第一次匿名页面后拒绝对“一个完整故事与一个空错误页”做主观质量盲评。该决定合理：M1 没有可读正文时，A/B 选择只能衡量系统可用性，不能衡量 Mood 对故事连贯性、个性化和可信度的影响。因此原 v1 结果与盲评包保留为失败审计，但不作为最终主观比较。

第一次修复命名为 `grounded-story-v6.1-structured-photographer-mood`。它在动态 JSON Schema 中要求 M1 返回至少一个 `mood_reflection{evidence_id,mood_label,text}`，M0 必须为空；validator 再检查 evidence/标签配对、第一人称和正文词义。独立 v2 真实结果表明“结构必填”仍不足：

| 变体 | 状态 | 尝试 | 延迟 | 结果 |
|---|---|---:|---:|---|
| M0 | `ok` | 1 | 27,330.624 ms | pass |
| M1 | `error` | 2 | 59,825.065 ms | `E_MOOD_REFLECTION_TEXT`、`E_MOOD_REFLECTION_VOICE`、`E_MOOD_UNUSED` |

Qwen 已输出结构化 `mood_label=happy`，但 reflection 文本没有明确写出“我感到开心”；修复稿仍不满足语义一致性。v2 manifest SHA-256 为 `a0ed8864f4fac84a959604818b75160f88d1e1860be4deb652f474d54894c53c`，结果 SHA-256 为 `dfaf4be531167abd3d30fa8e592709b0d051628bd6673966f345783afaa1e39b`。这说明小模型可以服从字段结构，却不一定正确完成“复制标签并自然改写”的跨字段语义约束。

最终生产修复命名为 `grounded-story-v6.2-deterministic-photographer-mood`。设计原则改为：**模型负责自然场景叙事，程序负责必须精确的人工 Metadata**，与程序注入 Evidence ID、引用和冲突记录的思路一致：

- M0/M1 的模型输出 JSON Schema 完全相同，不再要求 Qwen 复制 Mood 字段。
- system prompt 对 M0/M1 完全相同；M1 user context 仍含人工 Mood，模型只能把它作为克制的语气线索，不能自行输出具体心情声明。
- 应用按证据顺序读取人工标签，对每种不同标签确定性生成一条第一人称拍摄者心情句，例如“按下这张照片的快门时，我感到开心”。这些句子带 evidence ID、标签、`source=manual`、`subject_role=photographer` 和 `confirmed=true`，插入对应事实组后，并在 audit 中明确显示为 `verified_photographer_mood`。
- 程序注入不推断原因、目的、关系、照片人物情绪或医学状态；模型若在其他字段自行写具体 Mood，会触发 `E_MOOD_OUTSIDE_VERIFIED_BLOCK`。
- 该设计保证 Mood 可追踪、可展示且不会因 4B 模型复制失败而丢失，但必须诚实表述为**混合确定性生成系统**，不能声称“Qwen 学会了识别或使用心情”。

独立 v3 固定条件真实结果：

| 变体 | 状态 | 尝试 | 延迟 | Mood 反思 | Mood 覆盖 | 重复率 |
|---|---|---:|---:|---:|---:|---:|
| M0 | `ok` | 1 | 22,919.171 ms | 0 | N/A | 0.0 |
| M1 | `ok` | 1 | 22,954.938 ms | 3 | 1.0 | 0.0 |

M1 的 3 条反思分别覆盖 `happy`、`tense`、`excited` 三种不同人工标签；无无依据 Mood 声明、无修复、无 fallback。M0/M1 system prompt SHA-256 均为 `d3171b3dd91c257e7d629f5e27b0248ed7c7051c2808032c52741c483fce52b3`，模型 digest 仍为 `359d7dd4bcdab3d86b87d73ac27966f4dbb9f5efdfcc75d34a8764a09474fae7`。v3 manifest SHA-256 为 `13f34eb45b6e984d192e370934372f2d531cdd84b7721e6129396ffb191acd42`，结果 SHA-256 为 `06b30fd68b1344c5e45e5a66cc9b33dfb49e4db940e4d8cce8ed4d5d0ea9d79b`。生产 `stories` 仍为 20，数据库完整性为 `ok`。

最终完整 `unittest discover` 为 **150/150** 通过；主应用 AppTest 零异常，真实 v3 盲评包 AppTest 为 0 exception、1 个 A/B/平局控件和 6 个评分控件。新的匿名页面只含两个 `ok` 正文，满足用户提出的“不要对空故事盲评”。随后用户对 v3 页面完成并锁定评价；揭盲结果见 27.7。

### 27.7 M0/M1 单案例盲评完成与揭盲结果

2026-07-22，v3 匿名评价文件已提交并锁定。锁定后才读取独立映射文件：用户选择的匿名 **Story B 对应 M1（启用人工确认的拍摄者 Mood metadata）**。各维度评分在揭盲后重新映射到真实变体：

| 变体 | 连贯性（1--5） | 个性化（1--5） | 可信度（1--5） | 总体偏好 |
|---|---:|---:|---:|---|
| M0：无 Mood | 4 | 3 | 3 | 未选择 |
| M1：启用 Mood | 3 | 4 | 3 | **选择** |

自动指标仍为：M0/M1 均 `status=ok`、无修复、无 fallback、重复率 0.0、无无依据 Mood 声明；M1 的 Mood 覆盖率为 1.0，M0 为 N/A。两者延迟分别为 22,919.171 ms 和 22,954.938 ms，差异仅 35.767 ms。

该结果表明，在这一个热气球事件和这一次单用户盲评中，Mood 版本获得了总体偏好并提高个性化评分，但没有同时提高所有维度：连贯性从 4 降到 3，可信度保持 3。合理解释是，确定性插入的已确认心情增强了个人化感受，却可能削弱正文的自然衔接。由于只有一个案例、一个评价者且 M1 是“Qwen 场景叙事＋程序注入已确认 Mood”的混合系统，**不能报告统计显著性、总体性能提升或生理情绪识别能力**；它只能作为 future wearable affect metadata 的探索性 proof-of-concept 和安全—个性化权衡案例。

公开摘要不含照片、UUID、本地路径、故事全文或匿名 A/B 映射，`private_content_committed=false`。公开摘要 SHA-256 为 `9cda05dd606a0937369e1440200c2f2aaf240a30f89999bfc5ec0172c8c12380`；私有锁定响应 SHA-256 为 `99c5dcd179f41b7f4ce3db2bb05f03e3f539896d36fb5bdca051e6e9c6cb583a`。揭盲聚合增加了 A/B 评分到 M0/M1 的显式映射及回归测试；最终完整测试为 **151/151** 通过，公开摘要隐私扫描发现 0 个禁用字段、0 个 UUID、0 个图片或本地路径标记。原 v1/v2 失败结果继续保留，不用 v3 成功覆盖失败审计；原 A0--A4 与 N0--N3 结果也未重跑。

## 28. 2026-07-22：Qwen3:4B Story 双轨正式实验

### 28.1 新版范围与研究问题

旧 `llama3:latest` N0--N3 的输出、盲评和负面结果继续作为历史实验保留，本轮没有覆盖、重命名或用于新版主表。本轮只评估本地 `qwen3:4b`，研究问题拆为：

- **RQ2a**：在 Qwen3:4B 下，事件级证据组织与严格校验如何影响事实支持、结构合规、回退率、延迟和可读性？
- **RQ2b**：第一人称事件叙事能否比旧式自由故事提高连贯性和个性化，同时保持证据可信度？

模型 digest 固定为 `359d7dd4bcdab3d86b87d73ac27966f4dbb9f5efdfcc75d34a8764a09474fae7`；所有正式调用使用 `temperature=0.25`、`num_ctx=8192`、`num_predict=1200`、`think=False` 和由 case ID 确定的 seed。正式输出只运行一次；内容差不得重采样。所有调用均为 `save=False`，生产 `stories` 在实验前后均为 **20**。

Faithful 沿用原冻结的 12 个案例：2 单图、8 Event、2 Date，中英文各 6。Creative 重新冻结 12 个主案例和 6 个后备，排除旧 Story 正式案例、生产 Story 已使用照片、Mood 热气球案例及 2026-07-07/10/11/12 开发日期；案例无照片重叠并要求原图、主 Caption 和主 Scene Graph 有效。Creative manifest SHA-256 为 `507610b67028fc6ce5c0231e1a4df5261de8cd2f9ac5a89564b4f42786fb7f5e`。

### 28.2 代码、预检与恢复审计

新增 `qwen_story_evaluation.py`、`scripts/run_qwen_story_experiment.py`、`scripts/run_qwen_story_preflight.py`、`scripts/qwen_story_blind_review_app.py`、`scripts/audit_qwen_story_claims.py` 和 `evaluation/QWEN_STORY_PROTOCOL.md`。实现内容包括：QF0--QF3/QC0--QC2 独立输入投影、固定 digest/seed、共享首稿、原子 JSONL、身份哈希 checkpoint、后备顺序、23/24 动态盲评、声明审计和公开隐私摘要。

真实预检只使用开发案例。原拟使用的 2026-07-10 Date 在任何模型调用前被检测为与旧正式案例重叠并拒绝，随后按开发规则改用不重叠的 2026-07-11 Event。最终 Faithful 与 Creative 预检都确认共享首稿和生产数据库不变。预检报告内部 canonical hash 为 `39e533c637f2bbc5ddc3060eb14b510f2d191dd44026f8914fe0592bed2d3bee`，文件 SHA-256 为 `d8b0968d75c7ea2acb1f6ea0f701dace55f6dee0e7197a54956db85337b85431`。

Faithful 正式运行在 43/48 条时触发外层终端 3609 秒上限，当时正在第一个 Date 的 QF3 修复调用。43 条已有记录均已原子保存；随后只用相同 seed 和身份哈希恢复未落盘调用，没有重跑已完成内容。被中断调用属于进程/传输失败，按协议允许同 seed 恢复。Creative 正式运行没有传输中断。

### 28.3 Faithful QF0--QF3 结果

Faithful 生成恰好 **48 条、48 个唯一 case/variant**，每个变体 12 条：

| 变体 | 状态 | 语言合规 | 引用有效 | 组覆盖 | 平均延迟 |
|---|---|---:|---:|---:|---:|
| QF0 | 12 `ok` | 0.500 | N/A | N/A | 106.47 s |
| QF1 | 12 `invalid` | 0.000 | 0.000 | N/A | 101.43 s |
| QF2 | 10 `unvalidated`、2 `invalid` | 0.667 | 0.667 | 0.667 | 82.66 s |
| QF3 | 7 `ok`、2 `repaired`、3 `fallback` | **1.000** | **1.000** | **1.000** | 127.81 s |

QF3 fallback 率为 0.250，证据 ID accounting 为 1.000，重复率为 0.0139。QF2→QF3 语言合规差值为 `+0.333`，10,000 次 paired bootstrap 95% CI `[0.083, 0.583]`。QF1 没有任何可比较的有效文本指标，因此 QF1→QF2 重复率的配对样本 `n=0`；不能为了得到数值把 invalid 当作重复率 0。

QF0 虽然 12 条都被自由文本接口标记为 `ok`，但只检查“有字符串”，并不代表是合格故事。实际输出大量泄漏模型规划、提示要求和输入清单，且中文案例经常输出英文。该结果说明“自由文本可返回”与“可用故事”必须分开报告。

### 28.4 Creative QC0--QC2 结果

Creative 主样本严格为 **12×3=36 条**；后备样本 6×3=18 条单独报告，不混入主表：

| 主样本变体 | 状态 | 重复率 | observer 合规 | 平均正文长度 | 平均延迟 |
|---|---|---:|---:|---:|---:|
| QC0 | 12 `ok` | 0.0452 | 1.000 | 787.67 | 108.65 s |
| QC1 | 12 `unvalidated` | 0.0139 | 0.500 | 162.58 | 30.06 s |
| QC2 | 6 `ok`、2 `repaired`、4 `error` | 0.0000 | 0.667 | 124.50 | 42.01 s |

QC0→QC1 重复率差值为 `-0.0313`，95% CI `[-0.0523, -0.0132]`，说明事件分组首稿在当前样本中减少了重复。但 QC2 对 4/12 主案例两轮校验失败并返回空正式正文，系统 error 率为 0.333。因为 error 必须保留为系统结果，QC1→QC2 的语言合规和结构完整性差值均为 `-0.333`，95% CI `[-0.583, -0.083]`。这不是“validator 降低单篇合规性”，而是严格失败策略把不合规草稿变成不可用输出的系统级代价。

六个后备中 QC2 为 3 `ok`、3 `error`。因此 12 个主案例只有 8 个有效 QC0/QC2 配对，后备只补入 3 个，最终为 **11/12**。按照预注册规则，后备用尽后停止，没有继续选择第 7 个新案例，也没有放宽校验或重采样。

### 28.5 23 组盲评，而不是伪造 24 组

预注册目标是 12 个 Faithful＋12 个 Creative，共 24 组。真实可用材料只有：

- 12 组 QF0/QF3；
- 11 组 QC0/QC2；
- 合计 **23 组**。

盲评包明确写入 `status=incomplete`、`case_count=23`、`target_case_count=24`，并在页面显示黄色说明。失败 Creative 案例仍进入 4/12 主 error 率，只是不让用户对空错误页做文本质量评价。真实 AppTest 为 0 exception、23 个 A/B/平局控件、138 个评分滑块和 1 个 incomplete warning。

用户完成并锁定全部实际可用的 23 组后才读取独立 A/B 映射；23 组均有选择和六个维度评分，无重复 token、无缺失、无平局，也没有填写可选文字原因。揭盲结果如下：

| 轨道 | 基线胜 | 新方法胜 | 平局 | 双侧 exact binomial |
|---|---:|---:|---:|---:|
| Faithful：QF0 vs QF3（12组） | **QF0 12** | QF3 0 | 0 | `0.00048828125` |
| Creative：QC0 vs QC2（11组） | **QC0 11** | QC2 0 | 0 | `0.0009765625` |

Faithful 评分及“QF3−QF0”配对差值：

| 维度 | QF0均值 | QF3均值 | 差值 | 95% paired-bootstrap CI |
|---|---:|---:|---:|---:|
| 连贯性 | 3.333 | 3.000 | −0.333 | `[−0.583, −0.083]` |
| 信息量 | 3.417 | 2.833 | −0.583 | `[−1.167, −0.167]` |
| 证据一致性 | 3.333 | 3.000 | −0.333 | `[−0.583, −0.083]` |

Creative 评分及“QC2−QC0”配对差值：

| 维度 | QC0均值 | QC2均值 | 差值 | 95% paired-bootstrap CI |
|---|---:|---:|---:|---:|
| 连贯性 | 3.364 | 3.091 | −0.273 | `[−0.545, 0.000]` |
| 个性化 | 3.273 | 3.000 | −0.273 | `[−0.545, 0.000]` |
| 可信度 | 3.364 | 2.909 | −0.455 | `[−0.909, −0.091]` |

因此本次盲评已经完成所有**可用的23组**，但预注册的24组目标仍未达到，论文必须同时报告这两个事实。由于没有文字原因，不能把 12:0/11:0 具体归因于“更丰富”“更自然”或某一缺陷；只能结合可见输出和自动指标讨论可能的安全性—可读性权衡。

### 28.6 逐声明审计与失败点

审计共 2,500 个文本单元：

| 标签 | 数量 |
|---|---:|
| `supported` | 577 |
| `unsupported` | 853 |
| `uncertain` | 494 |
| `non_factual` | 576 |

该审计来源标记为 `agent_evidence_audit`，使用冻结的 BLIP/Qwen/CLIP 观察和可靠 Metadata 做保守词项/概念核对；有 Evidence ID 时只审引用证据，无引用的 legacy/标题/过渡使用整案例证据。跨语言无可靠匹配时优先标 `uncertain`，不把翻译词表缺失强行判为事实错误。Creative 明示想象过渡和提示规划泄漏标 `non_factual`。这不是人工多标注者一致性，也不证明上游视觉模型观察正确。

主样本 factual-unit unsupported 比例：QF0 `0.5812`，QF3 `0.0139`；QC0 `0.4357`，QC1 `0.1324`，QC2 `0.1860`。QF3 的低比例伴随 3/12 fallback，QC2 的结果只来自有正文单元且同时存在 4/12 系统 error，因此不能把这两个比例单独解释成完整质量提升。最明显失败包括：

1. QF1 12/12 结构无效；
2. QF0/QC0 大量泄漏“用户要求、我应该、Title、Let me write”等提示规划；
3. QC1 虽结构完整，但 observer 第一人称合规只有 0.500；
4. QC2 主样本 4/12 error，冻结后备 3/6 error；
5. 严格门控提升可检查性时，会增加延迟、fallback 或空输出。

### 28.7 对 RQ2a/RQ2b 的最终回答

- **RQ2a**：事件分组 QF2 比逐图 QF1 更容易产生可解析结构，但仍有 2/12 invalid；QF3 将语言、引用和组覆盖提高到 1.000，并把审计 unsupported 比例降至 0.0139，但代价是平均 127.81 秒、2 次修复和 3 次 fallback。用户盲评又以 QF0 12:0 选择基线，QF3 在连贯性、信息量和证据一致性评分上分别低 0.333、0.583 和 0.333。答案是：严格验证提高机器可检查的形式合规和声明安全，却没有提高本次单用户感知质量，反而形成明显的安全性—信息量/可读性权衡。
- **RQ2b**：QC1 相比 QC0 的重复率下降且生成更短，但首稿 observer 合规只有 0.500；QC2 又产生 4/12 主 error，冻结后备用尽后只形成 11 个可读配对。11 组盲评中 QC0 以 11:0 获胜，QC2 的连贯性、个性化和可信度分别低 0.273、0.273 和 0.455。因此当前证据不支持“第一人称事件叙事提高用户感知连贯性或个性化”；它改善了重复和结构化审计，却牺牲可用率，且在有效配对中也未获得偏好。
- 本实验**不比较 Qwen 与 Llama**。旧 Llama 表不进入新版主表，任何跨模型现象只能作为背景观察，不能称为受控模型比较。

### 28.8 哈希、隐私与测试

- Faithful 私有结果 canonical SHA-256：`db6644c8260bc7f21daf06b93fede945ad976c4319072076ebedfaf724769917`
- Creative 私有结果 canonical SHA-256：`9357aec30b457b44ab70408adfd4ac2201afaa96b65b232505ff391a931e4518`
- 最终公开摘要文件 SHA-256：`87e35cf8bc1aab9364bf91d74fa214f04ea9daadf78b3058132c558f9b9d477d`
- 私有盲评包 SHA-256：`b6859ad156f7a7952d2b1f6447f135843499ed4843ca1b320abe89409e0f31f2`
- 私有 A/B 映射 SHA-256：`10ce1748ed152d87cc31be75edcb04252290de2f67284d159dde33327cb76690`
- 私有声明审计 SHA-256：`85297ed63f2e68a512f08a289b4522adf8cbc9a79150422a2044007ee0e016ff`
- 私有锁定响应 SHA-256：`407a8f6d6be741b3d73821cef6e49ddbeb908d43521f4c5d645e3128b68351d0`
- 私有揭盲明细 SHA-256：`5413b375d23af748274be95a748841afe9e450aed24da59a0f7fc37e07eb9a09`

所有私有 JSON/JSONL 均由 `.gitignore` 命中；公开摘要隐私扫描为 0 个禁用字段、0 个 UUID、0 个照片/本地路径标记，`private_content_committed=false`。相关定向测试 10/10 通过；完整 `unittest discover` 为 **161/161** 通过；真实 23 组 Streamlit AppTest 为零异常。生产 `stories=20`，未运行 BLIP/Qwen Scene Graph 重建，也未修改旧 A0--A4、N0--N3 或 Mood M0/M1 输出。

### 28.9 实验状态与论文下一步

实际23组盲评、锁定、揭盲、配对bootstrap、exact binomial、公开摘要和论文记录均已完成。生产数据库仍为 `stories=20`，公开隐私扫描仍为零错误。实验技术流程至此结束，不需要再次生成 Story 或重做评分。

论文下一步是把本节压缩为：一张 QF0--QF3 自动指标表、一张 QC0--QC2 自动指标表、两行盲评结果，以及各一个 Faithful fallback 与 Creative prompt-leak/error 定性案例。必须明确这是一个用户对个人相册案例的探索性评价；Faithful 有12组，Creative只有11个有效配对，不能写成24/24完成，也不能外推为总体用户偏好。

## 29. 2026-07-25：pic2 第二批照片与 Scene Graph 合并

### 29.1 输入与目标

- 新照片：`E:\leeds\pic2\`，1228 张（1142 `.JPG` + 86 `.jpg`）。
- 新 Scene Graph：`E:\leeds\qwen_scene_graph_pic2_success\qwen_scene_graph_pic2_success.jsonl`，1224 行成功，4 张 Qwen 生成失败（`2026-04-12 180425/180550/200653.jpg`、`DSC02284.JPG`）。
- 目标：把成功批次并入现有 SQLite 规范源 + Chroma，使新照片可检索、可作叙事证据；保持身份/哈希绑定与诚实溯源。

新批次仍是 legacy 格式（有 `image_path`/`scene_graph`/`generated_at`，无生成时 `photo_id`/SHA/manifest），与 §15 的 374 批次同类，因此走恢复入口而非严格 Manifest importer。

### 29.2 恢复器扩展：子目录作用域

原恢复器（§15）假设“相册根目录 == 单一批次”，其 `album_uncovered` 覆盖校验会因已有 376 张而拦截第二批。本轮给 `scene_graph_recovery.py` 增加可选参数 `batch_relative_root`、`scripts/recover_legacy_personal_scene_graphs.py` 增加 `--batch-subdir`：`--album-root` 仍为 `photos/`（保证 `get_photo_by_path` 路径匹配与 SQLite 一致），但扫描与覆盖校验只针对 `pic2/` 子目录，键仍相对 `photos/`（即 `pic2/<name>`）。路径解析、内容哈希与 mtime 检查全部不变。新增回归测试 `test_batch_subdir_scopes_coverage_to_an_incremental_batch`：子目录作用域下相册其余照片不计入 `album_uncovered`，且子目录内完整覆盖时 `blocking_issues==0`。

### 29.3 关键处理点（如实记录）

- 布局：新照片放入子目录 `photos/pic2/`，SQLite 相对路径为 `pic2/<name>`（递归扫描存储相对 `photos/`）。与主相册 3 个同名 `DSCF0219/0323/0390.JPG` 经内容哈希比对为不同内容，子目录布局天然避免路径与哈希冲突。
- **保留 mtime**：全部照片以 `shutil.copy2` 复制保留原 mtime。1223 张 mtime 均 ≤ 各自 `generated_at`（最早 2026-07-24），通过恢复器 `modified_after_generation` 守卫；若复制时把 mtime 重置为当前时间会导致整批被拦截。
- **重复图去重**：`_20260720141833_1025_2 (32).jpg` 与 `(33).jpg` 字节完全相同（同 SHA-256），SQLite `content_sha256` UNIQUE 只保留一行；已删除冗余副本 `(32).jpg` 及其 jsonl 行（原图仍在 `E:\leeds\pic2`）。故最终新增 1223 张而非 1224。
- **失败 4 张**：本轮不进系统（用户决定）；如需可另跑严格 Manifest 重生成。
- 溯源标记：恢复记录固定 `provenance=legacy_path_time_recovered`、`generation_sha256_verified=false`、`prompt_version=legacy-...-unversioned`、`dataset_id=personal-pic2-v1`；不能描述为生成时 Manifest 哈希验证。

### 29.4 两阶段导入与最终一致性

导入前备份 SQLite + Chroma 至 `outputs/scene_graph/backups/pic2_preimport_20260725_101943/`。恢复与索引均先 dry-run 再 apply：

```text
恢复 dry-run/apply: total 1223, recovered 1223, album_uncovered 0, checksum_conflict 0, modified_after_generation 0, blocking 0
索引 dry-run/正式:  total 1223, applied 1223, skipped 0, checksum_conflicts 0, malformed 0
```

正式写入后 `scripts/report_index_state.py`：

| 存储层 | 合并前 | 合并后 |
|---|---:|---:|
| SQLite photos / Caption / FTS5 / tagged | 376 | 1599 |
| SQLite Scene Graph / triples | 374 / 5,222 | 1597 / 22,200 |
| Chroma image vectors / SG triple vectors | 376 / 5,222 | 1599 / 22,200 |
| 自动事件 / 已分配 / 未分配 | 162 / 356 / 20 | 226 / 1579 / 20 |
| 含 GPS 照片 | 258 | 317 |
| 索引失败 | 0 | 0 |

增量 CLIP+BLIP 实测 1725.68 秒（约 28 分 46 秒，1223 张，0 OOM / 0 失败）。SQLite 与 Chroma 三元组计数一致（22,200）。`unittest discover` 162/162 通过。真实 SG-only 检索烟测确认新照片可经 Scene Graph 通道召回（如 `dessert on a plate → pic2/2025-12-23 215350.jpg`、`person wearing a bow tie → 多张 pic2 图`）。

### 29.5 实验边界

本轮只扩充索引与 Scene Graph，未重跑任何正式实验。已冻结的 A0–A4 v1 检索消融与 N0–N3 / Qwen / Mood Story 正式实验均在 2026-07-16 的 376 张相册上完成，其结论仍以 376 张 case study 为准。若要在 1599 张相册上得出正式检索/叙事结论，需重新构建对应 qrels 并冻结后重跑。

## 30. 2026-07-25：Story 失败显式化 + 生产温度固定

### 30.1 用户反馈

- 忠实模式两次失败时旧行为是 `_fallback()` 输出固定模板（"事件照片记录 / 开头的照片组按可靠时间…"），把真实失败原因藏在 `warnings` 里；用户要求直接显示"生成失败 + 逐次原因"，不再展示无意义模板。
- 创意模式调高温度也会失败。

### 30.2 根因（真实复现，`save=False`，qwen3:4b，seed 固定，事件 17 张/5 组）

| 运行 | 结果 | 逐次真实原因 |
|---|---|---|
| 忠实 @ 0.25 | error | 两次均 `E_LENGTH_TRUNCATED`：`done_reason=length`，超 `num_predict=1200`，输出被截断为无效 JSON |
| 创意 @ 0.25 | ok | 一次通过 |
| 创意 @ 0.7 | ok | 一次通过 |
| 创意 @ 1.0 | repaired | 首次 `E_CREATIVE_CLICHE '仿佛时间被'`，修复后通过 |

**决定性根因（进一步定位）：** 单独提高 `num_predict`（1200→2048→4096）忠实仍 `done_reason=length` 截断，且 4096 需 4 分钟以上。检查截断原文发现 qwen3:4b 在**宽松 `format="json"` 模式**下把输入 evidence plan（`label`/`evidence_groups`/全部 `photo_ids`/`model_observations`）**逐字回显**，一直生成到 token 上限，而不是产出所需的 `{title, paragraphs[...]}`。创意之所以稳定，是因为它用**严格 `output_schema`**（结构化生成）把输出限死。所以忠实截断的真正原因是**宽松 JSON 模式下小模型的输入回显**，不是简单的预算不足。创意在低温稳定，高温引入陈词/校验失败。**全部冻结实验都用温度 0.25**，而生产创意默认 0.7、滑块到 1.0 属未验证配置，也是创意失败诱因。

### 30.3 生产改动（不影响任何冻结实验）

- 移除温度滑块；忠实/创意均固定 `STORY_DEFAULT_TEMPERATURE=0.25`。模式差异来自 **grounding 契约 + 输出结构**（忠实 v3-event / 创意 v6.2 分层第一人称），而非采样随机性。
- **给忠实路径加严格 `output_schema`（`PromptBuilder.output_schema`）**，与创意一致，强制 `{title, paragraphs[{text,evidence_ids,group_id}], uncertain_observations, unused_evidence_ids, creative_transitions(maxItems=0)}` 结构，消除输入回显与随之而来的截断（`done_reason` 由 `length` 变 `stop`）。
- 生产 `num_predict` 经参数下传提高到 `STORY_PRODUCTION_NUM_PREDICT=2048`（`OllamaGenerator.generate`/`generate_story_with_context` 新增 `num_predict` 参数）；冻结默认 `OLLAMA_STORY_NUM_PREDICT=1200` 不变，Mood 等走同一函数但不传该参数，仍用 1200，可复现性不受影响。
- 忠实两次失败在生产返回 `status="error"` + 逐次 `attempt_diagnostics`（含 `done_reason` 与校验码），UI 显示"❌ 生成失败 + 逐次原因"，不再展示模板；`_fallback` 通过新参数 `allow_deterministic_fallback`（默认 `True`）保留在代码与历史测试路径。
- `OllamaGenerator` 记录 `done_reason`，区分 `length` 截断 (`E_LENGTH_TRUNCATED`) 与普通解析/运行失败。
- 新增 3 项单测（忠实 error 路径、默认 fallback 保留、length 截断诊断）；`unittest discover` **165/165** 通过。
- 冻结路径 `story_evaluation.py` / `qwen_story_evaluation.py` 各自独立的生成器与 `NUM_PREDICT=1200` 不变，Mood 用 Creative(v6.2)，均不受影响。

### 30.4 修复后实测

| 场景 | 修复前 | 修复后 |
|---|---|---|
| 忠实 @0.25，3 张/2 组（典型事件）| — | **ok，16.6 s**，产出接地中文段落 |
| 忠实 @0.25，17 张/5 组（困难事件）| error：两次 `length` 截断（回显输入）| `done_reason=stop`、75.8 s；两次因**内容质量**失败（`E_GROUP_COVERAGE`、`E_UNSUPPORTED_CLAIM '准备'`），逐次原因显式展示 |

结论：严格 schema 已消除"截断→无意义模板"这一真实 bug，忠实在中小事件上可正常生成；困难的多组事件上 qwen3:4b 仍可能因覆盖不全或推测词校验失败，但现在是**诚实、可读的逐次失败**而非占位文本。进一步提升多组忠实成功率（如按位置绑定 `paragraph[i].group_id`、或对忠实也做 ≤4 组合并）属后续模型/prompt 调优，不改变当前诚实失败行为。

### 30.5 论文表述（可直接引用）

> Story generation exposes two user-facing modes — Faithful and Creative — that select different evidence-grounding contracts and output structures rather than different sampling randomness. Faithful forbids first person, speculation, emotion, causation and unseen events; Creative keeps an identical grounded factual layer but adds a visibly separated first-person opening/transition/closing layer. Both run at a fixed decoding temperature of 0.25 — the same value used in all frozen A0–A4 and Story evaluations — so deployed behaviour matches evaluated behaviour. An earlier build exposed a 0–1 temperature slider; high values made the narrative violate the strict validators or overrun the generation budget, so the slider was removed as a low-level parameter inappropriate for end users. Both modes use structured (schema-constrained) decoding: in loose JSON mode the small local model tended to echo the input evidence plan and run to the token limit, whereas a strict output schema bounds the response to the required title/paragraph structure and eliminates that truncation. On repeated validation failure the system reports an explicit, auditable failure with per-attempt reasons (parse/truncation or the specific grounding violation) instead of substituting a low-information deterministic template, consistent with the project's rule never to present unsupported or placeholder text as a result.

温度结论：**忠实 = 创意 = 0.25**；两模式的区别由 prompt/结构契约承担，而非温度。

## 31. 2026-07-26：忠实模式 Tier 0 结构修复 + 取消禁用词硬校验

### 31.1 背景与失败分类

§30 把"截断→无意义模板"的真实 bug 修掉后，忠实在中小事件可生成，但多组困难事件仍频繁失败。逐条审计 `GroundingValidator.validate` 后，残留失败分两类：

- **A 类·结构/簿记**（把本可程序化记账的负担压给小模型）：`E_GROUP_PARAGRAPH_COUNT`、`E_GROUP_COVERAGE`（复现命中）、`E_CITATION_MISSING/UNKNOWN/GROUP_MISMATCH`、`E_EVIDENCE_ACCOUNTING`（17 张事件两次 attempt 分别"漏报 P017""多报 P002/P003/P017"，§30 截图同源）、`E_UNCERTAIN_*`、`E_CONFLICT_UNREPORTED`、`E_CREATIVE_IN_FAITHFUL`。
- **B 类·内容**：`E_UNSUPPORTED_CLAIM`（46 个禁用词，中文子串匹配易误报，复现命中 `'准备'`）、`E_FIRST_PERSON_IN_FACT`、`E_PARAGRAPH_DUPLICATE`(>0.80)、`E_PARAGRAPH_ECHO/PROSE`、`E_LANGUAGE_*`。

根因还有两点不对齐：①忠实 prompt 只告诉模型避开 5 个 hedge，而校验器实际禁 46 词；②忠实此前**不做确定性 normalise**（创意做），把全部逐 ID 簿记交给 qwen3:4b。

### 31.2 决定（仅改生产忠实；创意不动）

采用 **Tier 0**：确定性注入 + 忠实合并到 ≤4 组；并**先取消忠实模式的禁用词硬校验**，用 `config` 开关实现，**默认关闭、可一键恢复**。

### 31.3 生产改动（`story_agent.py` / `config.py`）

**F1 确定性注入（消除全部 A 类簿记失败）：** 忠实 `PromptBuilder` 改为与创意同构的"分组契约 + 程序注入"。
- `build_story_prompt`：模型只返回 `{"title","groups":[{"group_id","factual_text"}]}`（每组一段），不再自写 `evidence_ids/uncertain/unused/paragraph 计数`；文风降为**软引导**（优先可见观察、中立第三人称）。
- `output_schema`：改为分组形状（`title` + `groups`，`minItems=maxItems=len(groups)`，`item={group_id enum, factual_text}`，`additionalProperties:false`）。
- 新增 `PromptBuilder.normalise_payload`：镜像 `LayeredCreativePromptBuilder`，但 `creative_transitions=[]`；程序确定性注入 `paragraphs=[{text, evidence_ids=组内全部, group_id}]`、`uncertain` 由 `conflicts` 生成、`unused=[]`。
- `generate_story_with_context`：`normalise_payload` 由"仅创意"扩展到**两模式都走**；`_creative_contract_errors` 经 `GroundingValidator` 对忠实也生效（`if mode in {"faithful","creative"}`）。

**F2 忠实也合并到 ≤4 组：** `_reduce_groups` 由"仅 creative"扩展到两模式都合并到 `STORY_NARRATIVE_MAX_PARAGRAPHS`，降低覆盖/重复/长度难度。

**F3 取消禁用词硬校验（开关）：**
- `config.py` 新增 `STORY_FAITHFUL_SPECULATION_BAN = False`。
- 忠实分支按开关传 `speculation_terms = None if STORY_FAITHFUL_SPECULATION_BAN else ()`，关闭时不再产生 `E_UNSUPPORTED_CLAIM`（`validate` 已支持该参数，未改校验逻辑）。
- prompt 删去"禁止 possibly/likely/..."硬措辞，改为"guidance, not a hard constraint: 如不确定是否观察到的细节就省略"。
- `E_FIRST_PERSON_IN_FACT`（第一人称）**保留**——忠实=中立第三人称的定义性约束。

### 31.4 论文影响（必须如实记录）

取消禁用词后，生产忠实**不再由校验器强制拦截"未见活动/情绪/因果/亲属"推断**，降级为"**提示软引导 + 确定性注入的证据引用可追溯**"。这与 §30.5 / 论文 III.G 此前"Faithful forbids ... speculation, emotion, causation and unseen events"的表述**不再一致**，需改写为：

> Faithful asks the model for neutral third-person observer prose grounded in visible observations, and **deterministically injects every evidence citation, uncertain observation and unused-evidence accounting** so the small local model is never responsible for per-photo bookkeeping; the production build additionally treats the list of speculation/emotion/causal/kinship terms as **soft prompt guidance rather than a hard validator gate** (config-gated, default off, restorable), relying on traceable citations plus prompt guidance. First-person narration remains hard-rejected. Creative is unchanged.

### 31.5 冻结实验不受影响（可复现性边界）

- **结果不变**：N0--N3 / QF--QC 的输出仍以 `evaluation/private/*.jsonl` 保存的快照为准。
- **校验层不变**：`story_evaluation.py` / `qwen_story_evaluation.py` 直接调用 `GroundingValidator.validate(parsed, context, "faithful", language)`，**不传 `speculation_terms`**，因此无论开关如何都保留完整 46 词硬禁——冻结 N3 仍是"严格低幻觉"。
- **已知耦合（已评估，按计划不动冻结 harness）**：`story_evaluation.py:200` 共用 `PromptBuilder.build_story_prompt`，而 `_normalised_payload` 仍按旧的 `paragraphs` 形状解析、且在 `_generate_n2/_n3` 中对**原始**模型输出做校验。由于生产 PromptBuilder 已改为分组形状 `{title,groups}`，**若有人重新运行冻结 N2/N3 的实生成**，模型会产出分组形状、与旧解析/原始校验失配而失败。冻结实验已完成、以快照为准，故 re-run 不在论文复现路径内；如未来需重新实生成，须同步给冻结 harness 的解析与校验加上 `PromptBuilder.normalise_payload`（属后续工作，未在本轮范围内）。
- **版本号不变**：`PromptBuilder.PROMPT_VERSION` 仍为 `grounded-story-v3-event`（被 N3 spec、`frozen_scope_v1.json`、`freeze_dissertation_scope.py` 引用），未升版以保持冻结清单一致；契约实质变更已在本节记录。

### 31.6 验证

- `unittest discover -s tests` **169/169 通过**（含新增 4 项：忠实分组契约确定性注入、忠实 ≤4 组合并、开关关时推测词不拦、开关开时仍拦；并改写 3 个原先喂旧 `paragraphs` 形状的忠实生成测试为分组形状）。
- `GroundingValidator.validate` 默认行为不变（直接调用仍全禁 46 词），故旧的全部直接校验测试无需改动即通过。
- 待真机验收（需 Ollama）：§30.4 的 17 张/5 组事件，忠实@0.25 `save=False` 应由 error→**ok**（覆盖/记账由注入消除、无禁用词拦截、≤4 组）；2--3 组小事件应仍 ok（回归）。

## 32. 2026-07-26：BLIP2 全量 HPC 生成、质量门控与混合 Primary 切换

### 32.1 范围与可复现性边界

本轮是冻结 A0--A4、N0--N3、QF--QC 与 Mood M0/M1 之后的**生产升级**，不覆盖或重跑既有论文实验。冻结实验中的 “BLIP Caption” 仍指 `Salesforce/blip-image-captioning-base`；本节结果必须表述为 **post-experiment BLIP2 production upgrade**。

输入为当前生产相册的 1,599 张照片（原 376 张＋`pic2` 1,223 张）。CLIP 图像向量与 Tags 在本轮之前已全部存在于本地 Chroma/SQLite；本轮只在 AIRE HPC 生成 BLIP2 Caption，不在本机运行 BLIP、BLIP2、CLIP、Qwen 或 Story 模型。

固定模型与环境：

| 项目 | 固定值 |
|---|---|
| 模型 | `Salesforce/blip2-opt-2.7b` |
| revision | `59a1ef6c1e5117b3f65523d1c6066825bcf315e3` |
| 生成参数 | `max_new_tokens=50`, `do_sample=false`, `no_repeat_ngram_size=3` |
| 精度 | FP16 |
| 运行环境 | Python 3.10.19, PyTorch 2.10.0+cu126, CUDA 12.6, Transformers 5.2.0, NumPy 1.26.4, Pillow 11.1.0 |
| GPU | AIRE NVIDIA L40S，三卡数组 |

关键哈希：

- 全量 Manifest SHA-256：`8fcc3faeeee1dd7fdb0019af65c251672ef81c9a2a9624025670da7e5c0b3219`
- 最终生成脚本 SHA-256：`5d94290843f59a9caa0b2bddb97fa98d07bf3a09399012cccfac2267057f80a2`
- 合并结果 SHA-256：`e42eb37c700eb51471df39148fad7b993e42724e7ef595e8f2e05ecb16bfb694`
- 下载归档 SHA-256：`7720774eaef1e2761b0df5415cb07d09f534f13170034b8e01d1bd240a8de060`

### 32.2 预检、失败与修复过程

1. 首次 12 图预检中，基础 BLIP Caption 12/12 完全一致、CLIP Top-3 Tags 12/12 一致，但 4 个跨硬件 CLIP 余弦低于过严的 `0.99999`。在保留 Top-3 完全一致的条件下，将跨硬件数值门槛记录为余弦 `≥0.998`、最大标签分数差 `≤0.003`；作业 `6853517` 随后 0:0 通过，BLIP2 两次确定性生成 12/12 一致。
2. 首次全量作业 `6881881` 生成 1,595/1,599；4 条被错误计为超过 50 token。根因是 Transformers 5.2 的 BLIP2 输出序列包含 32 个图像占位 token，旧脚本把它们当成文本 token。修复后改为对**解码后的 Caption**调用同一 tokenizer 且 `add_special_tokens=false`。
3. 续跑 `6882147` 后达到 1,598/1,599；`IMG_4988.jpg` 命中真实 `repetitive_word`，说明它不是计数误判。
4. 为所有照片统一加入 `no_repeat_ngram_size=3`，重新预检并全量运行。最终作业 `6882169` 三个数组任务均为 `COMPLETED 0:0`，耗时约 2 分 38 秒至 2 分 46 秒；HPC 与下载后的本地严格审计均为 1,599/1,599、0 缺失、0 契约错误。

该过程说明：**结构审计通过不等于语义质量通过**。加入三词组防重复后，`IMG_4988.jpg` 仍生成：

```text
edinburgh, scotland, scottish capital, edinburgh, edinburg, edi, ede, ed, ...
```

模型通过改变拼写规避了 trigram 约束，但内容仍是明显退化的地名/词干列表。因此该照片被明确排除出 BLIP2 生产 Caption，而不是为了得到表面上的 1,599/1,599 而放宽门控。

### 32.3 自动质量门控

对 1,599 条结构合格输出的统计：

| 指标 | 结果 |
|---|---:|
| 空 Caption / `an image` 回退 / Prompt 泄漏 | 0 / 0 / 0 |
| token 中位数 / 均值 / P95 | 8 / 8.133 / 13 |
| 达到 50-token 上限 | 1（即上述退化案例） |
| 生成参数、identity、脚本哈希变体数 | 1 / 1 / 1 |
| 完全相同 Caption 组 / 涉及照片数 | 200 组 / 731 张 |

重复 Caption 很大一部分来自连续相似照片（例如同类雕像、天空、水面），不能单独解释为生成错误；因此又进行了盲评。

在冻结 376 张相册的 12 条 dev 查询上，只比较 Caption-only BM25：

| Caption | macro nDCG@10 |
|---|---:|
| 基础 BLIP | 0.2674255944 |
| BLIP2 | 0.3630172656 |
| BLIP2 − BLIP | **+0.0955916712** |

该结果通过预设的 `−0.05` 门槛，但样本仅为 12 条 dev 查询，不能外推为 1,599 张扩展相册上的正式检索提升，也不能替代视觉准确性审核。

### 32.4 50 组匿名 BLIP/BLIP2 审核

固定 seed `20260725`，抽取旧相册 25 张与 `pic2` 25 张。用户在不知道 A/B 映射的情况下分别判断整体偏好、准确性和“哪一侧包含更多无依据细节”。50 组、150 个字段全部完成。

| 指标 | BLIP | BLIP2 | 平局 | 非平局双侧 exact-binomial `p` |
|---|---:|---:|---:|---:|
| 整体更好 | 16 | **23** | 11 | 0.3368 |
| 更准确 | 15 | **24** | 11 | 0.1996 |
| **包含更多无依据细节（越低越好）** | **15** | 23 | 12 | 0.2559 |

分组描述：

- 旧 376 张抽样：整体偏好 BLIP2 13 vs BLIP 8（4 平）；准确性 13 vs 8（4 平）；更多无依据细节 BLIP2 12 vs BLIP 8（5 平）。
- `pic2` 抽样：整体偏好 BLIP2 10 vs BLIP 8（7 平）；准确性 11 vs 7（7 平）；更多无依据细节 BLIP2 11 vs BLIP 7（7 平）。

这是同一用户对个人相册的探索性盲评。所有 exact-binomial 结果均不显著，不能声称 BLIP2 总体优于 BLIP。描述性结果体现了清楚的权衡：BLIP2 更常被认为信息更好、更准确，但也更常加入照片无法支持的细节。

盲评私有文件 SHA-256：

- case 包：`4b047e8759a7dc7df7dc73b1aea5b6ac8172c902c188da112eb00fce2ea218c6`
- A/B 映射：`63d2207f657fa2f3d99272a77896dcca97cae46357116b5a6cbc15ba0985f318`
- 完整回答：`0cfc2f4b7fb54c6df69e7931eb08630cb2a69360fb6eb23acab12a2c73025d75`

### 32.5 最终生产状态与用户决策

按严格安全门控，BLIP2 在“更多无依据细节”上被选中 23 次、基础 BLIP 15 次，因此自动建议是**不切换 primary**。用户在获知该结果及风险后，明确要求直接切换；本节将其记录为知情后的生产选择，而不是实验门控自然通过。

实际采用可回滚的混合 Primary：

| 数据状态 | 数量 |
|---|---:|
| 基础 BLIP 总记录 | 1,599 |
| BLIP2 非退化记录 | 1,598 |
| 当前 primary BLIP2 | 1,598 |
| 当前 primary 基础 BLIP | 1（`IMG_4988.jpg`） |
| 恰好一个 primary 的照片 | 1,599 |

`IMG_4988.jpg` 的旧 BLIP Caption 为 `a large church with a clock on the front`；它也不完全可靠，但至少避免保存已确认的 BLIP2 词干循环。该案例应作为 BLIP 与 BLIP2 都可能产生错误细节的定性失败例，而非宣称基础 BLIP 是真值。

切换前数据库备份：

- 路径：`tmp/spn_hpc_blip2_job/downloaded_6882169/backups/smart_photo_pre_blip2_20260726T140443Z.db`
- SHA-256：`d052a5dbc15bedad059e68a217485e2d919b7a2ea5b7f4f61b4c357bd0e612ed`

切换在一个 SQLite 事务中完成；FTS5 的 Caption 字段已刷新并抽样确认与 BLIP2 primary 一致。照片、CLIP/Scene Graph Chroma 向量、Qwen 三元组、Event 和 Story 数据均未重建或修改。若需回滚，只反向恢复 1,599 条基础 BLIP primary 并刷新 FTS5，不需要重新运行任何模型。

### 32.6 详情页 Primary Caption 模型标识修正

Primary 切换后，详情页仍固定显示 `BLIP Caption / BLIP 描述`。数据库读取本身没有错误，
问题仅在 `app.py` 的标题是历史硬编码，容易让用户误以为页面仍在使用基础 BLIP。

界面现改为根据详情查询返回的 `caption.model_name` 动态显示：

- `Salesforce/blip2-opt-2.7b`：`BLIP2 Caption / BLIP2 描述`；
- 基础 BLIP 回退：`BLIP Caption / BLIP 描述`；
- 其他模型：`Primary Caption / 当前主要描述`。

标题下同时显示完整的 `Primary model / 当前模型`，使 Library、论文截图和失败案例审计能够
区分实际 Caption 来源。截图对应的 `pic2/DSC00534.JPG` 已核对：当前 primary 是 BLIP2，
Caption 为 `a man sitting on a bench`；旧基础 BLIP Caption
`a man standing on a balcony` 保留为 non-primary。

修改没有更改 Caption、FTS、向量或 Story 数据，也没有运行任何模型。Streamlit AppTest
零异常；照片详情弹窗功能验收实际显示
`Primary model: Salesforce/blip2-opt-2.7b`。

## 33. 2026-07-26：项目从 D 盘完整迁移至 E 盘

项目运行目录由 `D:\code\vscode\Msc-code\smart-photo-narrative` 迁移为
`E:\leeds\smart-photo-narrative`。迁移采用“停止应用、SQLite checkpoint、复制、逐文件
SHA-256 校验、E 盘测试、删除 D 盘源目录”的顺序，没有直接剪切运行中的数据库。

路径审计确认 `config.py` 使用 `Path(__file__).parent.resolve()` 推导项目根目录，照片、
SQLite、Chroma 和缓存目录均相对于项目根；SQLite 中 1,599 条照片路径全部为相对路径，
Chroma metadata 中没有 D/E 盘或 HPC 绝对路径。因此迁移不需要数据库迁移、向量重建或
照片重新索引。README 的活动启动命令更新为 E 盘路径；历史实验记录中的旧路径作为审计
证据保留。

执行迁移前，用户已经主动删除根目录 `.git`，因此本次迁移基线不包含该 Git 历史；项目内
原有的 `Msc/.git` 仍作为普通子目录复制。删除后的冻结基线为：

| 项目 | 结果 |
|---|---:|
| 永久文件数（排除 SQLite `-wal/-shm`） | 2,101 |
| 总字节数 | 15,014,575,167（13.983 GiB） |
| 源/目标 Manifest SHA-256 | `daf05f79f9e338d03115d876116a05bfcf0922bed2339ef82442164899ab13df` |
| 缺失 / 额外 / 哈希不一致 | 0 / 0 / 0 |

SQLite `PRAGMA wal_checkpoint(TRUNCATE)` 返回 `(0, 0, 0)`，应用数据库和 Chroma SQLite
的 `integrity_check` 均为 `ok`。E 盘验证保持：

- 1,599 Photos、3,197 Captions、1,599 Scene Graph、22,230 Triples；
- 4,797 Tags、226 Events、31 Stories、5 Mood；
- 1,598 个 BLIP2 primary 与 1 个基础 BLIP primary，primary 异常数为 0；
- 1,599 行 FTS，FTS Caption 与 primary Caption 不一致数为 0；
- 1,599 张照片文件全部存在，数据库绝对照片路径数为 0；
- Chroma 两个集合、23,799 个 Embeddings 和 647 条 queue 记录保持不变。

迁移后从 E 盘使用原 `torchtest` 环境完成验收：

- 完整单元测试：169 项，全部通过；
- 独立 Streamlit AppTest：Library、Story Basket 和 Date evidence preview 零异常；
- 独立只读页面冒烟：Library、Search、Events、Story 全部通过；
- 真实 Streamlit 健康检查：HTTP 200，响应 `ok`，检查后进程已关闭。

首次复制后的 2,101 个文件哈希完全一致。E 盘应用启动后，只有
`indexes/chroma_v2/chroma.sqlite3` 的物理文件哈希发生变化；逐表比较确认变化仅为
Chroma 的 `acquire_write` 内部维护表由 109 行增加到 110 行，其他所有表（包括
Collections、Embeddings、metadata 和 queue）逐行哈希一致。这是正常打开持久化 Chroma
产生的运行维护记录，不是向量或业务数据变化。

测试中的 BLIP 成功/失败行来自 mock 测试数据；本轮未运行真实 CLIP、BLIP、BLIP2、
Qwen Scene Graph 或 Ollama，也未重建索引、Event 或 Story。全部验收完成后删除 D 盘
源目录内容。删除后 D 盘路径为 0 文件、0 子目录；由于当前 VS Code/Codex 工作区仍持有
目录句柄，Windows 暂时保留空目录外壳，重新打开 E 盘工作区后即可删除。后续唯一生产
工作目录为 `E:\leeds\smart-photo-narrative`。


## 34. 2026-07-26：Story 新增可切换远程 LLM 后端（DashScope qwen3.5-27b）

### 34.1 背景与目标

Story 文本生成此前**只**走本地 Ollama 的 `qwen3:4b`（`config.py` 的 `OLLAMA_MODEL`，由
`story_agent.py` 的 `OllamaGenerator` 封装）。本变更新增一条**可在前端切换**的远程 API 路径，
直接调用阿里云百炼（DashScope）OpenAI 兼容接口上的 `qwen3.5-27b`（欧洲区 `eu-central-1`），
本地小模型保留为默认与备选。

关键有利条件：`StoryGenerator` 已支持依赖注入 `generator` 参数
（`self.generator = generator or OllamaGenerator(model)`），且核心生成/校验/重试逻辑
（`generate_story_with_context`，2 次尝试 + 修复循环）完全通过 `self.generator.generate(...)`
抽象调用。因此**无需改动核心生成与校验逻辑**，只新增一个同接口的远程 generator、让工厂按后端选择、
前端加切换控件。

### 34.2 改动文件

| 文件 | 改动 |
|---|---|
| `config.py` | 新增远程后端配置段（接 `STORY_PRODUCTION_NUM_PREDICT` 之后）：`REMOTE_LLM_BACKENDS=("local","remote")`、`REMOTE_LLM_DEFAULT_BACKEND`（默认 `local`，可被 `SMART_PHOTO_STORY_BACKEND` 覆盖）、`REMOTE_LLM_BASE_URL`（含 `{WorkspaceId}` 占位，可被 `SMART_PHOTO_REMOTE_BASE_URL` 覆盖）、`REMOTE_LLM_MODEL=qwen3.5-27b`、`REMOTE_LLM_TIMEOUT=120`、`REMOTE_LLM_NUM_PREDICT=2048`。另在 `BASE_DIR` 定义之后新增自包含 `.env` 加载器 `_load_env_file()`（零新依赖，`os.environ.setdefault` 不覆盖 shell 已有值），在任何 `os.getenv` 之前执行。 |
| `story_agent.py` | ① `import os` + 引入新 config 符号；② 新增 `DashScopeGenerator`，与 `OllamaGenerator` **同接口**（`generate`/`is_available`/`get_error_message`/`last_done_reason`/`model`），用 openai SDK 调 DashScope 兼容端点，`response_format={"type":"json_object"}`、`max_tokens`、`seed`，并把 `finish_reason` 映射到 `last_done_reason`（`length` 即截断）；③ 工厂 `get_story_generator(model, *, backend)` 由单例改为**按 backend 缓存**（`local`→`OllamaGenerator`，`remote`→`DashScopeGenerator`）。 |
| `app.py` | ① 导入新 config 符号；② `story_generator(backend)` 走 `@st.cache_resource` 按 backend 缓存；③ Story 页新增「Story backend / 故事后端」选择器 + 远程提示；④ 上下文聚合 / 生成 / 重试三处统一传 `backend`；⑤ 侧边栏显示 `Backend: remote (qwen3.5-27b)` 或 `Backend: local (qwen3:4b)`。 |
| `.env`（新建） | 密钥填写位置：`DASHSCOPE_API_KEY=` 与可选的 `SMART_PHOTO_REMOTE_BASE_URL=`、`SMART_PHOTO_REMOTE_MODEL=`。 |
| `.gitignore`（新建） | 忽略 `.env` / `.env.*`，防止密钥误提交。 |

### 34.3 密钥与失败处理（可审计原则）

- **密钥来源**：API Key **仅**从环境变量 `DASHSCOPE_API_KEY` 读取（现由项目根 `.env` 加载，
  终端显式 export 优先于文件），绝不写入代码、config 默认值或前端界面。这与 §11/决策日志中
  「密钥写入配置文件 → 改为环境变量；旧凭据须撤销轮换」的教训一致。
- **失败处理**：远程调用失败（网络错误 / 缺密钥 / 缺 SDK / 占位 URL 未替换）**直接报错**，
  在现有重试循环里产生可审计的 `E_GENERATION_*` 码与 `status="error"`，**不**静默回退本地，
  与现有严格校验、可审计风格一致。
- **缺密钥或 `{WorkspaceId}` 未替换时**，`DashScopeGenerator.is_available()` 返回 `False`
  并给出明确 `get_error_message()`，`generate()` 抛 `RuntimeError`。

### 34.4 初始实现的不改动项与边界（后续远程结构修复见 §34.7）

- 核心 `generate_story_with_context` 的生成 / 校验 / 修复循环、提示词、schema、温度
  （`STORY_DEFAULT_TEMPERATURE=0.25`）均不动。
- 冻结实验路径不受影响：`story_evaluation.py`、`qwen_story_evaluation.py`、`mood_evaluation.py`、
  `scripts/run_story_acceptance.py`、`generate_diary()`（仍以默认 `local` 调用）均未改动；
  冻结的 N0–N3 / QF–QC / Mood 实验日志仍按本地 `qwen3:4b` 复现。
- **结构化输出取舍**：远程用 `response_format={"type":"json_object"}`（DashScope 兼容端点最稳），
  未用 `json_schema`；提示词已锁定 schema，且现有 `GroundingValidator.parse_payload` 会重新解析
  与校验 JSON，符合「不动核心校验」约束。若确认该端点支持 `json_schema` 响应格式，可后续加开关。
- **本地路径回归**：工厂仍缓存本地 `OllamaGenerator(qwen3:4b)`，默认 backend 为 `local`，
  本地生成行为与改动前一致。

### 34.5 验证（截至本节记录时）

- `config.py` / `story_agent.py` / `app.py` 三文件 `py_compile` 通过；`app.py` 模块级 import 无引用错误。
- 工厂按 backend 分别缓存；`local` 仍为 `OllamaGenerator(model=qwen3:4b)`，`remote` 为
  `DashScopeGenerator(model=qwen3.5-27b)`。
- 无 `DASHSCOPE_API_KEY` 或 `{WorkspaceId}` 未替换时：`is_available()=False`、`generate()` 抛
  `RuntimeError` 且不回退本地（模拟确认）。
- 经 stub 模拟 openai SDK 验证请求字段：`model=qwen3.5-27b`、`messages=[system,user]`、
  `temperature=0.25`、`max_tokens=2048`、`response_format={"type":"json_object"}`、`seed` 正确；
  `finish_reason="length"` 正确映射到 `last_done_reason`。
- `.env` 加载器经用例验证：引号剥离、`export ` 前缀、`# 行内注释` 截断、shell 优先级、空值/无等号跳过。
- **初始记录时尚未进行**：在真实 DashScope 端点上跑端到端生成（依赖用户填入有效密钥与
  WorkspaceId）；真实验收及其暴露的 Schema 问题随后已完成，见 §34.7。远程模型与本地模型的
  正式对比实验仍未进行。

### 34.6 状态与后续

- **初始状态：已实现、待真实验收**。该待办随后已在 §34.7 完成，并基于真实失败修复了远程
  Schema 传递；用户已确认生产生成成功。
- **后续（研究设想，未验证）**：远程 `qwen3.5-27b` 与本地 `qwen3:4b` 在忠实 / 创意模式下的叙事质量、
  可追溯性与幻觉差异，可作为讨论章节的扩展观察。这**不在**冻结实验范围内，若要做须单独设计实验、
  显式标注为「非冻结 / 生产扩展」，且不与 N0–N3 等已冻结结果混排。

### 34.7 真实 DashScope 调用失败、根因修复与最终验收

#### 真实失败现象

用户配置有效 API Key 与 Workspace 专用欧洲区端点后，远程服务能够返回可解析 JSON，但一次
16 张照片、4 个叙事组的生产生成连续两次被本地校验器拒绝。两次错误完全一致：

- 4 个段落均触发 `E_PARAGRAPH_TEXT`（段落没有正文）；
- 证据组覆盖失败；
- 因没有可计入的有效段落，证据核算认为 `P001`--`P016` 全部未使用，而返回值中的
  `unused_evidence_ids` 仍为空。

这不是认证、网络、照片、Caption 或 Scene Graph 失败。只读核对确认 `.env` 中 API Key 与用户
下载的业务空间 CSV 的 `apiKey` 值完全一致，远程 Base URL 与 CSV 的 `openAiCompatible` 值完全
一致，且 Streamlit 进程启动时间晚于 `.env` 修改时间；同时页面收到的是可解析 JSON，而不是
`E_GENERATION_*`、HTTP 或 JSON parse 错误。API Key 原文没有写入日志或本文档。

#### 决定性根因

初版 `DashScopeGenerator.generate()` 接收了核心 Story 层传入的动态 `output_schema`，却立即执行
`del output_schema`，请求中只保留：

```text
response_format = {"type": "json_object"}
```

`json_object` 只约束输出为合法 JSON，不会强制业务字段名。与此同时，Faithful system prompt 写着
“按照 supplied schema 返回”，但远程模型实际没有收到该 Schema。模型因而可能使用旧的
`paragraphs[].text` 或其他合法 JSON 结构；`PromptBuilder.normalise_payload()` 只读取
`groups[].group_id` 与 `groups[].factual_text`，所以四个期望组都被确定性转换为空正文，继而产生
截图中的覆盖与证据核算连锁错误。

第二次尝试此前也只把错误列表附到 Faithful user prompt，没有附回第一次被拒输出及完整结构要求，
因此远程模型容易再次输出同类结构。这解释了两次校验错误完全相同，而不是说明16张照片本身均不可用。

#### 修复

本次只修改生产远程适配与修复提示，不改变冻结实验：

1. 在 `DashScopeGenerator` 增加 `_schema_instruction()`，将每次调用的动态 JSON Schema
   确定性序列化并附加到远程 system prompt；
2. 明确要求属性名逐字匹配，并禁止用旧 `paragraphs/text` 替代
   `groups/factual_text`；
3. 继续使用 DashScope 的 `response_format={"type":"json_object"}` 保证 JSON 可解析，
   业务结构由显式 Schema、现有 normaliser 和 validator 共同约束；
4. Faithful 首稿失败后的唯一一次修复现在同时包含原始被拒输出、具体校验错误和
   `title + groups[{group_id, factual_text}]` 契约；
5. 保持两次尝试上限、温度、证据注入、冲突处理、失败显式化和“不静默回退本地”策略不变。

#### 验证结果

- 新增远程 stub 回归：确认请求 system message 包含动态 Schema、合法 `group_id` 和
  `factual_text`，同时仍传递 `json_object`、模型、温度和 token 预算；
- 新增 Faithful repair 回归：确认第二次提示包含 `<rejected_output>`、第一次原始输出、
  `E_PARAGRAPH_TEXT` 等错误及结构契约；
- Story 定向测试：54 项全部通过；
- 完整 `unittest discover -s tests`：171 项全部通过；
- 使用临时 SQLite、1 张模拟照片、1 个证据组执行一次真实 DashScope
  `qwen3.5-27b`、中文 Faithful、temperature 0.25、`num_predict=600`、`save=False` 验收：

```text
status: ok
attempt_count: 1
top_level_keys: [groups, title]
first_group_keys: [factual_text, group_id]
paragraph_count: 1
validation_codes: []
done_reason: stop
saved: false
```

真实验收没有写入生产 `stories` 表，也没有运行 CLIP、BLIP/BLIP2、Qwen Scene Graph 或本地
Ollama。用户随后在生产 Story 页面重新生成并确认成功。

#### 论文边界

这是正式 QF/QC、N0--N3 和 Mood 实验冻结后的**生产远程后端适配修复**。它证明远程结构化输出
链路能够正确工作，但单次成功不构成 `qwen3.5-27b` 故事质量优于本地 `qwen3:4b` 的证据；
既有实验数值、Prompt 哈希和结论均不覆盖或重算。如需比较远程与本地质量，应另行冻结案例、
模型版本、费用、延迟和盲评协议。

## 35. 2026-07-27：移除 72 个 CLIP 预设标签并解除其对 Story 的约束

### 35.1 动机

用户要求全面取消 CLIP 零样本预设标签（72 个词，每张照片自动 Top-3），并解除标签对 Story
（Faithful 与 Creative）生成的约束，同时不得影响检索与故事生成功能本身。

标签原有四个用途：metadata 硬过滤、FTS5 文本列、UI 展示/筛选、以及 Story 证据。其中前三者
对检索排序**无贡献**（主检索 Caption 通道已限定 `fields=("caption",)`，标签只在用户主动勾选
时作为候选集过滤器）；冻结的 qrels v1 全部 48 条查询**无一条**使用标签过滤，因此 A0--A4 结果
不受影响。

### 35.2 关键发现：`tags` 表承担两个不相关职责

`indexing_service._completion_markers` 把标签同时当作 **CLIP 完成 / 模型版本身份标记**：

```python
tags_exist = clip_tag_sources == {self._clip_tag_source}
vector_exists = photo_id in vector_ids if vector_ids_available else tags_exist
clip_exists = vector_exists and tags_exist
```

标签行的 `source`（`clip:openai/clip-vit-base-patch32:transformers`）是增量快路的完成标记，
也是 §14.3 记录的 v1→v2→v1 陈旧向量保护的载体，由三个回归测试守护
（`test_clip_version_round_trip_never_reuses_stale_vector_identity`、
`test_clip_version_tag_commit_failure_cannot_authorize_new_vector`、
`test_incremental_skip_works_with_minimal_vector_store_protocol`）。

**直接删除标签行会使 1599 张照片的完成标记全部失效**，每次索引重跑 CLIP，并丢失版本身份保护。

### 35.3 方案：拆分语义与身份，保留哨兵标记

新增 `config.CLIP_INDEX_MARKER_TAG = "__clip_indexed__"`：每张照片在原 `clip:<model>:<version>`
source 下只写这一行**非语义**标记。`replace_tags` / `list_tag_sources` / `clear_source_prefix`
全部复用，**无需 schema 迁移**，三个安全测试原样通过。哨兵在
`storage._refresh_photo_fts` 与 `retrieval_engine.get_available_tags` 中被显式排除，因此既不进
FTS5 检索文本，也不出现在 UI 筛选列表。

代码改动：`config.py`（删 `PRESET_TAGS`/`TAG_PROMPTS`，加哨兵常量）、`model_pipeline.py`
（删 `_compute_tag_embeddings`/`classify_images_batch`/`tag_embeddings`）、`indexing_service.py`
（解耦分类调用，改写哨兵，保持“清旧标记→写向量→事务写新标记”顺序不变）、`storage.py`、
`retrieval_engine.py`、`app.py`（保留 Tags 多选控件但列表为空，移除详情弹窗标签行）。

### 35.4 解除标签对 Story 的约束（三处）

| 位置 | 原行为 | 现行为 |
|---|---|---|
| `EvidenceItem.evidence_text()` | 标签参与证据分组的 Jaccard 判据 | 移除，仅 caption + 三元组 + 地点 |
| `EvidenceItem.observation_record()` | 输出 `clip_tags_model_observations`，**进入 LLM prompt** | 移除该字段 |
| `_group_has_person_observation()` | 扫描全部 observation 字段，标签命中 `people/portrait/...` 即可单独把“是否有人”判定翻为真，进而放开 Creative 第一人称资格 | 因证据中已无标签而**自动不再依据标签判定** |

`EvidenceItem.tags` 字段保留（恒为空 tuple），使冻结实验脚本 `story_evaluation.py:146`、
`qwen_story_evaluation.py:631`（引用 `item.tags`）仍可导入运行。

### 35.5 数据迁移与验证

新增 `scripts/migrate_remove_clip_tags.py`（默认只读，`--apply` 前自动备份数据库）。实测：

| 项目 | 迁移前 | 迁移后 |
|---|---:|---:|
| `tags` 行数 | 4,797 | 1,599（全部哨兵） |
| 残留语义标签行 | 4,797 | **0** |
| FTS5 中 tags 列非空的文档 | 1,599 | **0** |
| 覆盖照片 / 多 source 歧义 | 1,599 / 0 | 1,599 / 0 |

备份：`outputs/backups/tags_removal_20260727_092256/smart_photo.db`。Chroma 未被本迁移修改。

端到端验证：

- **增量快路保住**（决定性）：`rebuild_multimodal_index.py` → discovered 1599 / **indexed 0** /
  skipped 1599 / failed 0，耗时 **19.4 s**（未触发任何 CLIP 重算），向量 1599 image + 22,230 SG。
- 检索回归：hybrid 三通道正常召回；日期过滤 eligible 14；`get_available_tags()` 返回 `[]`。
- Story 回归（3 张 / 2 组事件，`save=False`）：Faithful `ok` 19.8 s、Creative `ok` 16.2 s，
  prompt 中已无 `clip_tags`。
- 全套 `unittest discover`：**173/173 通过**（新增 2 项：索引后每张恰一个哨兵且 FTS 无标签文本；
  Story 证据不含标签字段）。
- `report_index_state.py` 字段 `tagged_photos` 改名为 `clip_marked_photos`，避免误读为语义标签。

### 35.6 论文边界（必须如实声明）

- **A0--A4 检索消融**：零影响。qrels v1 无任何标签过滤，标签也从不参与排序打分。
- **N0--N3 / QF--QC / Mood**：结果已保存为静态文件，**不重跑即不受影响**；但**若重跑**，证据
  中将不再含 `clip_tags_model_observations`，输出会与原记录不同，**不得声称与原结果等价**。
- 历史 32 条故事 `grounding_json` 中的 `clip_tags_model_observations` 保持原样——那是当时真实
  使用的审计记录，删除反而不诚实。
- 论文中不应再把“CLIP 零样本标签”列为系统能力；Creative 第一人称资格判定现仅依据 BLIP caption
  与 Qwen 三元组中的人物证据。

---

## 36. 新版检索实验与单事件 Story 案例实现（2026-07-27）

按预注册方案落地三层独立实验代码，全部单元测试 **100/100 通过**；离线流水线已对真库校验。
旧 A0–A4 / N0–N3 / QF–QC 结果保持原静态文件不动，不混入新版主结果。

### 36.1 文件清单

| 层 | 文件 | 作用 | 测试 |
|---|---|---|---|
| L1 高级 LLM 配置 | `config.py`（新增 EVAL 区块） | `SMART_PHOTO_EVAL_*`：双区域、固定快照 `qwen3.7-plus-2026-05-26`、temp 0、non-thinking、区域锁 | — |
| L1 | `eval_llm.py` | `EvalLLMClient`（region-paired、视觉、非思考结构化、固定 seed、模型快照纪律）+ `RegionLock` + `mask_secret` | 21 |
| L1 | `.env.example` | 仅占位符（无真实 Key），文档化区域配对与端点格式 | — |
| L1 | `scripts/eval_lock_region.py` | freeze / show / unlock 区域锁 | — |
| L1 | `scripts/eval_llm_region_speedtest.py` | 1 预热 + 10 次固定五图请求 → P50/P95/成功率/错误类型 | — |
| L2 检索实验 | `eval_corpus.py` | 冻结 1,596 候选库、373 目标池、确定性分层抽样 300（软上限 ≤6/event、≤2/近重复组） | 13 |
| L2 | `eval_metrics.py` | Hit@1/3（近重复排除）、e2e Success、Wilson 95%、exact McNemar+Holm、P95、Rank 堆叠 | 15 |
| L2 | `eval_query_gen.py` | Qwen3.7 单图生成 + CLIP×4 干扰 + 匿名审核 + 一次修复 + 规则校验 | 14 |
| L2 | `eval_retrieval.py` | R0–R7 变体、RRF(k=60)、固定权重、两个只读 BLIP/BLIP2 FTS5 索引 | 15 |
| L2 | `eval_experiment.py` + `scripts/run_retrieval_experiment.py` | 编排：冻结→生成查询→R0–R7→指标→McNemar/Holm；写私有/公开结果 | 6 |
| L3 单事件 Story | `eval_story_case.py` | 冻结 Event 1（12 图+SHA-256）、S-F/S-C0/S-C1 生成（save=False 无 fallback）、匿名化 | 12 |
| L3 | `eval_story_review.py` | Qwen3.7 两组盲评（S-F vs S-C0；S-C0 vs S-C1），1–5 维度 + 声明审计 | 4 |
| L3 | `scripts/run_story_case_experiment.py` + `scripts/label_event_moods.py` | 生成+评审+输出；12 图 Mood 标注助手 | — |

### 36.2 已对真库校验的冻结事实

- **候选库**：CLIP ∩ 基础 BLIP ∩ BLIP2 ∩ 主 Scene Graph = **1,596** 张（含 `pic2/` 批次）。
- **目标池**：原始 376 张个人照片（`relative_path NOT LIKE 'pic2/%'`）∩ 全通道 = **373** 张。
- **抽样**：seed `spn-known-item-full-corpus-v1` → int `4259099391`，确定性分层抽 **300**。
  - 诊断：≤6/事件是瓶颈（多事件含数十张）；按“原则上”语义改两阶段软抽样——先按上限抽多样性核心（282），再放宽补足到 300。
- **两个只读 FTS5 索引**：`eval_blip_fts`=1,599、`eval_blip2_fts`=1,598；写在独立文件 `data/eval_caption_fts.db`，**不动生产 `photo_fts` / Primary**。
- **Event 1**（`9fb2f2af-ebb4-5f2b-8e7b-d380b898f043`）：12 张、12:19–16:25、全部具 BLIP2+SceneGraph；order sha256 `93f0f6529a30…`。
- **生产 Stories 基线**：实验前 = **35**；`--run` 前后断言不变（save=False）。

### 36.3 运行期依赖（不阻塞已建代码，仅阻塞真实跑数）

1. `.env` 填 `SMART_PHOTO_EVAL_*`（华北 `cn-beijing` / 德国 `eu-central-1`，Key 与 Base URL 配对）。
2. `python scripts/eval_llm_region_speedtest.py` 测速；若华北不理想，手动改 `ACTIVE_REGION=frankfurt` 重测。
3. `python scripts/eval_lock_region.py freeze` 冻结区域（锁定后不得中途混区；模型快照不可用即停，不静默换别名）。
4. 检索：`run_retrieval_experiment.py --full --limit 5` 冒烟 → `--full`（全 300）。
5. Story 案例：`scripts/label_event_moods.py` 标注 12 图 Mood（`paris_20251218_mood_v1`）→ `run_story_case_experiment.py --run`。
   - 生成用生产远端 `qwen3.5-27b`（`DASHSCOPE_API_KEY`）；评审用实验 `qwen3.7`（`SMART_PHOTO_EVAL_*`）。

### 36.4 论文边界（必须如实声明）

- 检索 benchmark 是**由固定视觉 LLM 生成并自审核的合成 known-item 集**，不冒充人工查询集；查询生成失败的目标保留、不替换。
- R7（日期+地点过滤）必须**同时报告过滤前后候选池大小**，不得把候选缩减误称为语义排序提升。
- 预注册比较用双侧 exact McNemar + Holm 校正；**不再报告 Recall / MRR / nDCG / 六类宏平均**。
- Story 案例为**单事件定性案例研究**（曾在旧本地 Qwen 后备出现 Prompt 泄漏），不声称总体代表性、不做显著性检验。
- S-C0 与 S-C1 **唯一实验变量是 Mood**（单元测试断言两上下文仅 `use_photographer_mood` 不同）；Faithful 完全忽略 Mood。
- 私有结果含查询/UUID/故事全文/Mood；公开摘要仅聚合指标 + 匿名节选 + SHA-256。所有照片/DB/Chroma/Prompt/脚本/结果保存哈希。

### 36.5 待生成论文产物（需真实跑数后填充）

- R0–R6 主结果表（Hit@1/3、e2e Success、P95、Wilson 区间）。
- R6-M / R7 元数据对比表（同一 60 条）。
- Rank 1 / Rank 2–3 / Miss 堆叠条形图。
- 三故事对比表 + 评审维度分。
- 旧 Prompt 泄漏 vs 当前输出的批判性案例分析。

---

## 37. 检索实验最终结果 + 调权消融（2026-07-28，德国 Qwen3.7）

### 37.1 本轮修复（相对 §36 的实现）

| 修复 | 内容 |
|---|---|
| FTS AND→OR | `eval_retrieval._fts_query` 改为 OR，对齐生产 `match_all_terms=False`（原 AND 把 R1/R2 误打成 0） |
| R7 候选池落盘 | `run_experiment` 记录 `pool_before/after_median/...` 到私有+公开结果（规范 §36.4 要求） |
| 查询词数上限 15→20 | `QUERY_MAX_WORDS=20`，生成器+审核器 prompt 同步改 5–20 |
| 人工修正 48 条 | `outputs/failed_review/` 由用户人工校对后并入，300 条全部可用 |
| 调权消融脚本 | `scripts/reweight_ablation.py`、`scripts/retrieval_human_ablation.py` |

### 37.2 主结果 R0–R7（300 条人工查询，固定 k=60，CLIP1.0/cap0.8/SG0.9）

指标为 Hit@1 / Hit@5（@3 已弃用）。300/300 有效 → **Success@k == Hit@k**（分母均 300）。

| 变体 | Hit@1 | Hit@5 | 说明 |
|---|---|---|---|
| R0 CLIP | **0.713** | **0.937** | 最强单通道 |
| R1 BLIP BM25 | 0.203 | 0.427 | |
| R2 BLIP2 BM25 | 0.337 | 0.520 | BLIP2 > BLIP |
| R3 SceneGraph | 0.237 | 0.457 | |
| R4 CLIP+BLIP | 0.460 | 0.773 | |
| R5 CLIP+BLIP2 | 0.540 | 0.803 | |
| R6 全融合 | 0.503 | 0.820 | 真·三通道融合 |
| R6-M（60 子集） | 0.417 | 0.750 | R6 在元数据子集的配对基线 |
| **R7（子集+日期地点过滤）** | **0.800** | **0.983** | 候选池中位 3 |

McNemar（Holm 校正）：R0 vs R4 @1 **b=94/c=18 p<0.0001**、@5 **b=63/c=14 p<0.0001**（CLIP 单通道在两个截断都显著优于融合）；R5 vs R6 @1/@5 均 n.s.；R7 vs R6-M @1 **b=1/c=24 p=2e-6**、@5 **b=0/c=14 p=1.2e-4**（候选缩减在两个截断都显著）。

### 37.3 调权消融一：固定权重网格（CLIP+BLIP2+SG，w_clip=1.0 固定）

复用 237 有效查询，扫 w_blip2/w_scene ∈ [0,1]（121 点）。**最优 = w_blip2=0, w_scene=0 = 纯 CLIP**，无任何权重能超 CLIP 单通道。结论：弱通道（caption/SG）对视觉描述查询的 top-1 无互补信号，只注入噪声。

### 37.4 调权消融二：k + 权重联合网格（300 人工查询）

| 网格 | 范围 | 最优 | Hit@1 | Hit@5 | vs R0 |
|---|---|---|---|---|---|
| Grid A：CLIP+BLIP | k∈{1,5,10,20,60} × w_blip∈{.05,.1,.2,.4,.8} | k=1, w_blip=0.05 | 0.713 | 0.937 | = R0（忽略 BLIP） |
| CLIP+BLIP2（套用 A 最优） | k=1, w_blip2=0.05 | — | 0.713 | 0.937 | = R0 |
| Grid B：CLIP+BLIP2+SG | k×w_blip2×w_scene（125 点） | k=1, w_blip2=0.4, w_scene=0.2 | **0.720** | **0.940** | +0.007/+0.003（噪声内） |

**核心发现**：
1. **k=1 统治所有最优**——Hit@1 只看第 1 名，k 越小 rank-1 权重越主导；规范 k=60 对 Hit@1 是次优。
2. **基础 BLIP 是纯噪声**（Grid A 最优=忽略）；BLIP2+SG 联合调权最多 +0.7pp（@1）/ +0.3pp（@5），在噪声内（约 2/300）。
3. **规范 k=60 下融合显著劣化**（R0 vs R4 @1/@5 均 p<0.0001）；即使放宽到 @5，纯 CLIP（0.937）仍胜所有融合（R6 0.820）。
4. **@5 下融合贡献更可见但仍不及 CLIP**：R6(0.820)>R5(0.803)>R4(0.773)，加通道对 top-5 召回略有帮助，但天花板仍是 R0。
5. **唯一显著大幅提升是 R7 元数据过滤**（@1 0.417→0.800、@5 0.750→0.983），且是候选空间缩减（池中位 3），非语义增强。

### 37.5 论文结论（强化版）

- 在**合成视觉描述型 known-item 查询**上，**CLIP 单通道 ≈ 调参后多模态融合**（差距 ≤0.7pp，噪声内）；固定 k=60 下融合显著劣化。
- 真正有效的是**元数据硬过滤**（R7），须表述为候选空间缩减。
- **边界**：结论仅对视觉描述查询成立，不外推到自然语言查询。

### 37.6 产物

- `evaluation/private/retrieval_results_n300.json`（当前：OR + 20词 + 人工修正，252 LLM-ok + 48 人工）
- `evaluation/private/retrieval_results_n300_AND_baseline.json`（AND bug 基线，审计用）
- `evaluation/private/retrieval_results_n300_OR_237valid.json`（OR/15词中间态）
- `evaluation/private/reweight_ablation.json`（消融一）
- `evaluation/private/retrieval_human_ablation.json`（消融二：主结果 + Grid A/B）
- `outputs/queries_300.csv`（300 条描述，UTF-8-sig）
- `outputs/failed_review/`（48 条人工校对照片 + 描述清单）

### 37.7 网格消融完整明细（Hit@1 / Hit@5，w_clip=1.0 固定）

**McNemar（Holm 校正）**

| 比较 | 截断 | b | c | p | 结论 |
|---|---|---|---|---|---|
| R0 vs R4 | @1 | 94 | 18 | <0.0001 | R0 显著优 |
| R0 vs R4 | @5 | 63 | 14 | <0.0001 | R0 显著优 |
| R5 vs R6 | @1 | 35 | 24 | 0.19 | n.s. |
| R5 vs R6 | @5 | 20 | 25 | 0.55 | n.s. |
| R7 vs R6-M | @1 | 1 | 24 | 2e-6 | R7 显著优 |
| R7 vs R6-M | @5 | 0 | 14 | 1.2e-4 | R7 显著优 |

**Grid A：CLIP+BLIP，Hit@1 矩阵**（行 k，列 w_blip）

| k＼w | 0.05 | 0.1 | 0.2 | 0.4 | 0.8 |
|---|---|---|---|---|---|
| 1 | **0.713** | 0.713 | 0.713 | 0.707 | 0.647 |
| 5 | 0.713 | 0.713 | 0.700 | 0.680 | 0.597 |
| 10 | 0.713 | 0.710 | 0.693 | 0.617 | 0.553 |
| 20 | 0.710 | 0.670 | 0.647 | 0.577 | 0.497 |
| 60 | 0.657 | 0.630 | 0.553 | 0.513 | 0.460 |

> 右下角 k=60/w=0.8 = 规范 R4 = 0.460（最差格）。Hit@1 随 w_blip↑ 和 k↑ 单调下降。

**Grid A：CLIP+BLIP，Hit@5 矩阵**

| k＼w | 0.05 | 0.1 | 0.2 | 0.4 | 0.8 |
|---|---|---|---|---|---|
| 1 | 0.937 | 0.937 | 0.933 | 0.937 | 0.940 |
| 5 | 0.937 | 0.937 | 0.937 | 0.953 | 0.943 |
| 10 | 0.937 | 0.937 | 0.947 | **0.960** | 0.950 |
| 20 | 0.937 | 0.937 | 0.953 | 0.953 | 0.893 |
| 60 | 0.940 | 0.957 | 0.933 | 0.860 | 0.773 |

**Grid A 最优 + 套用到 CLIP+BLIP2**

| 配置 | Hit@1 | Hit@5 |
|---|---|---|
| R0（参考） | 0.713 | 0.937 |
| Grid A 最优(Hit@1)：k=1, w_blip=0.05 | 0.713 | 0.937 |
| Grid A 最优(Hit@5)：k=10, w_blip=0.4 | 0.617 | **0.960** |
| CLIP+BLIP2 套用 A 的 Hit@1 最优(k=1,w=0.05) | 0.713 | 0.937 |

**Grid B：CLIP+BLIP2+SG 前列配置**

| 排序 | k | w_blip2 | w_scene | Hit@1 | Hit@5 |
|---|---|---|---|---|---|
| Hit@1 最优 | 1 | 0.40 | 0.20 | **0.720** | 0.940 |
| Hit@1 第2 | 10 | 0.05 | 0.05 | 0.720 | 0.940 |
| Hit@1 第3 | 1 | 0.20 | 0.20 | 0.720 | 0.937 |
| Hit@5 最优 | 10 | 0.40 | 0.20 | 0.670 | **0.960** |
| Hit@5 第2 | 10 | 0.40 | 0.10 | 0.690 | 0.957 |
| Hit@5 第3 | 60 | 0.10 | 0.05 | 0.687 | 0.957 |

**Hit@1 vs Hit@5 权衡读数**：
1. **Hit@1**：纯 CLIP（0.713）最优；任何融合都拉低，规范 R4（k=60/w=0.8）最差（0.460）。
2. **Hit@5**：调参融合（k≈10, w_caption≈0.4）能到 **0.960**，比纯 CLIP（0.937）高 **+2.3pp**，但代价是 Hit@1 掉 ~4–10pp——top-1 与 top-5 召回的权衡。
3. **k 是关键旋钮**：k=1 最大化 Hit@1（rank-1 主导）；k≈10 最大化 Hit@5（更平滑）。
4. **R7 元数据过滤**是唯一在两个截断都大幅且显著提升的（@1 0.417→0.800、@5 0.750→0.983），属候选空间缩减。

---

## 38. 单事件 Story 案例结果（Phase 4，2026-07-28）

### 38.1 设置

- 事件 `9fb2f2af`（2025-12-18 · Event 1，12 张，全部 BLIP2+SG），order sha256 `93f0f6529a30`。
- Mood 集 `paris_20251218_mood_v1`（12/12 人工标注，分布 excited×3/happy×4/calm×3/neutral×2）。
- 生成：生产远端 `qwen3.5-27b`，temp 0.25，max_tokens 2048，固定 seed，`save=False`，无 fallback。
- 评审：实验 `qwen3.7`（德国，冻结区域），temp 0，固定 seed，12 图+元数据+Mood，故事匿名 A/B。
- **生产 Stories 计数实验前后均 = 35（不变，save=False 验证通过）。**

### 38.2 三故事

| 故事 | 模式 | Mood | 状态 | 段落 | 字数 | 创意过渡 |
|---|---|---|---|---|---|---|
| S-F | faithful | off | ok | 4 | 231 | 0 |
| S-C0 | creative | off | repaired | 4 | 353 | 2 |
| S-C1 | creative | on | repaired | 4 | 302 | 2 |

### 38.3 评审一 S-F vs S-C0（连贯/证据一致/第一人称/想象/未支撑声明）

S-C0 全面优于 S-F：coherence 5 vs 4、evidence_consistency 5 vs 3、first_person 5 vs 1、unsupported_claims 1 vs 2（低=好）。Creative 观察者胜 Faithful。

### 38.4 评审二 S-C0 vs S-C1（Mood 单变量，A 版：LLM 织入 Mood）

**A 修复（2026-07-28）**：原设计 Mood 是确定性模板附录（评审判为脱节、覆盖不全，S-C1 反而低于 S-C0）。改为 **LLM 把每张照片 Mood 织入叙事**（放开 prompt + 评审器 `E_MOOD_OUTSIDE_VERIFIED_BLOCK`/`E_MOOD_REFLECTION_REQUIRED`，保留"只用核实标签/禁编因"护栏，禁确定性注入）。

修复后 **S-C1 全面胜 S-C0**：

| 维度 | S-C0（无Mood） | S-C1（Mood织入） |
|---|---|---|
| 连贯 | 4 | **5** |
| 个性化 | 3 | **5** |
| mood_usage | 1 | **5** |
| mood_credibility | 1 | **5** |
| 虚构情绪成因（低=好） | 1 | **1** |

S-C1 正文每段织入第一人称 Mood（"Capturing this scene, I felt neutral at first, then happy..."/"I felt excited by the architecture..."），4 种 Mood 全覆盖，**不编造成因**（"without explanation"，invents_emotion_cause=1，non_factual=0）。评审：S-C1 成功整合 Mood 标签，S-C0 无 Mood 数据判为不完整。

### 38.5 定性结论（单事件，不做显著性检验）

1. **Creative 观察者 >> Faithful**：本事件 Creative 在连贯/证据一致/第一人称上远胜。
2. **Mood 织入后 S-C1 >> S-C0**（A 修复后）：mood_usage/credibility/个性化 5:1，且防幻觉护栏生效（不编情绪成因、non_factual=0）。
3. **设计教训**：确定性模板 Mood 附录读起来弱（脱节/覆盖不全）；LLM 织入（仅用核实标签、禁编因）显著更好。

### 38.6 边界与产物

- **单事件定性案例**（曾在旧本地 Qwen 后备出现 Prompt 泄漏），不声称总体代表性、不做显著性检验。
- S-C0 vs S-C1 唯一实验变量是 Mood（单元测试断言两上下文仅 `use_photographer_mood` 不同）。
- 产物：`evaluation/private/story_case_event1.json`（故事全文+Mood+评审明细+SHA-256，私有）；`evaluation/public/story_case_event1_summary.json`（评分表+声明审计计数+匿名，可公开）。

